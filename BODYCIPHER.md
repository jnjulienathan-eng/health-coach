# BODYCIPHER
_Single source of truth. Read at the start of every Claude Code session. Update at the end of every session._
_Last updated: September 9, 2026 (Session: wake_time field — follow-up to the sleep_analysis debug logging hook below, which confirmed the raw HAE payload includes `inBedEnd`/`sleepEnd` (identical values in every real sync so far — Apple Watch's auto-detected sleep doesn't distinguish "stopped sleeping" from "got out of bed"). **Task 1:** removed the now-unneeded `TEMPORARY DEBUG` `console.log` block for `sleep_analysis` in `app/api/health-import/route.ts` — the existing `inBedStart`→`bedtime`/`totalSleep`→`sleep_duration_min` parsing block was untouched. Committed and pushed separately before Task 2's migration. **Task 2 — new `wake_time` column:** `ALTER TABLE daily_entries ADD COLUMN IF NOT EXISTS wake_time text;` — flagged and run by Julie in the Supabase SQL editor before any code referencing the column was written. `inBedEnd` → `wake_time` wired into the existing `sleep_analysis` handling in `app/api/health-import/route.ts`, same HH:MM substring extraction as `inBedStart`→`bedtime`. **Write rule: overwrite-on-change, not COALESCE** — unlike `bedtime`/`sleep_duration_min` (manual entry wins), there's no manual entry point for `wake_time` anywhere in the app, so a webhook write can't clobber anything; pattern mirrors `apple_hrv_avg`'s equal-value skip guard (a re-sent day with an unchanged `wake_time` doesn't count toward `metricsImported`). Added to `MetricPoint`/`DayMetrics` types and the existing row-fetch `.select()` list in the same file. **Data plumbing only, no UI:** `wake_time` added to `SleepData` (`lib/types.ts`) and `emptySleep()`, and to `rowToEntry()` (`lib/db.ts`) so it's readable from the entry object — deliberately NOT added to `saveEntry()`'s upsert payload, same as `apple_hrv_avg`, since it's entirely webhook-owned. No changes to `SleepSection.tsx`, the Glucose Stability card, or Behavior/Outcome Score — out of scope this session, landed ahead of a future session that reads `wake_time` on the Glucose Stability card. **Files changed:** `app/api/health-import/route.ts` (debug block removed; `inBedEnd` field + parsing + overwrite-on-change write logic + select-list addition), `lib/types.ts` (`SleepData.wake_time`, `emptySleep()`), `lib/db.ts` (`rowToEntry()`). Verified: `tsc --noEmit` and `eslint` both clean. Not observable in the browser preview (webhook route gated by `x-api-key`, no UI surface added) — not exercised against a live HAE payload this session. Not merged — pushed to `claude/wake-time-field`; no PR opened. Previous: September 9, 2026 (Session: sleep_analysis debug logging hook. Temporary, read-only investigation — no parsing, mapping, or write logic changed. **Goal:** confirm whether Health Auto Export's `sleep_analysis` metric includes an end-of-sleep or end-of-time-in-bed timestamp (e.g. `inBedEnd`/`sleepEnd`) alongside the `inBedStart`/`totalSleep` fields this route already reads — not yet confirmed against a real payload. **Change:** in the existing per-metric loop in `app/api/health-import/route.ts` that already logs unrecognized metrics and does the glucose-substring CGM-prep sample log, added a third check — `metric.name === 'sleep_analysis'` — that logs the full raw metric object (`JSON.stringify(metric)`, all data points, every key HAE sends) rather than a truncated sample, since sleep_analysis typically carries few points per sync. Placed alongside, not inside, the existing `sleep_analysis` parsing block further down (which still only reads `inBedStart`/`totalSleep` and is untouched) — this is pure logging, no behavioural change; `bedtime`/`sleep_duration_min` continue to be written exactly as before. **Marked TEMPORARY in a code comment** — meant to be removed once a real overnight HAE sync confirms the field shape from Vercel logs. **File changed:** `app/api/health-import/route.ts` — one `if (metric.name === 'sleep_analysis')` block with one `console.log` added to the first per-metric loop (no other lines touched). Verified: `tsc --noEmit` clean. Not observable in the browser preview (webhook route gated by `x-api-key`, no UI touched) — not exercised against a live HAE payload this session; next real overnight sync will produce the log output for review. Not merged — pushed to `claude/sleep-analysis-debug-logging`; no PR opened. Previous: September 9, 2026 (Session: CGM low-glucose connector build, Session A — four tasks, confirmed/approved individually before proceeding. **Task 1 (read-only, no changes) — confirmed `user_profiles` schema:** queried the live table directly via the anon key (`select=*`, one existing row) rather than `information_schema` (not exposed to anon via PostgREST). Columns: `user_id` (**text**, value `'julie'` — matches the same convention as daily_entries/training_sessions/health_appointments/glp1_injections, not uuid), `display_name`, `hrv_baseline`, `rhr_baseline`, `created_at`. No separate `id` column — `user_id` is the key. No app code reads/writes this table currently (confirmed via grep — it predates the current model). Corrects the stale "Preferences, macro targets" description in DATA MODEL → `user_profiles` below. **Task 2 (SQL output only, not run by Claude Code) — `wearing_cgm` migration:** `ALTER TABLE user_profiles ADD COLUMN IF NOT EXISTS wearing_cgm boolean NOT NULL DEFAULT true;`, run by Julie in the Supabase SQL editor before Task 3 began. **Task 3 — LibreView library investigation:** installed `libre-link-unofficial-api` (alpha, TypeScript) as a dependency. Wrote a throwaway script (deleted after use, per instruction) that authenticated against a real LibreLinkUp account via `LIBRE_LINK_EMAIL`/`LIBRE_LINK_PASSWORD` env vars (never pasted into chat — Julie created a git-ignored `.env.libre.local` herself) and called `login()`/`fetchConnections()`/`fetchReading()`/`read()`/`history()` with `VERBOSE=true` to inspect real response shapes. **Found and fixed a live blocker:** the library's hardcoded default `lluVersion` (`4.7.0`) is below Abbott's currently-enforced minimum, causing every call past `login()` to fail with `403 / {status: 920, data: {minimumVersion: "4.16.0"}}` — fixed by passing `{ lluVersion: '4.16.0' }` explicitly. **Key finding:** `history()`/`graphData` returned 141 real intraday readings spanning ~11h40m at ~5-minute cadence (each with `ValueInMgPerDl`, a pre-converted mmol `Value`, `Timestamp`, and `FactoryTimestamp`) — fundamentally more granular than the existing `cgm_readings` write path (HAE → one row per day, a daily average). `Timestamp` ran ~2h ahead of `FactoryTimestamp` on every reading, consistent with a Berlin/CEST device-local adjustment on `Timestamp` vs. sensor-true-UTC on `FactoryTimestamp`. **Task 4 — built the connector:** see DATA MODEL → `cgm_readings` below for the full `app/api/cgm-import/route.ts` writeup, and the `user_profiles` entry below for the `wearing_cgm` column. New files/changes: `app/api/cgm-import/route.ts` (new), `vercel.json` (three new cron entries), `package.json`/`package-lock.json` (already committed in Task 3), `BODYCIPHER.md` (this entry + DATA MODEL corrections). Verified: `tsc --noEmit`, `eslint`, `next build` all clean (route registered as dynamic `ƒ`). Not exercised end-to-end against live Supabase or a live cron trigger — no local `SUPABASE_SERVICE_ROLE_KEY` (same documented limitation as every other service-role route) and Vercel Cron only fires once deployed; the LibreLinkUp auth/`history()` call path itself was exercised live in Task 3 with the same library/credentials/`lluVersion` fix, just not through this route's actual write. No DB migrations run by Claude Code this session beyond what's noted above (Task 2's migration was run by Julie). Not merged — pushed to `claude/cgm-libre-connector-session-a`; no PR opened. Previous: September 8, 2026 (Session: Sleep fasting-glucose decimal fix, `/api/cgm` source filter, Glucose Stability card rebuilt to 5 rows, and a documentation correction. **Documentation correction (no code involved):** the `cgm_readings` DATA MODEL entry below was stale — it said the table doesn't exist in Supabase yet and the read path was pushed to a branch but never merged. Both were wrong, confirmed by Julie this session: the table exists and is live, the unique constraint on `(user_id, recorded_at)` is applied (after a duplicate row was found and manually deleted), the read path is merged, and real `GlucosePhone` data is flowing. Corrected in place in DATA MODEL → `cgm_readings` below — no migration run this session, none needed. **Task 1 — Sleep, fasting glucose decimal entry:** `components/sections/SleepSection.tsx`'s shared `NumInput` gained optional `step`/`inputMode` props (default unchanged, so HRV/RHR/Nap are unaffected); the fasting glucose field now passes `step="0.1"` `inputMode="decimal"`. Root cause was the mobile keypad (`inputMode="numeric"` has no decimal key), not the parse logic — `Number(e.target.value)` already handled decimals correctly. **Task 2 — `/api/cgm` gained an optional `?source=` filter:** applied via `.eq('source', source)` before the existing order/limit chain when present; behaviour unchanged when absent. Added ahead of a future second write feed (a LibreView connector) that will write into `cgm_readings` under a different `source` value than `GlucosePhone`. **Task 3 — Glucose Stability card rebuilt to 5 rows** (`app/page.tsx`): Waking Glucose (manual, `daily_entries.fasting_glucose_mmol`) → Day Average (`/api/cgm?source=GlucosePhone`) → Meal Spikes Today (from the existing `/api/nutrition/day` route) → Low Events Today (empty state only, no source yet) → HbA1c (unchanged, moved last). The 7-day fasting-average display and the mockup's "log lows from screenshot" flow are both dropped. Full detail in the dedicated Today Tab entry below and in DATA MODEL → `cgm_readings`. Verified: `tsc --noEmit`, `eslint`, `next build` all clean; exercised in local dev browser preview (toggle on/off, all rows both populated and empty) — underlying data fetches 500 locally, same pre-existing missing-`SUPABASE_SERVICE_ROLE_KEY` limitation as every other service-role route, not a regression. **Bugfix pass, same day, on live-testing feedback:** the Day Average caption ("Today's average so far · as of yesterday") was self-contradictory — root cause was `formatAsOf()` comparing UTC date strings against a Berlin-midnight-stamped `recorded_at`, misclassifying that day's own reading as "yesterday" for most of each Berlin day; fixed by switching `formatAsOf()` to Berlin-calendar-date comparison and making the caption prefix conditional on the result ("Today's average so far" vs. "Full-day average"). Also reworded the CGM toggle's caption (previously described the effect of switching it off while displayed in the ON state) and confirmed via code trace that Waking Glucose is already correctly date-specific with no fallback. Full detail and verification-question answers in the dedicated Today Tab entry below. Not merged — pushed to a new branch; no PR opened. Previous: September 7, 2026 (CGM read path — Glucose Stability card wired to real data. Follow-up to the CGM real write path below, now that `cgm_readings` has real data flowing in. **Important correction, confirmed by Julie this session:** `cgm_readings` is one row **per day** (that day's running glucose average from Apple Health/HAE), **not** an intraday stream — the write-path entry below's "one row per timestamped reading" framing was written before real data existed and is now corrected in DATA MODEL → `cgm_readings`; no code changed as a result, only the documented assumption. **New route:** `app/api/cgm/route.ts` — `GET` returns the most recent `cgm_readings` row (`recorded_at`, `value_mmol`, `source`) for `user_id = 'julie'`, or `null`. Service-role via `supaAdmin()`, inline query (no `lib/db.ts` function, matching `/api/glp1`'s minimal pattern), `dynamic = 'force-dynamic'` + `revalidate = 0`. **Card wiring (`app/page.tsx`):** the Glucose Stability card's CGM Data row — previously always showing "CGM data not connected" regardless of the (inert) CGM toggle — now shows the real reading (`{value.toFixed(1)} mmol/L`, same three-colour threshold as the Fasting Glucose readout directly above it in the same card: optimal ≤4.8 / moderate ≤5.6 / low above) plus a relative "As of today/yesterday/{date}" label, via a new `formatAsOf()` helper alongside the existing `todayStr()`/`yesterdayStr()` utilities. Falls back to the original "CGM data not connected" text when no rows exist yet (table isn't even created in Supabase yet — migration still pending). New `fetchLatestCgmReading()` wrapper + `cgmReading` state, loaded in the same effect as `fetchGoalsData()`. **Explicitly not touched:** the CGM toggle checkbox (`cgmEnabled`) — still renders exactly as before, unwired, per instruction; no chart/spectrum/trend view — out of scope per instruction. New `CgmReading` type in `lib/types.ts`. Verified: `tsc --noEmit`, `eslint`, `next build` all clean. Not exercised against live data this session — no local `SUPABASE_SERVICE_ROLE_KEY`, and `cgm_readings` still doesn't exist in Supabase (migration written in a prior session, not yet run by Julie). Not merged — pushed to a new branch; no PR opened. Previous: September 7, 2026 (CGM real write path — following up on the same-day defensive prep below, now with a confirmed payload. Logging hook confirmed the real Health Auto Export mapping: metric name `blood_glucose` (exact), units `mmol/L`, value field `qty`, timestamp field `date` (`"YYYY-MM-DD HH:MM:SS +offset"`), source field `source` (confirmed `"GlucosePhone"`). **Migration SQL updated, not run:** `cgm_readings`' CREATE TABLE (still not run — table doesn't exist in Supabase yet) now includes a unique constraint on `(user_id, recorded_at)`, so a resent export containing already-written readings can be skipped rather than duplicated. Written to DATA MODEL → `cgm_readings` for Julie to run herself in the Supabase SQL editor. **Write logic added to `/api/health-import`:** the existing glucose-substring logging hook (below) is unchanged; a new exact-name (`blood_glucose`) write path was added alongside it. Every entry in the metric's `data` array is processed individually (not aggregated by day like most other metrics in this route, since a real export can carry many readings once the sensor's been running) — `qty` → `value_mmol`, `date` → `recorded_at` (passed through as-is, same convention as `training_sessions.start_time`), `source` → `source`, `user_id = 'julie'`. Inserted via `supaAdmin()`, duplicate-skipped by catching a `23505` unique-violation on `(user_id, recorded_at)` — same insert-then-catch pattern as `vo2_max`'s `biomarker_readings` dedup, never overwrites an existing reading. Tracked as `cgmImported`/`cgmSkipped` counters, included in the route's summary log line and JSON response. `blood_glucose` added to `RECOGNIZED_METRIC_NAMES` so it no longer also logs as "unrecognized". **File changed:** `app/api/health-import/route.ts` — `blood_glucose` added to `RECOGNIZED_METRIC_NAMES`; a `cgmReadings` array populated in the existing per-metric-point loop; a new CGM write section (insert-per-reading, 23505 skip) added between the WAIST and WORKOUTS sections; final summary log line and JSON response extended with `cgm`/`cgmSkipped`. Verified: `tsc --noEmit`, `eslint`, and `next build` all clean. Not observable in the browser preview (webhook route gated by `x-api-key`, no UI touched) — not exercised against a live Health Auto Export payload this session (no local `SUPABASE_SERVICE_ROLE_KEY`, and the `cgm_readings` table doesn't exist in Supabase yet pending Julie's migration run). **No read UI/API route exists yet** — the Glucose Stability card on the Today tab still shows its CGM-toggle placeholder; wiring it to real data is a separate follow-up. Not merged — pushed to a new branch for review; no PR opened. Previous: September 7, 2026 (CGM integration prep — defensive only, no parsing logic, no writes to real data. Sensor (Libre 3 Plus) is active, syncing via LibreLink → GlucosePhone → Apple Health ("Blood Glucose") → Health Auto Export. **New table `cgm_readings`** — one row per timestamped reading (not a daily aggregate): id (uuid PK), user_id (text, default 'julie'), recorded_at (timestamptz), value_mmol (numeric), source (text), created_at (timestamptz default now()). Migration SQL written to DATA MODEL → `cgm_readings` for Julie to run herself in the Supabase SQL editor — **not run this session**. Table does not exist in Supabase yet. Locked decision for whenever read/write code is built: must go through a server route on the service-role client (same pattern as `/api/entries`/`/api/glp1`/`/api/health-appointments`), never browser-direct anon-key, so it doesn't join the RLS backlog. **Defensive logging hook in `/api/health-import`:** for any incoming metric whose name contains `"glucose"` (case-insensitive substring, not an exact match — in case the real field name differs from what's expected), logs metric name, units, and a 3-entry sample of raw data points (not the full payload) to the same debug-log path already used for unrecognized metrics. Expected name is `blood_glucose`, inferred from HAE's naming pattern for other metrics (`heart_rate_variability`, `resting_heart_rate`) — **not yet confirmed against a real payload**. No mapping to `cgm_readings` exists yet; nothing is written to it. **File changed:** `app/api/health-import/route.ts` — glucose-substring check + sample-logging block added inside the existing per-metric loop that already logs unrecognized metric names, right after that existing check. Verified: `tsc --noEmit`, `eslint`, and `next build` all clean. Not observable in the browser preview (webhook route gated by `x-api-key`, no UI touched) — not exercised against a live Health Auto Export payload this session. Not merged — pushed to a new branch for review; no PR opened. Previous: September 7, 2026 (RLS security fix, session 3 of a multi-session plan — session 2 (below) is merged and its bugfix pass is confirmed. This session covers `health_appointments`' three remaining dedicated functions, `saveHealthAppointment`/`fetchHealthAppointments`/`seedDefaultAppointments`, all still 100% browser-direct anon-key calls from `app/page.tsx` going into this session. **New route:** `app/api/health-appointments/route.ts` (service-role via `supaAdmin()`) — `GET` returns the full appointment list (`fetchHealthAppointments`); `POST` takes either `{ seed: true }` (→ `seedDefaultAppointments`) or `{ id, last_completed_date?, next_due_date?, notes? }` (→ `saveHealthAppointment`), same discriminated-body pattern as picking one action per POST. Built with `export const dynamic = 'force-dynamic'` + `revalidate = 0` from the start, matching the other RLS-fix routes and session 2's bugfix-pass convention. **Mechanism, extending sessions 1/2's pattern:** all three `lib/db.ts` functions gained an optional `client: SupabaseClient = supabase` parameter (default unchanged, still anon) and are now called only from the new route with `supaAdmin()` — not deleted, not made dead code, just no longer called directly from the browser; commented as such in `lib/db.ts` immediately above the three functions. **`app/page.tsx`:** the three names dropped from the `@/lib/db` import; three small `fetch()` wrapper functions of the same names (`fetchHealthAppointments`, `seedDefaultAppointments`, `saveHealthAppointment`) added just above the `App` component (alongside the session 2 wrappers), and all four original call sites (the seed-on-empty-appointments effect, `handleMarkDone`, `handleSaveAppt` ×2) swapped to use them with the same call shape and error handling as before — no logic changes at any call site. **Explicitly not done:** RLS is NOT enabled on `health_appointments` — Julie needs to test the Health Log tab (appointment list, vaccination list, seeding, mark-done, edit-and-save) on the branch preview first; see the checklist under "RLS security fix — status" in the RLS section. Combined with session 2's move of the health_appointments read inside `getGoalsData()`, this table now has zero remaining browser-direct anon-key call sites anywhere in the app — all three of daily_entries/training_sessions/health_appointments are now fully routed through service-role API routes, pending each one's own branch-preview testing pass before RLS goes on. Verified: `tsc --noEmit`, `eslint`, and `next build` all clean (new route correctly registered as dynamic `ƒ`); local dev environment has no `SUPABASE_SERVICE_ROLE_KEY` (pre-existing, documented limitation — see the Supabase env var bug note below), so the new route's actual data flow could not be exercised locally — real verification happens on the Vercel branch preview, same as every other service-role route in this app. Not merged — awaiting Julie's review. Previous: September 7, 2026 (RLS fix session 2 — bugfix pass, found via live branch-preview testing before merge/RLS. **Symptom:** the Today tab's Behavior Score for a fixed date (Aug 13) showed different numbers on different loads (77, then later 85/79 under rapid date-picker navigation), and disagreed with the Dashboard History list's number for the same date (62; Outcome Score matched at 64 both times). **Two separate, confirmed root causes — NOT one caching bug:** (1) **Cross-surface mismatch (77 vs 62), pre-existing, NOT introduced by either RLS session, NOT fixed this pass:** the Dashboard's `HistoryRow` (`app/page.tsx`) calls `behaviorScore(entry)`/`outcomeScore(entry)` from `lib/scores.ts` with no extra arguments — since both functions take the nutrition summary / rolling HRV baseline / rolling bedtime target as optional args defaulting to `undefined`/`88`/`'21:45'`, this silently live-recomputes a DIFFERENT score than the authoritative stored `behavior_score` column (which `recomputeScores()` computes server-side using the real rolling 28-day HRV median, real rolling 30-day bedtime average, and real `daily_nutrition_summary` data — critically, `behaviorScore()` with no `nutritionSummary` arg drops the entire 30%-weighted nutrition component). Confirmed via `git log -S` that this call pattern was introduced in commit `7c0cd7c` (April 30, 2026, the History/Dashboard merge) — five months before either RLS session touched this file. The Today tab's number (from `/api/scores?date=`, the authoritative stored value) is correct; the Dashboard History row's number is a materially different, simplified calculation. **Flagged for Julie, not fixed this session** — deciding whether History rows should show the same authoritative stored score (matches Today tab, requires either passing real baselines/nutrition into the client recompute or fetching the stored value per row) is a scoring-display decision outside "fix the caching bug," not something to change silently. (2) **Same-date instability under rapid navigation (85/77/79), root cause confirmed, FIXED this session:** neither `loadDay()` nor the Today-tab score-loading `useEffect` (both `app/page.tsx`) had any request-sequencing guard — rapid date-picker clicks fire overlapping `fetchEntry()`/`/api/scores` requests for different dates with no cancellation, and a slower response for an OLDER navigation could resolve AFTER a newer one and silently overwrite `entry`/`todayScored` with the wrong date's data. This race existed structurally before either RLS session (same `useEffect` shape), but session 2's change from a direct in-browser Supabase call to an HTTP round-trip through a new intermediate Next.js route (browser → Vercel function → Supabase) added latency and latency *variance* per request, making the pre-existing race far more likely to actually manifest — which is why it surfaced now. **Fix:** `loadDay()` gained a `latestEntryDateRef` checked after each await, bailing out if a newer navigation has since superseded the in-flight one; the score-loading effect gained the standard React `cancelled`-flag-via-cleanup pattern. **What was checked and ruled out:** Next.js's own Route/Data Cache — the build output (`next build`) shows every route in this family (`/api/entries`, `/api/entries/sleep-logged`, `/api/entries/cycle-day`, `/api/goals`, `/api/scores`, `/api/glp1`) marked `ƒ Dynamic` (server-rendered on demand), not `○ Static`, both before and after this fix; Next.js 15+ (this app runs 16.2.2) also defaults `fetch()` itself to `no-store` unless explicitly opted into caching; and `@supabase/supabase-js`'s internal fetch calls (`postgrest-js`'s `PostgrestBuilder`) set no `cache` option of their own, so they'd inherit whatever the ambient default is. **This does not rule out Vercel CDN-level caching or browser HTTP caching**, which the build output can't observe from this environment — belt-and-suspenders hardening was still applied regardless of root-cause uncertainty at that specific layer: `export const dynamic = 'force-dynamic'` + `export const revalidate = 0` added to all six routes above (`/api/scores` and `/api/glp1` were session 1 routes, not session 2's, but are in the identical risk family and `/api/scores` is the ACTUAL route serving the visible Behavior Score number — both included here since the user's own framing was "this affects daily_entries reads broadly, not just Behavior Score specifically"), and `{ cache: 'no-store' }` added to every `fetch()` call in `app/page.tsx` touching these routes (all seven wrapper functions, the score-loading effect's three fetches, both GLP-1 fetches, and the scores-recompute POST in `save()`). **Could not verify against production:** the branch's Vercel preview (`health-coach-git-claude-rls-29faef-...vercel.app`) is gated behind Vercel account login this session doesn't have credentials for, and local dev has no `SUPABASE_SERVICE_ROLE_KEY` (documented, pre-existing limitation) — so the actual DB value for Aug 13's `behavior_score`, and whether the fix eliminates the flicker in a live rapid-navigation test, still need Julie's confirmation on the branch preview. Locally: `tsc`, `eslint`, `next build` all clean (routes still correctly `ƒ Dynamic`); rapid-fire clicked the date picker 5× against local dev — no crash, no new error class, confirms the guard logic doesn't throw, though the local 500s (missing service-role key) mean the "does the right value win" path itself couldn't be exercised end-to-end. RLS was not touched. Previous: September 7, 2026 (RLS security fix, session 2 of a multi-session plan — session 1 (below) is merged and its `glp1_injections` RLS is now enabled, confirmed by Julie on the branch preview. This session moves `daily_entries` and `training_sessions` together, since `training_sessions` rides along inside `saveEntry`'s delete/reinsert and inside every daily_entries load function — they can't be split. **Scope:** `loadEntry`, `saveEntry` (both `app/page.tsx` call sites — `save()` and `resetCycleDay()`), `loadAllEntries`, `loadRecentEntries`, `isSleepLogged`, `deriveCycleDay`, `getGoalsData`. Per instruction, `getGoalsData()` moved here even though it also touches `health_appointments` (and, as a side effect, `biomarker_readings`) — it reads all three tables in one function, so touching it here was the only option, and it usefully reduces session 3's remaining `health_appointments` scope from 4 functions to 3 (`saveHealthAppointment`/`fetchHealthAppointments`/`seedDefaultAppointments`, all untouched this session per instruction — no health_appointments-only logic was touched). **New routes** (all `supaAdmin()`, all calling the existing `lib/db.ts` functions rather than reimplementing their logic): `app/api/entries/route.ts` (GET `?date=`→`loadEntry`, `?days=N`→`loadRecentEntries`, `?all=true`→`loadAllEntries`; POST→`saveEntry`), `app/api/entries/sleep-logged/route.ts` (GET `?date=`→`isSleepLogged`), `app/api/entries/cycle-day/route.ts` (GET→`deriveCycleDay`), `app/api/goals/route.ts` (GET→`getGoalsData`). **Mechanism, extending session 1's pattern:** all seven `lib/db.ts` functions gained an optional `client: SupabaseClient = supabase` parameter (default unchanged, still anon) — `loadEntry`/`loadAllEntries`/`loadRecentEntries` thread their `client` through to their internal `loadSessionsForDates` call, and `saveEntry` threads it through its `daily_entries` upsert and its `training_sessions` delete+insert, so training_sessions genuinely moves in lockstep with daily_entries rather than being missed. Unlike session 1's `glp1_injections` functions (marked superseded and left fully unused), these seven functions are **not dead** — they're now called only from the four new routes above instead of directly from the browser, and are commented as such in `lib/db.ts`. **`app/page.tsx`:** the seven names dropped from the `@/lib/db` import; seven small `fetch()` wrapper functions (`fetchEntry`, `fetchRecentEntries`, `fetchAllEntries`, `postEntry`, `fetchSleepLogged`, `fetchDeriveCycleDay`, `fetchGoalsData`) added just above the `App` component definition, and all eleven original call sites swapped to use them — same call shape, same error handling as before (or better: `fetchSleepLogged`/`fetchDeriveCycleDay` resolve to their safe fallback, `false`/`null`, on a non-2xx response, mirroring `isSleepLogged()`/`deriveCycleDay()`'s own never-throws behaviour instead of surfacing a fetch error). **Explicitly not done:** RLS is NOT enabled on `daily_entries` or `training_sessions` — this touches far more surface area than session 1 (sleep, training, nutrition-adjacent reads, cycle tracking, the Goals tab) and needs a real testing pass on the branch preview first; see the checklist under "RLS security fix — status" in the RLS section. `health_appointments`'s three dedicated functions are untouched (session 3). Verified: `tsc --noEmit`, `eslint`, and `next build` all clean (four new routes correctly registered as dynamic); local dev boots, every new route fails identically to every other service-role route already in the app (missing `SUPABASE_SERVICE_ROLE_KEY` in `.env.local`, a pre-existing documented limitation) and the app degrades gracefully with no crashes — real verification happens on the Vercel branch preview. Previous: September 7, 2026 (RLS security fix, session 1 of a multi-session plan — Supabase flagged "RLS Disabled in Public" on four tables: daily_entries, training_sessions, health_appointments, glp1_injections, all queried directly with the anon key and no RLS policy. A prior session produced the full investigation and call-site map (not itself a code change). This session covers only the two smallest, lowest-risk pieces. **Task 1 — anon→service-role swap in three places that already run server-side (no route changes, no behaviour change):** `lib/scores-server.ts` (`recomputeScores`), `app/api/scores/route.ts` (`GET` handler), and `app/api/coach/route.ts` (`getCoachContext`, plus its direct calls to `getHrvRolling28DayMedian`/`getBedtimeRolling30DayAvg`) all used to build their own anon-key client (or import the anon `supabase` from `lib/db.ts`) despite running entirely server-side — safe today only because RLS is off. Mechanism: `loadSessionsForDates`, `getHrvRolling28DayMedian`, `getBedtimeRolling30DayAvg`, and `getSleepDebtRolling7Day` in `lib/db.ts` each gained an optional `client: SupabaseClient = supabase` parameter — default unchanged (still the anon client, so every existing browser caller in `app/page.tsx` is untouched), and the three server files above now pass `supaAdmin()` explicitly. `app/api/coach/route.ts`'s local anon client construction inside `getCoachContext` was removed entirely in favour of the `adminClient` (`supaAdmin()`) it already builds for `daily_nutrition_summary`; the standalone `const createClient(...)` import is gone. `app/api/scores/route.ts`'s `GET` handler's separate `anonClient` was removed the same way, reusing its existing `adminClient`. Same tables, same filters, same queries — client swap only. **Task 2 — glp1_injections off the browser-direct pattern:** new `app/api/glp1/route.ts` (`GET` — list, `POST` — log an injection), both via `supaAdmin()`, logic ported field-for-field from `lib/db.ts`'s `loadGlp1Injections()`/`logGlp1Injection()` (same `user_id='julie'` filter, same `injection_number = max+1` sequencing, same `dose_mg` default of 2.5). `app/page.tsx`'s `loadGlp1`/`handleLogGlp1` now `fetch()` this route instead of importing those two `lib/db.ts` functions directly — both dropped from the `@/lib/db` import list. The two superseded `lib/db.ts` functions are left in place, unused, with a comment explaining why (first of a multi-session change, glp1_injections' RLS isn't on yet, so keeping the old anon-client path costs nothing and simplifies rollback) — not deleted this session. **Explicitly not done:** RLS is NOT enabled on glp1_injections yet — Julie will flip that in the Supabase dashboard herself, after confirming `/api/glp1` works correctly on the branch preview. The browser-direct anon-key call sites for daily_entries/training_sessions (11 call sites across 7 `lib/db.ts` functions, all in `app/page.tsx`) and health_appointments (10 call sites across 4 functions) are untouched — still anon-key, still browser-direct, still blocked on RLS — that's future-session work per the existing call-site map. Verified: `tsc --noEmit` and `next build` both clean; local dev boots and the GLP-1 card renders its correct empty "Not started" state — actual data flow through `/api/glp1` (and every other service-role route) can't be exercised locally since `.env.local` has no `SUPABASE_SERVICE_ROLE_KEY` (pre-existing, documented limitation, not introduced by this change) — real verification happens on the Vercel branch preview, where Production/Preview env vars are already correct. Previous: August 30, 2026 (Estradiol reverted from twice-daily AM/PM dosing back to a single AM-only dose, now 2 sprays instead of the original 3 — no night sweats for a sustained period made the PM dose unnecessary. "Estradiol PM" `HormoneCard` removed from `SupplementsSection.tsx`; "Estradiol AM" card renamed to just "Estradiol"; auto-default-on-toggle-ON for this card changed from 1 to 2. `estradiol_pm_taken`/`estradiol_pm_sprays` become dead fields — same pattern as the original pre-split `estradiol_taken`/`estradiol_sprays` columns: stay mapped through `lib/types.ts`/`lib/db.ts` both directions for historical row integrity, no longer read or written by any UI, scoring, or Coach-context-building code. Collapsed summary pill's `anyBadgeSaved` gate and `E2 ✓` badge, the Today-tab Behavior Score bullet (`getBehaviorBullets()`), and the Dashboard history row detail (`EntryDetail()`) — all in `app/page.tsx`/`SupplementsSection.tsx` — dropped the OR-across-AM/PM logic added Aug 18, 2026 and now key off `estradiol_am_taken` alone. Behavior Score (`lib/scores.ts → behaviorScore()`): the estradiol sub-component (15% of the 20%-weight supplements bucket), previously requiring `estradiol_am_taken && estradiol_pm_taken` for full credit (per the Aug 18 "used AND, not OR" judgment call — see BACKLOG note below, now resolved by this revert), simplified to `estradiol_am_taken` alone; 40/30/15/15 split otherwise untouched. **Not fully closed — flagged for Julie:** three reads of `estradiol_pm_taken` were found in `app/api/coach/route.ts` (the day-context line fed to the Coach LLM at line ~169, and prompt-instruction text at lines ~423/432 referencing "estradiol AM, estradiol PM") — left untouched per instruction rather than silently changed; Coach's prompt context and its instructions to itself will keep mentioning a PM dose until this is addressed. Previous: August 29, 2026 (Body Composition v3 — the card was rebuilt end to end around a single story: of the weight lost, how much was fat rather than muscle and water. Replaced the weight sparkline, the 65kg target-tick bar, and the 3-row manual Body Scan subsection with a current-weight hero, a tinted total-change block, a fat/lean-&-water split bar scoped to the first paired body-scan date (anchor B), three tiles (tappable Muscle with a band pill, Body fat, Waist), two 60-day full-density charts (fat mass + muscle on a shared kg axis; waist with a dashed 79cm target line), and a footer. Added two new Health Auto Export mappings — `body_fat_percentage` → `body_fat_pct` (%), `waist_circumference` → `waist_cm` (cm) — alongside the existing weight mapping; all three are overwrite-on-change and the webhook always wins (no manual-entry protection, by design — corrections happen in Apple Health). All three metric names, including `weight_body_mass`, are now confirmed against a real payload (29 Aug 2026) — closes the long-standing "field name not yet verified" caveat. New manual-only `smm_kg` marker (skeletal muscle mass — no HealthKit equivalent, so no webhook path). Fat mass (weight × body_fat_pct ÷ 100) is computed at read time in `getBodyCompositionData()` and never stored. `visceral_fat_l` dropped to a dead marker — rows retained, unread; see BACKLOG for the unit-mismatch note before anyone re-wires it. Muscle band thresholds (below 22kg / 22–26kg / above 26kg) are estimates read off the InBody unit's own on-screen band positions, which carry no numeric scale of their own. Known open bug, unchanged and out of scope: the VO2 Max spectrum bar's target tick renders near the current-value tick instead of at its correct scale position — see BACKLOG. Previous: August 28, 2026 — Barcode scanner bugfix — the scan screen could freeze permanently on the installed iOS home-screen PWA with no camera prompt, no error, and no recovery, caused by `getUserMedia()` sometimes hanging forever after the installed PWA is backgrounded/resumed; fixed by racing the camera-start call against a 7s timeout in `ScreenScan` so a hang now falls into the existing error/retry UI instead of a dead screen. No new commits touched the feature before this bug appeared — confirmed via git history, dependency, service-worker, and CSP checks — so this was an iOS-side regression, not a code regression. Previous: same day, Calorie stat strip — forced "kcal" onto its own line in all four columns, fixing inconsistent wrap where 4-digit values like "1,800" wrapped and 3-digit values like "144" didn't. Markup-only change, no new styling. Previous: same day, Calorie stat strip's third column relabelled REMAINING → LEFT — on real ~375px mobile widths "Remaining" wrapped mid-word, sitting lower than the other three single-line columns and breaking row alignment; "Left" never wraps. No other change — same value, same colour logic, same position. Verified at an emulated 375px viewport, not just desktop. Previous: August 27, 2026 — Calories subsection restyled from a single oversized "{X} kcal left" number into a 4-column stat strip — Eaten / Target / Remaining / Burned — mirroring the Training tab's Basal/Active/Total energy strip typography, with only the Remaining column colour-coded. Same underlying values, no new calculation. Required reintroducing the `basalCalories`/`activeCalories` props on `NutritionSection` that the Fixed-Baseline Targets change below had just removed, to source the Burned figure. A layout bug found during verification — REMAINING and BURNED labels colliding at the app's narrow fixed content width — was fixed with an explicit gap and word-wrap on the labels. Previous: August 27, 2026 — Fixed-baseline Nutrition Targets — the May 2026 Dynamic Nutrition Targets model, which derived calorie/fat/carb targets from daily_entries.basal_calories + active_calories (noisy Apple Watch all-day HAE figures), was replaced with a mostly-fixed daily target (lib/nutritionTargets.ts) that only adjusts on days with genuinely high structured training load, sourced from training_sessions.active_calories instead. Protein/Fat/Fiber stay fixed; Carbs and Calories get a credit above a 750kcal training-day threshold, capped at a 2100kcal ceiling. Calories bar replaced with a "remaining" display (three-colour states) plus a tap-to-reveal TEE breakdown and a training-day adjustment note. Behavior Score and the Training tab's Basal/Active/Total energy strip were explicitly not touched. Previous: August 24, 2026 — health_appointments dedup bugfix — seedDefaultAppointments() was a plain insert gated by a non-atomic, total-row-count check, so a concurrent double-fire of the first-load useEffect could insert the 9 default appointment rows twice; fixed to an upsert with onConflict/ignoreDuplicates against a new unique constraint on (user_id, appointment_type) that Julie added directly in Supabase after a manual dedup. Vaccination rows were unaffected — confirmed never seeded by app code. Previous: August 24, 2026 — GLP-1 calendar bugfix — the "Last 4 Weeks" grid was a trailing 28-day window ending today, which showed zero future days once today sat late in its own week-row; replaced with a rolling ±14-day window rounded out to full Monday–Sunday weeks, always 35 days / 5 rows with today in the middle row and due dates up to a week out visible. Previous: August 21, 2026 — Body Composition card bugfixes — manual Body Scan save (body_fat_pct, waist_cm, visceral_fat_l) switched from plain insert to upsert so re-saving a value for an already-logged date no longer fails; collapsed-state weight badge now hidden, shown only when the card is expanded. Previous: August 19, 2026 — New Body Composition long-term goal card below VO2 Max + Body Scan subsection; Health Auto Export weight mapping wired to `weight_body_mass` — overwrite-on-change, field name not yet verified against a real payload; New GLP-1 tracking card + inline refill reminder sub-card on the Today tab; HRT protocol changes — Estradiol split into AM/PM toggles, Testosterone AM toggle added)_

---

## DEVELOPMENT RULES (mandatory)

1. Read this file fully before touching any code.
2. Never rewrite a file from scratch. Make only targeted, minimal edits.
3. CSS custom properties only — no hardcoded colours or inline style values.
4. Always push to a new branch, never directly to main.
5. After completing all tasks, output a plain-English summary of every file changed and every line edited.
6. Ask before moving to the next task.
7. DB migrations always run in Supabase SQL editor before any code changes deploy. Never run them yourself — flag and wait.
8. Commit and push after every task.
9. Update this file (BODYCIPHER.md) to reflect any changes made — new fields, removed features, architectural changes, backlog updates. Do this before the plain-English summary.

---

## PROJECT

**App:** BodyCipher — decode your body. Personal health coaching web app for Julie.
**Live:** https://health-coach-rho.vercel.app
**Local:** `/Users/julienathan/Documents/Northstar/health-coach`
**GitHub:** `git@github.com:jnjulienathan-eng/health-coach.git` (SSH only — HTTPS has failed repeatedly)
**Stack:** Next.js (App Router) + Supabase (West EU, `cprcamywvhtcboprtkjp.supabase.co`) + Vercel + Anthropic API
**Key files:** `app/api/coach/route.ts`, `lib/db.ts`, `lib/types.ts`, `lib/scores.ts`, `lib/trainingLoad.ts`
**Scripts:** `scripts/generate-icon.mjs` — generates `public/apple-touch-icon.png` (180×180 PNG, dark bg + BodyCipher mark). Run with `node scripts/generate-icon.mjs` from the project root. Requires `sharp`.
**Sections:** `components/sections/` — SleepSection, TrainingSection, NutritionSection, HydrationSection, SupplementsSection, Glp1Section (+ named export `Glp1RefillCard`, not date-navigable), ContextSection
**Other components:** `components/CoachTab.tsx`, `components/DashboardTab.tsx` (not imported, preserved on disk), `components/HistoryTab.tsx` (not imported, preserved on disk), `components/GoalsTab.tsx` (not imported, preserved on disk), `components/SplashScreen.tsx`

---

## JULIE'S PROFILE (never change without explicit instruction)

- Early 50s, perimenopause, Munich, Bavaria
- Athletic, data-driven, health-optimised. Metric units only.
- HRV baseline ~88ms default, computed as 28-day rolling median of waking `hrv` when ≥14 readings available | RHR baseline ~52 bpm
- Sleep target: 7h30–8h30 | Bedtime target: 21:45
- Protein target: 130–140g/day | Fiber: 30–35g
- Active calorie target: 600 kcal intentional training, ~900 kcal total
- Training: Swim 50min, eGym 35min, Run 35min, Walk 75min
- Diet: whole food, largely dairy-free (exceptions: cottage cheese, occasional cheese, high-protein yogurt). Lower carb, not keto. Alcohol-free. Loves sardines, natto, fermented foods.
- Cuisine: Japanese, Korean, Chinese, Thai, Vietnamese, Mediterranean, Middle Eastern, Indian. **Never German/Bavarian.**
- Cycle: currently irregular, recent cycles 54–80+ days

---

## CURRENT STATE — WHAT IS BUILT

### Dynamic Nutrition Targets (May 2026) — superseded August 27, 2026

**Superseded by "Fixed-Baseline Nutrition Targets" directly below.** Section kept for history; the calorie/fat/carbs target derivation described here (basal + active calories, 40%/30% split) is no longer live code.

Daily calorie total = `basal_calories + active_calories` from `daily_entries`. If either field is null, show `—` throughout — no fallback numbers. Fat target = `ROUND((calories × 0.40) / 9)`g. Carbs target = `ROUND((calories × 0.30) / 4)`g. Protein (135g) and Fiber (32g) are fixed. `basal_calories` and `active_calories` use "overwrite if higher" — the webhook writes the incoming value only when the stored value is null or the incoming value is greater, so the figure updates through the day without a smaller or zero value overwriting a real reading. Training section shows Basal / Active / Total energy strip above quick-add pills. Nutrition section shows surplus/deficit line below Calories bar (`--color-status-optimal` when under, `--color-amber` when over, `--fs-label`, only rendered when both consumed and total are non-null and consumed > 0).

**Files changed:**
- `lib/types.ts`: `basal_calories` and `active_calories` added to `DailyEntry` interface and `emptyEntry()`.
- `lib/db.ts`: both fields extracted in `rowToEntry()`.
- `app/page.tsx`: both fields passed as props to `TrainingSection` and `NutritionSection`.
- `components/sections/NutritionSection.tsx`: `getDailyCalorieTarget()` removed; dynamic calorie/fat/carbs targets computed from props; `MacroBar` accepts nullable target; surplus/deficit line added below Calories bar.
- `components/sections/TrainingSection.tsx`: energy strip (Basal / Active / Total) added above quick-add pills.

### Fixed-Baseline Nutrition Targets (August 27, 2026)

Replaces the May 2026 Dynamic Nutrition Targets model above. The old model derived calorie/fat/carb targets from `daily_entries.basal_calories + active_calories` — noisy Apple Watch all-day HAE figures that include non-training movement, not just structured training. The new model is a mostly-fixed daily target that only adjusts on days with genuinely high structured training load, sourced from `training_sessions.active_calories` for the currently-navigated date instead.

**Constants and pure function — `lib/nutritionTargets.ts`** (same pure-module pattern as `lib/trainingLoad.ts`, no Supabase calls):
- `BASE_TEE = 2300`, `TARGET_DEFICIT = 500`, `BASE_CAL_TARGET = 1800` (= TEE − deficit)
- `PROTEIN_TARGET = 140`, `FIBER_TARGET = 35`, `FAT_TARGET = 60`, `CARB_TARGET = 175` — fixed, never adjusted
- `TRAINING_OUTLIER_THRESHOLD_KCAL = 750`, `EXCESS_CREDIT_RATE = 0.5`, `CALORIE_CEILING = 2100`
- `computeNutritionTargets(todayTrainingKcal)`: below/at the 750kcal threshold, returns the fixed baseline (`adjustment.active = false`). Above it, `excess = todayTrainingKcal − 750`, `addedKcal = round(min(excess × 0.5, 2100 − 1800))`, `calories = 1800 + addedKcal`, `addedCarbs = round(addedKcal / 4)`, `carbs = 175 + addedCarbs`. Protein/fat/fiber never change. `adjustment.active = true`.

**Training-kcal input:** `todayTrainingKcal` is the sum of `active_calories` across `entry.training.sessions` for the currently-navigated date. `entry.training.sessions` was already being passed into `NutritionSection` as the `sessions` prop (same data `TrainingSection`/Training Load already use) — no new query or fetch/prop-threading was needed. Computed inline in `NutritionSection.tsx`; `basalCalories`/`activeCalories` props (and the `entry.basal_calories`/`entry.active_calories` pass-through in `app/page.tsx`) were removed from `NutritionSection` since nothing in it reads them anymore. `TrainingSection` still receives `basalCalories`/`activeCalories` for its own energy strip — untouched.

**Nutrition section UI (`components/sections/NutritionSection.tsx`):**
- Protein and Fiber bars: unchanged fill-toward-target rendering; `target.max` now sourced from `targets.protein`/`targets.fiber` instead of `MACRO_TARGETS.protein.max`/`MACRO_TARGETS.fiber.max` directly (same numeric values, 140/35 — no visual change).
- Carbs and Fat bars: unchanged fill-toward-target rendering; `target.max` sourced from `targets.carbs`/`targets.fat` (carbs reflects the training-day credit when active).
- **Calories → "remaining" display:** the old consumed/target progress bar is gone. `remaining = target − consumed`. Large number reads `"{remaining} kcal left"` (remaining ≥ 0) or `"{abs(remaining)} kcal over"` (remaining < 0) — no clamping to zero. Colour: `--color-status-optimal` (remaining ≥ 150) / `--color-amber` (0 ≤ remaining < 150) / `--color-danger` (remaining < 0). Only rendered once `day` (the fetched day response) is non-null — same "don't show a number before real data exists" convention as the rest of the component; on a day with zero meals logged it correctly shows the full target as remaining, not `—`.
- **Adjustment note:** when `adjustment.active`, a small always-visible `--fs-label` line reads `"Training day: +{addedKcal} kcal, +{addedCarbs}g carbs"` directly below the remaining-calories number. Absent when not active.
- **Tap-to-reveal TEE breakdown:** tapping the remaining-calories number toggles a local `showTeeBreakdown` boolean (component state, not persisted — same tap-to-reveal pattern as the VO2 sparkline). Reveals `"{target} kcal target (2300 TEE − 500 deficit)"` on non-adjusted days, or `"{target} kcal target (2300 TEE − 500 deficit + {addedKcal} today's training)"` on adjusted days.

**Explicitly not touched (confirmed):**
- **Behavior Score (`lib/scores.ts → behaviorScore()`):** its active-calories component (weight 15%) already reads from `training_sessions.active_calories` summed + `cycling_calories`, not `daily_entries.active_calories` — this change doesn't affect it at all. No edits made.
- **Training tab's Basal/Active/Total energy strip (`components/sections/TrainingSection.tsx`):** still reads `entry.basal_calories`/`entry.active_calories` (the all-day Apple HAE figures) exactly as before. Unchanged.
- Protein/Carbs/Fat/Fiber bars stay fill-toward-target — none of them switch to a "remaining" display, only Calories does.
- No DB migrations — `training_sessions.active_calories` already existed and was already summed elsewhere (Training Load).

**Files changed:**
- `lib/nutritionTargets.ts` (new): constants + `computeNutritionTargets()`.
- `components/sections/NutritionSection.tsx`: `basalCalories`/`activeCalories` props removed; `dailyCalories`/`fatTarget`/`carbsTarget`/`calorieTargetRange`/`calorieDiff` derivation removed; `todayTrainingKcal` computed from the `sessions` prop; `computeNutritionTargets()` wired in; Calories bar replaced with the remaining-calories display, adjustment note, and tap-to-reveal TEE breakdown (`showTeeBreakdown` state added).
- `app/page.tsx`: `basalCalories={entry.basal_calories}` / `activeCalories={entry.active_calories}` removed from the `<NutritionSection>` call (still passed to `<TrainingSection>`, untouched).

### Calories → 4-column stat strip (August 27, 2026, later same day)

Superseded the single oversized "{X} kcal left" display from the section directly above. Same underlying values (`consumed`, `targets.calories`, `remaining`), no new calculation — restyled as a 4-column stat strip mirroring the Training tab's Basal/Active/Total energy strip pattern (`components/sections/TrainingSection.tsx`), same label typography (`--fs-label`/`--fw-bold`/`--ls-label-bold`/uppercase/`--color-text-secondary`) and value typography (`--fs-body`/`--fw-bold`) for all four columns — no visual hierarchy between them.

- **Columns, left to right:** EATEN (`consumed`) · TARGET (`targets.calories`) · REMAINING (`remaining` = target − consumed) · BURNED (`basalCalories + activeCalories`, `—` if either is null).
- **Colour:** only REMAINING is colour-coded, same three-state logic as before (`--color-status-optimal` ≥150 / `--color-amber` 0–149 / `--color-danger` <0). EATEN/TARGET/BURNED stay `--color-navy` (neutral, matching Training's strip).
- **basalCalories/activeCalories props reintroduced on `NutritionSection`** (`components/sections/NutritionSection.tsx`, `app/page.tsx`) — these were removed earlier the same day in the Fixed-Baseline Targets change above since nothing in the component read them at that point. BURNED needed the same `entry.basal_calories`/`entry.active_calories` values `TrainingSection` already receives, so the props and the `app/page.tsx` pass-through were added back. This is new plumbing, not a no-op — flagging since it re-touches something the prior entry explicitly called out as removed.
- **Adjustment note and tap-to-reveal TEE breakdown are unchanged** — same `targets.adjustment.active` note and `showTeeBreakdown` toggle, now rendered below the stat strip instead of below the single number. The whole strip is still the click target that toggles the breakdown.
- **Layout bugfix found during verification:** at the app's actual content width (~366–376px, since the app is capped at `max-width: 448px` and stays that narrow regardless of screen size), `justify-content: space-between` with 4 columns caused the REMAINING and BURNED labels to visually collide with zero gap ("REMAININGBURNED") — Training's strip never hit this since its 3 labels (Basal/Active/Total) are shorter. Fixed with an explicit `gap` on the row plus `flex: 1 1 0; minWidth: 0` on each column so they share space evenly, and `overflowWrap: anywhere` on the label span so a long label wraps onto a second line inside its own column instead of overflowing into its neighbour. On the narrowest supported widths, "REMAINING" can wrap mid-word (e.g. "REMAINI/NG") — acceptable degradation to avoid the collision; no overlap at normal widths.
- No separate surplus/deficit line existed elsewhere in the Calories subsection to remove — checked, only the tap-to-reveal TEE breakdown line was present, and it shows different information (target composition) than remaining/surplus.
- No flagged backlog item matching "amber-under-150/red-negative colour threshold" was found anywhere in this file to close out — the only colour-threshold logic for Calories is the three-state REMAINING rule documented above, which was already implemented (not flagged as open) before this change.

**Files changed:**
- `components/sections/NutritionSection.tsx`: `Props` gained `basalCalories?: number | null` / `activeCalories?: number | null` (defaulting to `null`); the single "{X} kcal left" block replaced with the 4-column stat strip (Eaten/Target/Remaining/Burned) described above, including the layout bugfix.
- `app/page.tsx`: `basalCalories={entry.basal_calories}` / `activeCalories={entry.active_calories}` added back to the `<NutritionSection>` call.

### Calories stat strip — third column relabelled REMAINING → LEFT (August 28, 2026)

The `overflowWrap: anywhere` fix above stopped REMAINING/BURNED from colliding, but on real mobile device widths (~375px, not caught during the prior session's desktop-only check) "REMAINING" itself still wraps mid-word onto a second line ("REMAINI" / "G"). Since the other three columns (Eaten/Target/Burned) stay single-line, that made the third column's value sit visibly lower than the other three, breaking row alignment.

- **Fix:** third column's label string changed from `'Remaining'` to `'Left'` in `components/sections/NutritionSection.tsx`. Nothing else changed — same value (`remaining` = target − consumed), same three-state colour logic (`--color-status-optimal` ≥150 / `--color-amber` 0–149 / `--color-danger` <0), same column position (third), same typography as the other three labels (including the still-present `overflowWrap: anywhere`, now inert for this column since "Left" never wraps).
- Verified at an emulated 375px mobile viewport (not just desktop) — all four values now sit on one aligned row at both the positive (green) and negative (red) states.
- The "Columns, left to right" and "Colour" bullets above are now stale on the label name only (they say REMAINING; the live label is LEFT) — left as-is for history rather than rewritten, per this file's append-only convention for dated changes.

**Files changed:**
- `components/sections/NutritionSection.tsx`: one label string changed, `'Remaining'` → `'Left'`, in the calorie stat strip's column array.

### Calories stat strip — "kcal" forced onto its own line (August 28, 2026, later same day)

Each stat rendered as a single string like `"144 kcal"` or `"1,800 kcal"`. Line wrap only happened when the string was too wide for its column, so 4-digit values (1,800) wrapped onto two lines while 3-digit values (144) stayed on one — inconsistent row heights, read as a layout bug rather than the LEFT-column-specific fix directly above.

- **Fix:** the `fmt()` helper in the calorie strip's IIFE now returns `{ value, unit }` instead of a single formatted string (`unit` is `null` for the `—` no-data case, e.g. Burned when basal/active calories aren't available). Each column renders `value` and `unit` as two separate stacked `<span>`s inside the existing `flexDirection: 'column'` container, instead of one string with a space. "kcal" now always sits on its own line, regardless of digit count.
- Applied identically to all four columns (Eaten/Target/Left/Burned). Same font sizes/weights/colours as before (`--fs-body`/`--fw-bold`, per-column colour incl. the Left three-state logic) — markup/layout change only, no new styling.
- Verified with a static mockup reproducing the exact inline styles at an emulated 375px mobile viewport, across the 3-digit/4-digit/amber/red cases (Supabase env vars aren't configured for local dev, so the live data path couldn't be exercised — see the Supabase env var bug note above).

**Files changed:**
- `components/sections/NutritionSection.tsx`: `fmt()` in the calorie stat strip changed to return `{ value, unit }`; each column now renders the value and "kcal" unit as two separate `<span>`s instead of one concatenated string.

### Navigation (4 tabs — restructured April 30, 2026)

| Index | Label | Component | Status |
|---|---|---|---|
| 0 | Today | Daily logging accordions (inline in app/page.tsx) | Live |
| 1 | Health Log | Hero card + appointment list (inline in app/page.tsx) | Live — design Step 5 complete |
| 2 | Coach | `CoachTab` | Live |
| 3 | Dashboard | Inline in app/page.tsx (charts + history list) | Live — design Step 6 complete |

Default active tab: 0 (Today). GoalsTab, DashboardTab, and HistoryTab components preserved on disk but removed from the tab bar and no longer imported in app/page.tsx.

### UI fixes (May 11, 2026 — BUGs 5, 6, 7)

- **BUG 5 — App icon:** `scripts/generate-icon.mjs` background colour updated from `#0a0a0a` to `#0B1120` (dark navy). `public/apple-touch-icon.png` regenerated.
- **BUG 6 — Hero greeting overflow:** `app/page.tsx` greeting `fontSize` changed from `var(--fs-display)` to `clamp(1.6rem, 5vw, 2.4rem)` so it scales down on long messages.
- **BUG 7 — Tab label:** Tab index 1 label changed from "Health Calendar" to "Health Log" in `app/page.tsx`.

### Glucose Stability card rebuilt to 5 rows + Sleep fasting-glucose decimal fix (Sept 8, 2026; bugfix pass same day)

**Sleep section — fasting glucose now accepts decimals.** `components/sections/SleepSection.tsx`'s `fasting_glucose_mmol` field couldn't accept values like "5.1" on mobile. Root cause was **not** the parse logic — the shared `NumInput` component's `onChange` already used `Number(e.target.value)`, which handles decimals fine, not `parseInt`. The actual cause was `inputMode="numeric"`, which gives iOS/Android a numeric-only keypad with no decimal point — "5.1" couldn't physically be typed. Fix: `NumInput` gained optional `step`/`inputMode` props (defaulting to the prior behaviour, so HRV/RHR/Nap are unaffected); the fasting glucose call site now passes `step="0.1"` `inputMode="decimal"`. No other Sleep field changed.

**Glucose Stability card — expanded state rebuilt from a 7-day-fasting-average + HbA1c layout to 5 rows** (`app/page.tsx`, Today Tab → Long-term Goals). The 7-day rolling fasting-glucose average is dropped entirely from this card — the `glucoseAvg`/`validGlucose` computation was removed from `app/page.tsx`. (The underlying `goalsData.fastingGlucose7d` fetch in `lib/db.ts → getGoalsData()` and its type in `lib/types.ts` are untouched — `GoalsTab.tsx`, preserved-on-disk and not imported, still reads it.) Built from a mockup (`glucose-stability-v1-mockup.html`, not committed to the repo) with two deliberate departures from it: the mockup's "Log lows from screenshot" button and confirm-list overlay sheet (a planned photo-import flow for low-glucose events) were dropped entirely — no camera input, no Vision route, no overlay were built, that flow is cancelled; and Waking Glucose (row 1 below) is NOT auto-filled from `cgm_readings` this session, manual only — see row 1.

Rows, top to bottom:
1. **Wearing CGM toggle** — moved to the top of the expanded body (was at the bottom). Existing `cgmEnabled` local state, unpersisted, resets on reload — unchanged mechanism, only its position and caption changed. Gates rows 2 and 4 only (not 1, 3, or 5). Caption reads "Gates Day Average and Low Events" (bugfix pass, same day — was "Turns off Day Average + Low Events", which read backwards while the toggle was ON since it describes the effect of switching it off; the new wording is neutral regardless of toggle state). **⚠️ Open decision, not resolved this session:** making this toggle persist would need a new column and a migration — deliberately out of scope. Flagging for Julie to decide whether/when to persist it.
2. **Waking Glucose** — reads `daily_entries.fasting_glucose_mmol` (via `entry.sleep.fasting_glucose_mmol`) for the currently-navigated date. Manual only this session — deliberately **not** sourced from `cgm_readings`, since the HAE value there is a same-day running average, not a waking reading, and wiring it here would display a number the label doesn't describe. Same three-colour threshold as the pre-existing Fasting Glucose readout (`--color-status-optimal` ≤4.8 / `--color-status-moderate` ≤5.6 / `--color-status-low` above, factored into a new shared `glucoseColor()` helper). Caption "Manual entry · edit in Sleep". Em dash when null. Always renders regardless of the CGM toggle. **Confirmed date-specific, no fallback** (bugfix-pass verification): `entry` comes from `loadEntry(date)` in `lib/db.ts`, which does `.eq('date', date).maybeSingle()` and returns `emptyEntry(date)` (all fields null) when no row exists for that exact date — never falls back to the most recent non-null value from another date. No code change was needed here.
3. **Day Average** — calls `/api/cgm?source=GlucosePhone` (see the `?source=` param added to that route above) via `fetchLatestCgmReading()`, which gained an optional `source` argument. Same colour thresholds as Waking Glucose. Em dash + "No CGM data" when no row exists. Gated by the CGM toggle; hidden when off. The state backing this row was renamed from `cgmReading` to `dayAverageReading` for clarity. **Caption is now conditional (bugfix pass, same day)** — see below.
4. **Meal Spikes Today** — count of today's (Europe/Berlin, 05:00 boundary) logged meals with `meal_logs.peak_glucose_mmol > 7.5`. Sourced from the existing `/api/nutrition/day?date=` route (no new route or query) via a new `fetchMealSpikesToday()` wrapper, loaded once on mount alongside the other not-date-navigable Today-tab data (same pattern as the GLP-1 card and Day Average — always reflects real "today", not the currently-navigated date). Caption "Peaks >7.5 mmol/L · from logged meals". Always renders regardless of the CGM toggle.
5. **Low Events Today** — **empty state only this session, by design.** No data source exists for it yet; the connector that will eventually supply it (LibreView, same future feed mentioned in the `cgm_readings`/`?source=` notes above) is out of scope for this session. Renders a placeholder value and a caption stating no connector is set up yet. No `glucose_low_events` table or anything like it was created. Gated by the CGM toggle; hidden when off.
6. **HbA1c** — unchanged (value, lab date, status label, Log/Update inline form all identical to before), only its position moved — now last instead of second.

**Collapsed header** now shows the Day Average value (gated by the toggle — reads "—" when the toggle is off, "No data" when on but no reading exists), replacing the old 7-day-average summary.

**Colour tokens:** all existing (`--color-status-optimal`/`-moderate`/`-low`, `--color-text-muted`/`-dim`, `--color-navy`, `--color-bg`, `--color-border-subtle`). **Flagged, not built:** the mockup's status pills use soft-tinted backgrounds (`success-soft`, `danger-soft`, a grey "no data" pill fill) that have no matching CSS custom property in this codebase — only its `amber-soft` matches (`--color-amber-tint`). Rather than inventing new hex values, status is shown as colour-coded text/labels instead of pill badges, matching this same card's own pre-existing HbA1c-row convention (colour-coded value + colour-coded status word, no background pill).

**Backlog note:** checked the BACKLOG section below for an existing item about the CGM row implying more precision than it has — no such item was found phrased that way, so there's nothing to formally close out. Row 3's caption already addresses the underlying concern regardless — see the bugfix pass directly below.

**Bugfix pass (Sept 8, 2026, later same day) — Day Average caption self-contradiction.** Live testing surfaced "Today's average so far · as of yesterday" — self-contradictory, and wrong in substance (a "yesterday" row is a complete day's average, not one still accumulating). **Root cause, found by tracing `formatAsOf()`:** `cgm_readings`' daily-average row is stamped to **Berlin local midnight**; Postgres/Supabase return `recorded_at` as a UTC timestamp, and Berlin midnight is UTC+1/+2, so it lands on the *previous* UTC calendar date. `formatAsOf()` compared that UTC date string against `todayStr()`/`yesterdayStr()` (also UTC-based) — for most of any given Berlin day, this misclassified that day's own reading as "yesterday," even though a hardcoded "Today's average so far" prefix was shown unconditionally alongside it.
- **Fix — `formatAsOf()` itself** (its only call site is this row, so this was safe to change in place rather than adding a second, parallel date helper): now compares **Berlin calendar dates** — `new Date(recordedAt).toLocaleDateString('en-CA', { timeZone: 'Europe/Berlin' })` against `getTodayBerlin()` and `shiftDay(todayBerlin, -1)` — the same Berlin-date technique already used elsewhere in this file and in `NutritionSection.tsx`, instead of raw UTC date-slicing. `todayStr()`/`yesterdayStr()` themselves (the app's date-navigator "today," used throughout `loadDay()` etc.) were **not** touched — only `formatAsOf()`'s internal comparison.
- **Caption is now conditional on `formatAsOf() === 'today'`:** row genuinely from today → `"Today's average so far · as of today"`; older → `` `Full-day average · ${formatAsOf(...)}` `` (renders "yesterday" or a short date). The value, colour thresholds, and the no-data empty state ("No CGM data") are unchanged.
- **Deliberately did NOT apply a literal 05:00 boundary**, despite that being the app's existing day-boundary convention for nutrition (`dayKeyFromTimestamp()` in `lib/nutrition.ts`) — flagging why: that function rolls any local time before 05:00 back to the previous calendar date, and this row's timestamp is stamped to Berlin midnight (00:00), which is *always* before 05:00. Applying it literally would make every day's own average permanently classify as "yesterday" for its entire lifetime — reproducing the exact bug being fixed, just unconditionally. Verified in isolation (Node, simulating a Berlin-midnight-stamped Sept 8 row viewed at 14:00 Berlin the same day): the fixed `formatAsOf()` correctly returns `'today'`; the prior UTC-slicing version returns `'yesterday'` on the same input, confirming both the root cause and the fix.

**Verification questions answered this pass:**
1. `tsc --noEmit`, `eslint`, `next build` — all re-ran clean (0 errors; same 6 pre-existing unused-var warnings in `app/page.tsx`, none from this session's code).
2. No hardcoded hex/`rgb()`/named colours were introduced by the card rebuild — `glucoseColor()` and every new row use only existing `var(--color-...)` tokens. One pre-existing `color: '#fff'` was found nearby (the card's header icon tile) — it predates this session, was never touched by the rebuild, and is outside "every new row's styling," but is flagged here for transparency.
3. Confirmed — Waking Glucose reads `entry.sleep.fasting_glucose_mmol`, where `entry` is loaded via `fetchEntry(currentDate)` for the currently-navigated date; nothing in that row reads `cgm_readings`.
4. Confirmed — the collapsed header shows the Day Average value (`{dayAverageReading.value_mmol.toFixed(1)} mmol/L`, or "No data"/"—" depending on toggle/data state), replacing the old 7-day-average summary. This was implemented in the original rebuild but not explicitly called out in that session's summary.

Verified: `tsc --noEmit`, `eslint`, `next build` all clean. Exercised in local dev (browser preview, mobile viewport) — toggle on/off, all five rows in both populated and empty states rendered correctly, toggle caption reads correctly in both states; the underlying data fetches themselves 500 locally (no local `SUPABASE_SERVICE_ROLE_KEY`, same pre-existing limitation as every other service-role route) and degrade gracefully, same as elsewhere in this app — real data verification happens on the Vercel branch preview. Not merged — pushed to a new branch; no PR opened.

### Today Tab (updated May 11, 2026 — UI polish fixes)

**UI fixes (May 11, 2026):**
- Score card bullet items: added `minWidth: 0` to the text span inside each bullet's flex row so long lines (e.g. "Nutrition: 126g protein, 60g fiber") wrap rather than overflow the card.
- Macro progress bar track: colour changed from `--color-primary-light` (light green) to `--color-navy-tint` (`#E8EEF7`, a very light blue tint of dark navy). New token `--color-navy-tint` added to `:root` in `globals.css`.
- Supplements StackAccordion "Taken" label: colour changed from `--color-status-optimal` (green) to `--color-navy` (dark navy). "Not taken" remains `--color-text-muted`.

### Today Tab (updated July 12, 2026 — hero redesigned, photos removed)

**Hero — solid navy + BodyCipher mark (July 12, 2026):**
Full-width hero container above the score cards, in `app/page.tsx`. Negative margins (`-20px` left/right/top) break out of the main container padding to reach full width — unchanged.
- **Photo system removed.** The `heroImage` state (random selection from 10 images in `/public/images/hero/`) and its `backgroundImage`/gradient-overlay styling are gone. Background is now solid `var(--color-navy)`.
- Images themselves (state-easy-blackforest, state-easy-bovic, state-easy-mushrooms, state-hard-iceland, state-moderate-scotland, state-moderate, state-nodata, state-rest-cat, state-rest-norway, state-rest) are **preserved on disk** in `/public/images/hero/`, just no longer referenced anywhere — same deprecated-but-preserved pattern as `DashboardTab.tsx`/`HistoryTab.tsx`/`GoalsTab.tsx`.
- **BodyCipher mark (refined July 12, 2026):** originally an oversized (130px wide) faint (`opacity: 0.08`) watermark spanning most of the hero — read as a faded echo of the splash screen, not a header logo. Replaced with a small (30×34px), fully solid `white` icon: same body-silhouette + ECG-line paths from `SplashScreen.tsx`/`generate-icon.mjs` (same `viewBox="220 36 220 250"`), amber dots dropped so it's a clean single-colour mark. `white` matches the existing convention already used for every other on-navy element in this hero (date text, chevrons, greeting) — there is no dedicated on-navy CSS token in this codebase. Rendered in its own flex row (`justifyContent: flex-end`, `padding: '12px 20px 0'`) directly above the date-navigator row, right-aligned — layout-driven placement rather than absolute-positioned coordinates, so it can't collide with the `›` chevron below it. Still `aria-hidden`, still decorative, still visually secondary to the greeting.
- **Greeting is now the primary visual element.** Font size increased from `clamp(1.6rem, 5vw, 2.4rem)` / `--fw-bold` to `clamp(2rem, 7vw, var(--fs-score-display))` (32–48px) / `--fw-score-display` (800). Still clamped/responsive so long greetings don't overflow (preserves the May 11 BUG 6 fix).
- **Height:** the fixed `320px` was dropped in favor of `minHeight: 200` plus padding (`paddingBottom: var(--space-lg)` on the container, `padding: '20px 20px 0'` on the greeting) — a solid navy block that tall with no photo behind it read as dead space. The hero now grows/shrinks naturally with greeting length instead of being pinned to a fixed height.
- Date navigator: unchanged — same logic, same white styling, same `‹/›` disabled-state treatment.
- Yesterday sleep banner and score card spacing: unchanged from the May 11 hero-photo layout.

### Today Tab (updated May 10, 2026 — Long-term Goals cards design-aligned)

**Long-term Goals design pass (May 10, 2026):**
VO2 Max and Cardiovascular Health cards aligned to DESIGN.md tokens:
- VO2 Max status badge: amber filled pill (`--color-amber` bg, `--color-navy` text, `--radius-full`, `--fs-label`, `--fw-bold`, uppercase); removed green outline/arrow style
- VO2 Max spectrum bar: flat grey track (`--color-border`), amber fill from left to current value (`--color-amber`), simple navy vertical tick at target (40); removed gradient fill
- VO2 Max band labels: `--fs-label` (12px), `--color-text-secondary`; removed monospace/hardcoded sizes
- LDL and HDL spectrum bars: same flat grey + amber fill treatment; removed gradient fills
- LDL/HDL active band labels: amber colour + bold weight; inactive bands `--color-text-secondary`
- Log bloodwork button: `btn-primary` class (navy bg, white text, `--radius-lg`, height 52px, full width); was `btn-secondary`
- Overdue warning text: `--color-amber`, `--fs-label`, `--fw-bold`, uppercase; was `--color-danger`

### Today Tab (updated May 10, 2026 — accordion interior restyling complete)

**Accordion open/expanded state — design pass (May 10, 2026):**
All six Today tab accordions restyled to match DESIGN.md tokens:
- Field labels: `--fs-label` (12px), `--fw-bold` (700), `--ls-label-bold` (0.05em), uppercase, `--color-text-secondary`
- All text inputs, number inputs, textareas: `minHeight: 48px`, padding `--space-sm`/`--space-md`, `font-size: --fs-body` (16px), `border-radius: --radius-md`, `1px solid --color-border`
- Field container gap: `--space-md` (16px) throughout
- Save buttons: `btn-primary` class (height 52px, `--color-navy`, `--radius-lg` after globals.css update)
- `components/ui/Section.tsx` now accepts optional `forceClose?: number` prop — when incremented, closes the accordion
- Auto-close after successful save wired into all five save handlers (Sleep, Training, Hydration, Supplements, Context)
- `globals.css`: `btn-primary` border-radius updated from `--radius-md` to `--radius-lg`; toggle checked colour updated from `--color-navy` to `--color-amber` (per DESIGN.md Toggle Row spec)
- Supplement `StackAccordion`: redesigned to match DESIGN.md Toggle Row spec — supplement name `--fs-body`, dose `--fs-label-sm` below name, `--color-border-subtle` dividers, min-height 56px per row, amber toggle when checked
- `HormoneCard`: border highlights amber when taken (was primary-colour), background is always `--color-surface` (removed primary-light tint)
- NutritionSection: no save handler — no auto-close; macro summary card background changed from `--color-bg` to `--color-surface`

---

### Today Tab (updated April 30, 2026)

**Structure (top to bottom):**
1. **Hero container** (320px, full-width) — random background photo + gradient overlay + date navigator (white) + greeting (white, `--fs-display`, bottom of hero)
2. Yesterday sleep banner (conditional, below hero)
3. **Score cards row** — Behavior Score, Outcome Score, Training Load (detailed versions with bullet points; copied from DashboardTab; data fetched via `GET /api/scores` + `loadRecentEntries(30)`)
5. Daily logging accordions (Sleep, Training, Nutrition, Hydration, Supplements, Context) — unchanged
6. **Long-term Goals section** — VO2 Max (spectrum bar + sparkline), Cardiovascular Health (LDL/HDL spectrums + ratio trend), Glucose Stability (5-row rebuild — Waking Glucose, Day Average, Meal Spikes Today, Low Events Today, HbA1c; see the dedicated "Glucose Stability card rebuilt to 5 rows" entry below for detail); copied from GoalsTab
7. Saved confirmation toast

All new state, effects, and handlers for the above sections live in `app/page.tsx` (`App` component). GoalsTab.tsx and DashboardTab.tsx are unchanged — copy only.

**Sleep section**
- Fields: duration (min), HRV (ms), RHR (bpm), bedtime, rested score (1–5), nap duration (min), fasting glucose (mmol/L, optional)
- All fields save to `daily_entries`

**Training section**
- Multiple sessions per day. Per session: activity type, duration (min), zone3+ minutes, active calories.
- Sessions stored in the `training_sessions` table (separate normalised table — NOT JSONB in daily_entries).
- Zone3+ intensity: 0–5 min = Easy, 6–15 = Moderate, 16+ = Hard
- Quick-add buttons: Swim, eGym, Run, Walk, Cycling. Cycling is a regular session type (for no-watch days) — no longer a separate transport toggle.
- "Cycled today (transport)" UI removed. `cycled_today`, `cycling_minutes`, `cycling_calories` columns remain in `daily_entries` and `TrainingData` for historical data compatibility — do not read or write from new code.
- Session cards show `start_time` as local "HH:MM" when present (populated by /api/health-import).
- Activity icons standardised across TrainingSection and HistoryTab (see ACTIVITY ICONS below).
- `avg_heart_rate` is permanently removed. Do not re-add.

**Nutrition section**
- Full ingredient-level food logging. Built and live on main.
- Eight Supabase tables: food_items, meal_logs, meal_log_items, daily_nutrition_summary, meal_templates, meal_template_items, recipes, recipe_ingredients.
- Five-screen logging flow: bottom sheet → search → weight entry → building meal → save confirmation.
- USDA FoodData Central integration (server-side only, cached to food_items on first use).
- Open Food Facts barcode scanning via html5-qrcode.
- **Bug fix (August 28, 2026) — intermittent freeze on installed iOS PWA:** confirmed via manual testing to be iOS-specific and intermittent — the barcode scan screen works normally in regular mobile Safari, but opening it in the installed home-screen PWA sometimes shows the camera UI with no permission prompt, no error, and no way out; force-quitting and relaunching the installed PWA temporarily fixes it. Root cause: iOS can leave `getUserMedia()` (called internally by `Html5Qrcode.start()`) permanently pending after the installed PWA is backgrounded and resumed — the promise never resolves or rejects, so the existing try/catch around camera init never fires and the screen just sits on "scanning" forever. Fixed by racing `scanner.start()` against a 7-second timeout in `ScreenScan`'s camera-init `useEffect` (`components/nutrition/MealLogger.tsx`); if the timer wins, it's treated as a normal start failure — same existing "Could not start camera" error UI and retry button, no new UI. The timeout is cleared if `start()` settles first, so a normal slow-but-working camera init isn't falsely killed. If the timeout fires, any late-resolving `start()` is stopped/cleared in the background so it can't keep the camera running (or fire a decode) behind the error screen. Investigation ruled out code regressions (no relevant recent commits), dependency drift (`html5-qrcode` unchanged since introduction), service worker interference (`public/sw.js` has no `fetch` handler), and CSP/Permissions-Policy (none configured anywhere in the app) before landing on this iOS-standalone-mode explanation. The `retry()` function (separate camera-restart path reachable from the error screen's "Try again" button) was deliberately left without its own timeout guard — out of scope for this fix, flagged as a follow-up if the same hang is ever hit twice in a row.
- Recipe builder: cooked and raw/assembled modes. Draft state for cooked recipes awaiting cooked weight.
- Photo estimation: Anthropic Vision, macros stored as top-level fields on meal_logs (logged_via = 'photo_estimate').
- My Library: recipes only. Templates tables and API routes preserved in DB, not surfaced in UI. Do not re-add templates to UI.
- Macro summary bar: consumed vs target for all 5 macros. Read from daily_nutrition_summary.
- Peak glucose: inline editable field on each meal card. PATCH /api/nutrition/meal. Not on any logging screen.
- Macro override: pencil icon on local library results. PATCH /api/nutrition/food-item.
- daily_nutrition_summary upserted after every meal save/edit/delete. Coach and Dashboard read from this.
- **Photo estimate meals are included in the summary:** `recomputeDailySummary` sums ingredient-based macros from `meal_log_items` then adds top-level macro fields (`calories`, `protein_g`, etc.) from `meal_logs` where `logged_via = 'photo_estimate'`. Both paths confirmed correct.
- **Nutrition accordion checkmark logic (updated April 28, 2026):** Green checkmark = protein ≥ 130g AND fiber ≥ 30g (both primary targets hit). Amber circle = meals logged but targets not fully met. No indicator = nothing logged. Reads from `daily_nutrition_summary` via the existing `day.summary` fetch. Implemented via `isPartial` prop on `components/ui/Section.tsx`.
- **Voice dictation on description field (Session F, May 10, 2026):** Mic icon button added to the Description label row in `ScreenPhotoEstimate`. Uses Web Speech API (`SpeechRecognition` / `webkitSpeechRecognition`). Tapping starts recognition; result appended to existing description text. Tapping again stops an active session. Button is hidden entirely if the browser does not support speech recognition. Recognizing state shown via amber icon colour. Implemented in `components/nutrition/MealLogger.tsx`.
- **Photo library picker (Session F, May 10, 2026):** `ImagePlus` button added alongside the existing camera button in `ScreenPhotoEstimate`. Camera button (`capture="environment"`) unchanged. Library button triggers a separate hidden `<input type="file" accept="image/*">` (no `capture` attribute) — opens the photo gallery on iOS, file picker on desktop. Both inputs share the same `imageFile` state and `setImageFile` handler. `lucide-react` package added as a dependency.
- **ScreenPhotoEstimate unified composer redesign (May 17, 2026):** The two separate Photo and Description sections replaced with a single composer card (`--color-surface`, `--radius-lg`, `--shadow-card`, `--space-md` padding). Card structure top-to-bottom: (1) photo thumbnail (max 180px, `object-fit: cover`, `--radius-md`, absolute-positioned × button to clear) — only rendered when `imageFile` is set; (2) auto-expanding textarea (rows 3 min, right-padding 44px) with mic button pinned inside bottom-right corner (same speech recognition logic); (3) toolbar row (1px `--color-border` top border) with camera icon label (44px, `capture="environment"` hidden input inside) and library icon button (44px, triggers `galleryInputRef`). No section labels. All state, handlers, hidden inputs, and the Estimate macros button are unchanged.
- **"Estimate with Claude" ingredient mode (May 17, 2026):** `ScreenSearch` (the "Add ingredient" screen reached via "+ Add another" from Building meal) now shows an "Estimate with Claude" `btn-secondary` button alongside "Create a recipe" and "Browse Library" when `context === 'meal'`. Tapping it sets `photoEstimateAsIngredient = true` and navigates to `photoEstimate` screen. `ScreenPhotoEstimate` gained two props: `ingredientMode?: boolean` and `onAddIngredient?: (item: BuildingItem) => void`. In ingredient mode, after the estimate API returns, an inline results step is shown (macro card with "Claude estimate · confidence" badge, "Per serving (100g)" breakdown) instead of navigating to ScreenConfirm. The "Add to meal" button POSTs to `/api/nutrition/food-item` with `source: 'ai_estimate'` and the estimated macros as `nutrients_per_100g`, then calls `onAddIngredient({ food_item, weight_grams: 100 })` which appends the item to `items[]` in the main component and navigates to `building`. Back on the results step clears `ingredientEstimate` and returns to the input step. Back on the input step returns to `search`. `/api/nutrition/food-item` POST route updated: `'ai_estimate'` added to the source type annotation and validation allowlist. `FoodItem.source` type updated in `MealLogger.tsx` (both the interface and `EditingMealProp` inline shape). `food_items.source` allowlist in BODYCIPHER.md: add `'ai_estimate'`. No weight entry screen for AI-estimated items — 100g weight is assumed and the math is correct (macros = nutrients_per_100g × 100 / 100).
- **Bug fix (May 18, 2026) — ai_estimate deduplication:** POST /api/nutrition/food-item was deduplicating `ai_estimate` items by name, returning a cached row with stale macros when the name matched a prior estimate (default name 'Estimated item' triggered this on every second photo-only estimate with no description). The review screen showed fresh `ingredientEstimate` values but `j.food_item` had the old nutrients, so the building screen and saved meal had wrong fiber and carbs. Fix: `ai_estimate` items skip the name-based lookup and always insert a fresh row. One-line change in `app/api/nutrition/food-item/route.ts`.
- Legacy daily_entries.nutrition JSONB fields are dead — do not read or write from any new code.

**Hydration section**
- Logs ml per day. Passes through to Coach context.

**Supplements section**
- Hormones (updated Aug 18, 2026 — three-toggle model): Progesterone (mg, toggle, unchanged) · **Estradiol AM** (sprays, toggle) · **Estradiol PM** (sprays, toggle) · **Testosterone AM** (pumps, toggle).
  - **Estradiol split (Aug 18, 2026):** Lenzetto spray dosing changed from one daily dose of 3 sprays to twice-daily (one spray AM, one spray PM). The old single `estradiol_taken`/`estradiol_sprays` toggle was replaced by two independent toggles, `Estradiol AM` and `Estradiol PM`, each with its own dose field. The old fields are retired (see Dead columns below), not removed.
  - **Testosterone added (Aug 18, 2026):** New transdermal testosterone protocol, one pump, mornings only. New `Testosterone AM` `HormoneCard`, dose field labelled "pumps". Not yet in hand at time of build (on order) — built ahead of time, defaults OFF like every other supplement until Julie actively confirms.
  - **Auto-default dose (Aug 18, 2026):** For all three new toggles (Estradiol AM, Estradiol PM, Testosterone AM), switching ON with a null dose auto-defaults the count to **1** (not 3 — the old 3-spray auto-default applied only to the retired single-dose estradiol field and was not carried forward, since each new toggle represents one spray/pump per dose). Toggle OFF does not clear the count, same as before.
  - Progesterone card unchanged — same mg toggle, same auto-default behaviour it always had (none).
- Morning stack (accordion, default collapsed): Creatine 5g, Vitamin D3+K2, Zinc+Selenium, Glucosamine, Omega-3, Berberine, DIM
- Evening stack (accordion, default collapsed): Magnesium glycinate 200mg, L-Theanine, Phosphatidylserine 150mg
- **DEFAULT STATE: everything OFF.** User must actively confirm.
- Ashwagandha remains removed — do not re-add. Phosphatidylserine 150mg was re-added to the evening stack in July 2026 (Julie resumed taking it nightly; no longer treated as cyclic/excluded).
- **Collapsed summary pill fix (July 12, 2026):** the summary pill ("AM ✓ · PM ✓ · Prog ✓ · E2 ✓") is built in `SupplementsSection.tsx`. It used to be gated on `isComplete` (`morning_stack_taken || evening_stack_taken`), so saving only estradiol or only progesterone rendered no pill at all. Fixed by gating the pill on a separate `anyBadgeSaved` check (`morning_stack_taken || evening_stack_taken || progesterone_taken || estradiol_taken`) — each of the four badges was already rendered independently inside the pill via `.filter(Boolean)`; only the outer gate was wrong. `isComplete` itself (still morning/evening-only) is unchanged and still drives the accordion header's green checkmark — that's a separate "day confirmed" concept from "is there anything to show in the pill."
- **Collapsed summary pill update (Aug 18, 2026):** `anyBadgeSaved` now also checks `estradiol_am_taken`, `estradiol_pm_taken`, and `testosterone_taken` (the retired `estradiol_taken` dropped from the gate). The pill stays at 5 compact badges, not 6: the single `E2 ✓` badge is now gated on `estradiol_am_taken || estradiol_pm_taken` (either dose shows it — kept as one badge rather than splitting into E2-AM/E2-PM to avoid cluttering a pill that already carries AM-stack/PM-stack/Prog) plus one new `T ✓` badge gated on `testosterone_taken`. Same pattern applied to the Today-tab Behavior Score bullet (`getBehaviorBullets()`) and the Dashboard history row detail (`EntryDetail()`), both in `app/page.tsx` — both now list `E2` (OR'd across AM/PM) and `T` alongside `AM`/`PM`/`Prog`.
- **⚠️ Flagged for review — Behavior Score hormone component (`lib/scores.ts → behaviorScore()`):** this is the locked scoring model's supplement bucket (morning 40% / evening 30% / progesterone 15% / estradiol 15% of the 20%-weight component). Two judgment calls were made rather than guessed silently:
  1. **Estradiol AM vs PM — used AND, not OR.** `estradiolConfirmed = sup.estradiol_am_taken && sup.estradiol_pm_taken` — full 15 points only when *both* doses are logged for the day. Rationale: the original toggle was all-or-nothing (one spray count credited the whole day), so requiring both doses mirrors "confirmed for the day" rather than crediting a half-day partial. **This differs from the collapsed-pill/bullet display logic above, which uses OR** (either dose shows "E2 ✓") — display optimises for "is anything logged", scoring optimises for "was the full regimen followed". If Julie wants OR here too (any one dose = full credit, matching pre-split leniency), that's a one-line change (`||` instead of `&&`).
  2. **Testosterone is NOT added to this component.** Folding a 5th supplement into a locked 40/30/15/15 split would mean re-deriving those weights (e.g. to fit a testosterone bucket, something would need to shrink) on a model explicitly marked "do not change without explicit instruction." Left out entirely for now — testosterone toggle state has no effect on Behavior Score. Flagging this for Julie to decide how (or whether) to weight it in.
- **Estradiol reverted to single AM-only dose (Aug 30, 2026):** the Aug 18, 2026 twice-daily AM/PM split above is reverted. No night sweats for a sustained period made the PM dose unnecessary — back to one daily Lenzetto dose, taken in the morning, now at **2 sprays** (was 3 sprays pre-split, then 1+1 AM/PM during the split; net dose reduced from 3 to 2).
  - **UI (`SupplementsSection.tsx`):** the "Estradiol PM" `HormoneCard` is removed entirely. "Estradiol AM" is renamed to just "Estradiol" (label text only — still reads/writes `estradiol_am_taken`/`estradiol_am_sprays`, field names unchanged). Auto-default-on-toggle-ON for this card only changed from 1 to **2**; Testosterone AM's auto-default-to-1 is untouched.
  - **`estradiol_pm_taken`/`estradiol_pm_sprays` are now dead fields** — same pattern as the original pre-split `estradiol_taken`/`estradiol_sprays` columns (see Dead columns below): stay mapped through `lib/types.ts` (`SupplementsData`) and `lib/db.ts` (`rowToEntry()`/`saveEntry()`, both directions) for historical row integrity, no longer read or written by any UI, scoring, or Coach code as of this change. Columns and mapping not dropped — no DB migration involved, nothing added or removed at the schema level.
  - **Collapsed summary pill and display logic (`SupplementsSection.tsx`, `app/page.tsx`):** the Aug 18, 2026 OR-across-AM/PM logic is reverted. `anyBadgeSaved` no longer checks `estradiol_pm_taken`. The `E2 ✓` badge, the Today-tab Behavior Score bullet (`getBehaviorBullets()`), and the Dashboard history row detail (`EntryDetail()`) all key off `estradiol_am_taken` alone now, not `estradiol_am_taken || estradiol_pm_taken`.
  - **Behavior Score (`lib/scores.ts → behaviorScore()`):** resolves the "Flagged for review" judgment call directly above — `estradiolConfirmed` simplified from `sup.estradiol_am_taken && sup.estradiol_pm_taken` to `sup.estradiol_am_taken` alone. 40/30/15/15 split otherwise untouched; this is the only line changed in the locked scoring model.
  - **⚠️ Flagged for Julie — not closed:** three reads of `estradiol_pm_taken` were found in `app/api/coach/route.ts` and deliberately left untouched (not silently changed): the day-context line built for the Coach LLM prompt (`~line 169`, currently reads `estradiol PM ${sup.estradiol_pm_taken ? '✓' : '✗'}`), and two prompt-instruction strings (`~line 423`: "check progesterone, estradiol AM, estradiol PM, and testosterone logged"; `~line 432`: "Supplement check (progesterone, estradiol AM/PM, testosterone, evening stack)"). Until addressed, Coach will keep being fed a PM-dose status (now always `✗`, since nothing sets `estradiol_pm_taken` anymore) and will keep prompting itself to check for a PM dose that no longer exists.

**GLP-1 card (added Aug 18, 2026)**
- New card on the Today tab, positioned between the Supplements accordion and the Context accordion. `components/sections/Glp1Section.tsx` — exports default `Glp1Section` (the accordion, uses the standard `Section` wrapper) and named `Glp1RefillCard` (a separate always-visible sub-card, same "always visible, not collapsible" pattern as the Health Calendar hero card). Both rendered by `app/page.tsx` in that order, directly below `SupplementsSection`.
- **Not date-navigable, unlike the other 5 logging accordions.** Sleep/Training/Nutrition/Hydration/Supplements/Context all read/write the currently-navigated `DailyEntry` (`entry` state, keyed to whatever date the Today-tab date-navigator is on). The GLP-1 card is different: it always reflects real "today" (Europe/Berlin) and the trailing 28-day window ending today, regardless of which date is selected on the date navigator. Backed by its own state (`glp1Injections` in `app/page.tsx`), loaded once via `loadGlp1Injections()` (`lib/db.ts`) in a mount-only `useEffect`, not tied to `currentDate`.
- **Data:** `glp1_injections` table (see Data Model section below). `injection_number` is assigned as `(highest existing injection_number) + 1` at log time — this tracks *logging order*, not chronological dose order (relevant for the "Log a different date" backfill path — see below).
- **Collapsed state:** status badge + "Dose N of 24" (N = highest `injection_number` on file, 0 if no rows). Badge states, computed from `daysSinceLast = today − most-recent injection date`:
  - `daysSinceLast > 7` → **Overdue** (red, `--color-danger`)
  - `daysSinceLast === 0` → **Taken this week** (green, `--color-success`)
  - `1 ≤ daysSinceLast ≤ 7` → **Due in Nd** (amber, `--color-amber` bg / `--color-navy` text), N = `7 − daysSinceLast`
  - No rows yet → **Not started** (grey, `--color-border` bg) — a 4th state added beyond the three named in spec, since none of Overdue/Taken/Due meaningfully describe a course that hasn't begun.
  - ⚠️ **Flagged:** the spec's three conditions ("next injection upcoming" / "most recent within last 7 days" / "more than 7 days since last") are not mutually exclusive as written — "within the last 7 days" and "upcoming" describe the same non-overdue window from two angles. Resolved with the priority order above (day-of injection = green, days 1–7 = amber countdown, beyond day 7 = red). Flagging this interpretation for review since the source spec text genuinely overlaps.
- **Expanded state:**
  - Header row: "±2 Weeks" label + a month-range readout (e.g. "Aug" when the grid stays within one month, "Aug – Sep" when it straddles two).
  - **Fixed Aug 24, 2026 bugfix:** the grid used to be a trailing 28-day window ending today (`gridStart = today − 27`), padded out to whole weeks. Because the window's *end* was always today, today could land anywhere in the last row depending on its weekday — most days it landed mid-to-late in the final row, leaving the rest of that row (and the whole rest of the month) with no future cells at all, so upcoming due dates were never visible. Replaced with a **rolling ±14-day window, Monday-start/Sunday-end aligned**: `gridStart` = Monday of the week containing (today − 14), `gridEnd` = Sunday of the week containing (today + 14). Since today−14 and today+14 are exactly 4 weeks apart they always share a weekday, so this always yields exactly 35 days (5 full Mon–Sun rows) with **no blank padding cells** and today always in the middle (3rd) row. The `M T W T F S S` header still aligns correctly since the grid always starts on a Monday.
  - Cell states, in priority order: **injected** (navy fill, white bold number, small amber dot near the bottom) → **due, overdue** (2px `--color-danger` border, when the computed next-due date has passed without a log) → **due, upcoming/today** (2px `--color-amber` border) → **today, not otherwise flagged** (2px `--color-navy` border) → plain.
  - Legend below the grid: Injected / Due / Today swatches matching the three border/fill treatments above.
  - **"Log injection — today"** button (`btn-primary`, full width, 52px): inserts `date = today`, `dose_mg = 2.5`, `injection_number = max + 1`. Three states beyond the default label, all flagged as judgment calls (spec only asked about the cap explicitly):
    - At 24/24 → disabled, relabelled **"Course complete"** (spec-requested; exact treatment was left to judgement).
    - Already logged today (any dose) → disabled, relabelled **"Logged today ✓"** — added to prevent an accidental duplicate same-day row; not explicitly requested.
    - Save failure → relabelled **"Save failed — retry"**, button re-enabled.
  - **"Log a different date →"** link (hidden once at the 24-dose cap, same reasoning as disabling the main button) opens an inline form: date input (`type="date"`, `max` = today — no future-dated entries), dose input (number, step 0.25, defaults to 2.5, editable). Submits through the same `logGlp1Injection()` / injection-number auto-increment path as the main button.
- **Refill warning sub-card (`Glp1RefillCard`):** always rendered, directly below the accordion, not inside it. Navy background, "REFILL REMINDER" amber label, "Call endocrinologist" headline, "Pen runs out at dose 24 — call by dose 20" subtext, and an amber pill showing `24 − highest injection_number` as "N doses left" — computed live from `glp1Injections`, no separate storage.
  - **Urgent state (doses left ≤ 4):** card background switches from `--color-navy` to **`--color-danger`** (`#E05252`, already defined in `globals.css` — not a new hex value; distinct from `--color-status-low` `#F97316`, the orange "warning" token used for HRV/score bands, so the two are not conflated). Label text switches from amber to white (amber-on-red has weak contrast; white-on-red is the standard readable pairing for this card's existing white headline/subtext) and the label text changes to "REFILL REMINDER — CALL NOW". The "N doses left" pill itself is unchanged (amber bg / navy text) in both states since it has its own opaque background and reads fine over either card colour.

**Context section**
- Fields: cycle day (auto-counter with manual reset), symptoms (multi-select), travelling toggle, sick toggle, notes
- `hrv_score` field: REMOVED from ContextSection.tsx. Do not re-add.
- Read-only metrics strip at top of accordion interior: RESTING HR (resting_hr_daytime, bpm) / WALKING HR (walking_hr_avg, bpm) / DISTANCE (walking_running_km, km). Same visual style as TrainingSection energy strip. Shows — when null. No edit controls.
- **"Sick today" toggle (added July 26, 2026):** Same visual treatment and row placement pattern as "Travelling today" — a plain checkbox row, immediately below the travelling row, above Notes. Wired through `is_sick` on `ContextData` (`lib/types.ts`), `rowToEntry()`/`saveEntry()` (`lib/db.ts`), same as travelling. No new prop plumbing needed in `app/page.tsx` — `ContextSection`'s `onChange` already does a generic `update({ context })`. Drives Behavior/Outcome Score exclusions and Coach training-suppression — see SCORING MODEL and Coach Tab sections.

**Body Composition card (added Aug 18, 2026) — superseded August 29, 2026**

Superseded by "Body Composition card v3" directly below. Section kept for history — the weight sparkline, 65kg target-tick bar, and 3-row manual Body Scan subsection described here are no longer live code.

- New card in the Today tab's Long-term Goals section, stacked directly below the VO2 Max card and above the Cardiovascular Health card (same collapsed/expanded accordion-card pattern, amber left-border-on-expand treatment, all inline in `app/page.tsx`). Reads from `biomarker_readings` — markers `'weight'`, `'body_fat_pct'`, `'waist_cm'`, `'visceral_fat_l'` — all four added to the `.in('marker', [...])` filter in `getGoalsData()` (`lib/db.ts`) alongside the existing four.
- **Collapsed state (fixed Aug 21, 2026):** shows only the card title, no figures. The current-weight readout + ↑/↓ delta-in-kg trend indicator (vs. the previous reading, both from `goalsData.biomarkers` filtered to `marker === 'weight'` — the array is already ordered `recorded_on` descending by `getGoalsData()`, so `[0]`/`[1]` are latest/previous with no extra query) now renders only when `bodyCompExpanded` is true. Previously the badge rendered unconditionally in the header button, so it showed in both collapsed and expanded states — inconsistent with what was wanted for this card specifically. VO2 Max and Cardiovascular Health headers are unchanged and still show their summary figures while collapsed; that's deliberate, existing behaviour for those two cards, not touched by this fix. Trend colour is neutral (`--color-text-secondary`) — deliberately not colour-coded green/up-is-bad or similar, since a weight delta has no inherent good/bad direction without dieting context that isn't tracked here.
- **Expanded state:**
  - Current-weight readout (36px value + "kg" unit, same treatment as the VO2 Max expanded-state headline number) plus the same trend indicator.
  - **Target-tick bar** at 65kg: a simplified version of the VO2 Max spectrum bar (`WEIGHT_SCALE_MAX = 100`, `WEIGHT_TARGET_KG = 65`, both in `app/page.tsx`) — flat grey track, amber fill 0→current, navy target tick + "target" label below the bar, current-value tick + label above. No named Poor/Fair/Good-style bands (unlike VO2) since body-composition tiers weren't part of the spec.
  - **Weight sparkline:** `getWeightSparklineData()` (`lib/db.ts`) — up to 90 most-recent `marker = 'weight'` readings, fetched newest-first via `.order(...).limit(90)` then `.reverse()`'d to chronological order for left-to-right plotting; decimated date labels (same `step = Math.ceil(points.length / 5)` rule as VO2). The sparkline rendering reuses `buildVo2Sparkline()` directly rather than duplicating it — that function only ever reads `.value`/`.recorded_on` off a generic `BiomarkerReading[]`, nothing VO2-specific, so no fork was needed. Tap-to-reveal (`weightActiveIndex` state) included for parity with the VO2 sparkline's existing tap-to-reveal.
  - **Body Scan subsection**, below the sparkline: three rows — Body Fat % (`marker = 'body_fat_pct'`, unit `'%'`), Waist Circumference (`marker = 'waist_cm'`, unit `'cm'`), Visceral Fat (`marker = 'visceral_fat_l'`, unit `'L'`). Each shows latest value + lab date (or "Not yet logged") with a Log/Update button that opens an inline Value + Date form — same interaction pattern as the HbA1c row in the Glucose Stability card, factored into a small reusable `BodyScanRow` component (`app/page.tsx`, defined just above the `App` component) since the three rows are otherwise near-identical. All three save via `saveBodyScanReading(marker, value, unit, recordedOn)` in `lib/db.ts` — **upsert on `(user_id, marker, recorded_on)`** (fixed Aug 21, 2026; was a plain insert, matching `saveHba1cReading()`'s pattern — that meant re-saving a value for a date that already had a reading threw a unique-constraint error and surfaced as "Save failed"). The unique constraint already existed (see the `vo2_max` 23505-skip note above), so no migration was needed — code-only fix. This is distinct from both neighbouring `biomarker_readings` write patterns: `saveVo2Reading()` is still deliberately insert-only-skip-on-conflict, and the HAE weight webhook still does its own select-then-conditional-upsert — neither was touched.
- **BMI is explicitly out of scope** — no BMI mapping, no BMI storage, no BMI display anywhere in this card.
- **Weight webhook mapping (wired Aug 18, 2026, field name unconfirmed).** See the Health Auto Export → biomarker_readings mapping note below: `/api/health-import` now writes to `marker = 'weight'` on the incoming metric name `weight_body_mass` — HAE's standard identifier for Apple's body-mass metric. This name was **not verified against a real payload** before wiring (a deliberate, low-risk one-attempt call — see the mapping note for what happens if it's wrong). The debug log added earlier (metric `name` + `units`, not the full payload, for any metric `/api/health-import` doesn't recognize) is still in place as the fallback: if `weight_body_mass` turns out to be the wrong name, the real name will show up there on the next sync and the string gets fixed in one place.

### Body Composition card v3 (August 29, 2026)

Replaces the card described above wholesale. The v2 card told several small stories (a weight trend against a fixed target, three separately-logged lab values); v3 tells one: **of the weight lost, how much was fat rather than muscle and water.** Same accordion-card shell (collapsed/expanded, amber left-border-on-expand, inline in `app/page.tsx`) — only the interior changed.

**Collapsed state:** title only, no figures (unchanged from v2's fixed Aug 21 behaviour — the header no longer carries even the weight+delta badge it had in expanded state; that moved into the body as the hero, see below).

**Data layer — `getBodyCompositionData()` (`lib/db.ts`), replaces `getWeightSparklineData()`:**
- Four queries: (1) all `body_fat_pct`/`smm_kg`/`waist_cm` rows ascending, limit 500; (2) earliest `weight` row; (3) latest `weight` row; (4) `weight` rows on the same dates as the `body_fat_pct` rows from (1).
- **Anchor A** = earliest weight row ever recorded (currently 15 Feb 2026, 86.2kg) — read from the data, never hardcoded. Feeds `totalDelta = currentWeight − anchorA.value`, the headline "since Feb" number. Not date-windowed.
- **Anchor B** = the first date with *both* a weight and a `body_fat_pct` reading (currently 18 Aug 2026) — feeds the fat/lean split, since fat mass can't be computed before the first paired body-composition scan. Also not date-windowed — the split always runs from the true first paired reading, however old.
- **Fat mass** = `weight × body_fat_pct ÷ 100`, computed only on paired dates (both readings present that day) — never carried forward onto a date missing a body-fat reading, never stored as a marker.
- **Split figures**, anchor B → latest paired date: `deltaFat`, `deltaWeight`, `deltaLeanWater = deltaWeight − deltaFat`, `fatShare = deltaFat ÷ deltaWeight × 100`. Target fat share is ≥75% — not colour-coded against it anywhere on the card; it's context, not a pass/fail state.
- **Chart series** are the one windowed exception: trailing 60 calendar days, full density (one entry per day, `null` where no reading — same shape the Dashboard `HrvChart` already segments its polylines around, no thinning/smoothing on either series).
- `saveBodyScanReading()`'s marker type widened to accept `'smm_kg'` alongside the legacy `'body_fat_pct' | 'waist_cm' | 'visceral_fat_l'` — kept, not replaced, since the v3 Muscle tile writes through it too.

**Card body, top to bottom (`app/page.tsx`):**
1. **Hero** — current weight, 36px mono value + "kg" (same treatment the v2 card used).
2. **Tinted container** (`--color-navy-tint` background) holding two rows: total change ("−9.5 kg · Since 15 Feb · 86.2 kg") and the fat/lean split — a stacked horizontal bar (fat navy, lean & water amber, second segment always labelled "Lean & water", never "Other") scoped to anchor B ("Of the 3.0 kg lost since your first body scan on 18 Aug"), with the two signed kg figures below it. Bar width clamps 0–100%; a `fatShare` above 100% (lean gained while fat fell) clamps the bar at 100% and reads the second figure as a gain (e.g. "+0.4 kg lean & water") — a good outcome, not a broken bar. If `|deltaWeight| ≤ 0.1kg` (or no paired data exists at all yet), the bar is replaced by a single muted line rather than dividing by near-zero.
3. **Three tiles** (`--color-navy-tint`, except Muscle) — Muscle (amber-tinted via new `--color-amber-tint` token, the only tappable tile, latest `smm_kg` + a band pill), Body fat (latest `body_fat_pct` + static "→ 24%" reference), Waist (latest `waist_cm` + static "→ 79 cm" reference, same constant the chart 2 target line uses). **Muscle band thresholds are estimates** — below 22kg "Below normal" (`--color-danger`), 22–26kg "Normal" (`--color-navy`), above 26kg "Above normal" (`--color-navy`) — read off the InBody unit's own on-screen band positions, which carry no numeric scale of their own; treat these as approximate until a better source turns up.
4. **Muscle inline entry form** — opened by tapping the Muscle tile or the footer's "Log muscle" action (same handler, `openMuscleEntry`). Value + date (defaults to today), saves via `saveBodyScanReading('smm_kg', value, 'kg', recordedOn)`, refetches `getBodyCompositionData()` on success.
5. **Chart 1** — fat mass (navy) + `smm_kg` (amber) on one shared kg axis, 60-day full density, `BodyCompFatMuscleChart` (`app/page.tsx`). Each line is segmented around its own null gaps independently — the two series don't share every date. Tap-to-reveal follows the VO2 sparkline pattern: fixed label position in reserved top headroom, active dot enlarges, `textAnchor` flips near the edges, tapping empty space clears.
6. **Chart 2** — waist (navy), same 60-day full-density shape, `BodyCompWaistChart`, with a dashed amber target line at 79cm (`WAIST_TARGET_CM`) and a navy tick at the chart's right edge. **The "target 79" text label is pinned in fixed top-left headroom, decoupled from the line's actual x/y** — found during manual verification against live data that real waist readings sit close to the target value, so a label drawn at the target's true position collided with the recent-data dot cluster (same reason the Dashboard `HrvChart`'s baseline label is pinned in fixed screen space rather than at the baseline's own y, not a new pattern).
7. **Footer** — "Synced from Apple Health · {date of the latest weight reading}" (`bodyComp.currentWeightDate`, added to `BodyCompositionData` alongside `currentWeight`) + a "Log muscle" action.

**Colours:** fat and waist navy, muscle and lean amber, throughout — no green anywhere in this card. All via CSS custom properties (`--color-navy`, `--color-amber`, `--color-danger`, `--color-navy-tint`, new `--color-amber-tint`).

**BMI stays out of scope** — no BMI mapping, storage, or display, same as v2.

**Removed as dead code once nothing referenced them:** `WEIGHT_SCALE_MAX`/`WEIGHT_TARGET_KG` constants, `BodyScanRow` component (checked first — the Glucose Stability HbA1c row has its own inline JSX, doesn't use it), the `weightSparkline`/`weightSparklineLoaded`/`weightActiveIndex` state, `getWeightSparklineData()` (`lib/db.ts`).

---

### Coach Tab

- 5 time-based modes detected client-side from time of day:
  - wakeup: before 09:00
  - posttraining: 09:00–12:00
  - afternoon: 12:00–17:00
  - earlyevening: 17:00–20:00
  - endofday: 20:00+
- Returns JSON: {recovery, training, nutrition, insight, question}. Null fields hidden.
- Reactive chat below briefing cards.
- Model: claude-sonnet-4-6 · max_tokens 2000
- Coach reads from daily_nutrition_summary (service-role client). Legacy daily_entries.nutrition JSONB is not read.
- Training sessions loaded via loadSessionsForDates() in getCoachContext so today's sessions are visible to the coach.
- earlyevening mode populates recovery and training fields and uses "this evening" language.
- **Sick-day training suppression (added July 26, 2026):** When `today.context.is_sick` is true, `buildBriefingPrompt()` (`app/api/coach/route.ts`) appends a `SICK DAY` rule to the four modes that populate a `training` field — wakeup, posttraining, earlyevening, endofday — instructing the model not to suggest, encourage, or nudge toward any training regardless of HRV; rest/recovery framing only. `afternoon` mode is untouched since its schema already hardcodes `training: null`. Reactive chat is untouched — this only applies to the `briefing` prompt.

---

### Dashboard (updated May 10, 2026 — design Step 6 complete — inlined in app/page.tsx)

**Score cards row removed** from Dashboard tab (April 30, 2026). Score cards remain on Today tab only.

- No score cards in Dashboard tab.
- 30-day trend charts: HRV, Sleep duration, Protein, Fiber, Training minutes.
- **Training Load expandable card (design Step 6):** Primary Insight Card shell — `--color-navy` background, `--radius-lg`, `--space-lg` padding, `--shadow-card`.
  - Collapsed: status label (18px/600/white) on left, zone-coloured 10px dot + ChevronDown SVG (18px, white 70%, rotates 180° on expand) on right. Zone colour computed via `zoneDisplayColor(ratio)` using design spec thresholds (not `tlResult.colour`).
  - Expanded: white 15% opacity divider, two sub-metric boxes (ACUTE TSU / CHRONIC TSU, white 8% bg), CSS linear-gradient ratio bar (0–0.6 amber / 0.6–0.8 #6DBF8A / 0.8–1.3 status-optimal / 1.3–1.5 amber / 1.5+ status-low), 3px×20px tick marker at current ratio, scale labels (0.6/0.8/1.3/1.5), ratio value (40px/800), 30-day trend SVG (amber line, white 8% optimal band). If fewer than 2 data points: "Not enough data yet" label.
  - Tapping the Training Load score card on the Today tab navigates to Dashboard and opens this card expanded via `trainingLoadExpanded` state. Manual toggle on the Dashboard card clears `trainingLoadExpanded` and resumes normal toggle behaviour.
- **Charts (design Step 6 + targeted fixes May 10, 2026):** HRV, Sleep, Protein, Fiber, Training minutes all use inline SVGs (no Recharts). Recharts dependency fully removed from Dashboard. ChartCard: `--shadow-card`, title 18px/700/`--color-text-primary`.
  - HRV chart — "HRV Overview — 30 Days" (160px SVG, `HrvChart`): **dual-axis** (added June 26, 2026).
    - **Left axis (waking HRV):** navy polyline, amber dots above baseline / navy dots below, amber dashed baseline drawn flat at the computed rolling-median value (28-day median of non-null `hrv` in `dashEntries`, fallback 88; see HRV baseline note under Outcome Score), "baseline {value}ms" label (rounded) at the right edge of the plot area (y = SVG_PAD_T + 12, textAnchor end), horizontal grid lines, left y-axis labels.
    - **Right axis (Apple all-day avg HRV):** plots `daily_entries.apple_hrv_avg` (passed as the `apple` prop, built as `appleHrvData` from `chart30`) as a **teal** (`--color-teal`) polyline on its **own auto-fit y-scale** — Apple avg sits ~29–63ms and would crush flat on the waking-HRV scale (~45–184ms). Scale = min/max of non-null values ± 5, floored at 0. 2–3 teal right-edge y-labels. Drawn under the waking line; no dots. Only rendered when at least one non-null Apple value exists.
    - **Null gaps:** both the waking and Apple lines are segmented around null days — the line breaks rather than connecting across missing days.
    - **Viewbox:** unchanged width (300). Right-axis room is made by shrinking the plot width locally inside `HrvChart` (`PAD_R`/`plotW`), so the shared `SVG_PAD_R` and the other dashboard charts are unaffected.
    - **Legend:** HTML (below the SVG, not inside it) — three compact inline items: navy line swatch + "Waking HRV", amber dashed swatch + "Baseline", teal line swatch + "Apple avg". All swatch colours via CSS custom properties.
    - **Data wiring:** `apple_hrv_avg` is now mapped through `SleepData` (lib/types.ts), `emptyEntry()`, and `rowToEntry()` (lib/db.ts). `loadRecentEntries` already used `select('*')` — no fetch/select change, no new query. New token `--color-teal: #2DD4BF` added to `:root` in globals.css.
  - Sleep/Protein/Fiber/Training minutes bar charts (120px SVG, `BarChart2T`): two-tone bars (navy below target, amber above target), amber dashed target line. No right-side target text label on any chart.
  - Targets: Sleep 7.5h, Protein 130g, Fiber 30g, Training 60min.
- **Training Load collapsed state (May 10, 2026):** Left column shows "TRAINING LOAD" label (12px/700/0.05em/uppercase/amber) above status text (18px/600/white). Color dot + chevron remain on right.
- **30-day trend label (May 10, 2026):** "30-DAY TREND" heading (12px/700/0.05em/uppercase/white 60% opacity) added immediately above the 30-day trend SVG in expanded state.
- **History section appended below charts** (April 30, 2026): chronological entry list with expandable detail rows and "Edit this day →" link (navigates to Today tab for that date). Uses `loadAllEntries()` fetched once on mount via dedicated state (`historyEntries`, `historyLoading`, `historyError`).
- **History rows (design Step 6 + targeted fixes May 10, 2026):** HISTORY section label (14px/700/uppercase/letter-spacing 0.05em). Each row: `--shadow-card`, min-height 72px, date (24px/700), emojis (16px), BEHAV/OUTC columns. Chevron: ChevronDown SVG (not ChevronRight), hidden when row is expanded. **Session icon cap (July 9, 2026):** the collapsed row's icon list is capped at 4 icons with a "+N" overflow pill — see "Activity icons" → "Icon cap" above. The expanded row detail (`EntryDetail`) is unaffected and still lists every session.
- **⚠️ Known bug, flagged Sept 7, 2026 (found during RLS fix session 2 branch-preview testing, unrelated to RLS) — BEHAV/OUTC columns don't match the Today tab's numbers for the same date.** `HistoryRow` (`app/page.tsx`) computes these by calling `behaviorScore(entry)`/`outcomeScore(entry)` from `lib/scores.ts` with no extra arguments, which silently falls back to `nutritionSummary = undefined` (drops the entire 30%-weighted nutrition component from Behavior Score), `hrvBaseline = 88` (fixed, not the real rolling 28-day median), and `bedtimeTarget = '21:45'` (fixed, not the real rolling 30-day average) — a materially different, simplified calculation from the authoritative stored `behavior_score`/`outcome_score` columns the Today tab reads via `/api/scores`. Present since the History/Dashboard merge, commit `7c0cd7c`, April 30, 2026 (confirmed via `git log -S`) — predates both RLS sessions, not something either introduced. Not fixed as part of the RLS work; needs its own decision (show the stored value per row instead, or thread the real baselines/nutrition summary into the client recompute) before it's touched.
- Data source: `dashEntries` (already loaded by the Today tab's `loadRecentEntries(30)` effect) reused for HRV/sleep/training chart computation. `loadAllEntries()` added separately for the history list.
- **Protein and Fiber charts (updated May 1, 2026):** Read from `daily_nutrition_summary` via `GET /api/nutrition/chart?days=31` (service-role admin client, bypasses RLS). Stored in `nutritionSummaries` state as a `Record<string, { protein, fiber }>` keyed by plain YYYY-MM-DD strings using `.slice(0, 10)`. Chart arrays are built from an **independent 30-consecutive-date range** (`nutritionChart30`) ending at `currentDate`, not from `dashEntries`/`chart30` — so bars show even on dates where `daily_entries` has no row. Date generation uses `Date.UTC` arithmetic + `.toISOString().slice(0, 10)` with no local-timezone shift; map lookup is a direct string key comparison. Legacy `daily_entries.total_protein` / `total_fiber` columns are no longer read by the charts.
- DashboardTab.tsx no longer imported — content fully inlined in app/page.tsx. Component preserved on disk.

---

### Goals Tab (BodyCipher Tab) — NOT IN NAV (removed April 30, 2026)

**Hero section**
- Static rotating greeting. Client-side only — no API call. Picks randomly from a time-banded array (wakeup/midmorning/afternoon/earlyevening/endofday) matching the Coach tab's mode logic. Initialized once per session via `useState(() => getGreeting())`. API route `/api/goals/greeting` deleted.
- Three score cards in single row: Behavior, Outcome, Training Load — equal width, equal height, top-aligned. All tap → Dashboard.

**Long-term goals section**
Three collapsible cards:

- **VO2 Max** — spectrum bar (Poor <23 / Fair 23–27 / Good 28–32 / Excellent 33–36 / Superior 37+). Current value marker. Target at 40. Inline entry form. Reads from biomarker_readings (marker = 'vo2_max'). **Display value is a 60-day rolling average** (`getVo2Rolling60DayAvg()` in lib/db.ts, renamed from `getVo2Rolling30DayAvg()` July 30, 2026 — window widened 30→60 days, still anchored on the current/read-time date), formatted to 1 decimal place via `parseFloat(avg.toFixed(1))`. Single latest reading is still used to pre-fill the manual entry form. **Sparkline** (`getVo2SparklineData()` in lib/db.ts): as of July 30, 2026, fetches readings within the same 60-day date window as the average (was: up to 90 most-recent readings by COUNT) — the sparkline now shows exactly the readings feeding the rolling-average number. Query orders ascending directly (no client-side `.reverse()` needed). (Bug fix May 11 2026: was ordered ascending .limit(6), silently cutting off all readings after the 6th oldest — superseded by the July 30 date-window change.) Date labels use decimation: `step = Math.ceil(points.length / 5)`, labels rendered at `i % step === 0` plus always the last index — keeps ~5–6 visible labels regardless of dataset size. Target tick label rendered at SVG y=44 (below the tick, 8px gap after tick end at y=36); spectrum bar SVG viewBox height extended from 38 to 50 to accommodate it. Date labels on sparkline split into two stacked `<text>` elements: month at y=63, day number at y=73 — halves horizontal label width to eliminate overlap.
  - **Tap-to-reveal (added July 30, 2026, live copy only — app/page.tsx):** `vo2ActiveIndex` state (`number | null`, default null) on the `App` component, reset to null when the card collapses or after a new reading is saved. Each sparkline reading gets an invisible full-height hit-column (`plotWidth / pointCount` wide, `pointerEvents="all"`, transparent fill) layered on top of the visible dots/line/labels; tapping toggles that reading's index (tap again to deselect, tap a different reading to move selection). A transparent full-SVG background rect (also `pointerEvents="all"`) sits behind everything and clears the selection when tapped (empty space/padding). The active reading's dot renders at `r=5` instead of `r=3` — the sole visual tie between the label and the dot is this enlargement plus the label's horizontal (x) alignment over it; the label's vertical position is fixed (see below), not derived from the dot's y. A compact two-line label (value via `.toFixed(1)`, no unit; date via the existing `fmtSparkDate()`) renders at that fixed position, horizontally centered/anchored on `active.x` — `textAnchor` flips to `start`/`end` within 50px of the SVG's left/right edge to avoid clipping. Colors via existing CSS custom properties only (`--color-navy`, `--color-text-dim`), matching the sparkline's existing dot/label styling. `GoalsTab.tsx`'s preserved-on-disk, not-imported copy of the sparkline is untouched — only the renamed function call was updated there so the build's type-check doesn't break on a stale reference.
    - **Label placement fix (July 30, 2026, two rounds):** round 1 — the original above/below flip put the peak reading's label on top of the line and area fill (not enough room above near-top dots). Fixed by reserving permanent top headroom: `buildVo2Sparkline()`'s `PAD_Y` (top plot inset, in `app/page.tsx` only — `GoalsTab.tsx`'s separate copy of `buildVo2Sparkline()` is untouched) increased from 8 to 32, growing the SVG `viewBox` from `0 0 280 76` to `0 0 280 100` and shifting the date-axis label `y` coordinates from 63/73 to 87/97 by the same 24px delta. `CHART_H` (48) is unchanged, so the line/fill shape is identical, just repositioned lower in a taller SVG. Round 2 — rendering the label just above the *dot* still collided on local dips/troughs (a dip's neighbours sit higher, so a dot-relative label drops into the rising line/fill beside it). Fixed by decoupling the label's y from `active.y` entirely: the label now renders at a **fixed** position inside the `PAD_Y=32` headroom band (`valueY = 12`, `dateY = 21`) that the plotted curve can never enter, regardless of whether the tapped reading is a peak, a trough, or mid-slope. Only the label's x (and the `nearLeft`/`nearRight` `textAnchor` edge-flip, unchanged) still tracks the active dot.
- **Cardiovascular health** — LDL:HDL ratio headline, spectrum bars, sparkline. Manual entry bottom sheet. Reads from biomarker_readings.
- **Glucose stability** — Collapsed state: built and live. 7-day rolling fasting glucose average from daily_entries. CGM toggle. **Expanded state: partially built (June 22, 2026).**

Glucose stability expanded state (June 22, 2026):
- Fasting glucose: 7-day avg displayed with colour-coded value (same as collapsed). Spectrum bar not yet built.
- **HbA1c row (built):** Shows current value colour-coded (Optimal <5.7% / Watch 5.7–6.5% / High ≥6.5%), lab date, and status label. "Log"/"Update" button toggles inline entry form with Value (%) + Date inputs. Saves to `biomarker_readings` via `saveHba1cReading()` in lib/db.ts (marker = 'hba1c', unit = '%'). No migrations — table already had 'hba1c' as a live marker.
- CGM toggle: present but CGM data not yet connected.

**Health calendar section (design Step 5 complete — May 10, 2026; vaccinations section added — May 11, 2026)**
- **Hero card** at top of tab: navy card (`--color-navy`) with circular amber ring (80×80px SVG, 3px stroke, 70% opacity), type-specific icon (inline SVG, lucide-style, 28px), "UPCOMING EVENT" label, appointment name (24px, white), date (16px, 70% white), and amber days-remaining pill. Surfaces the single nearest appointment where `next_due_date` is not null. Days remaining computed in Europe/Berlin local date. Fallback: "All appointments up to date." on a navy card.
- **Appointment rows — State A (next_due_date not null):** `--color-surface` bg, 1px `--color-border` border, `--radius-lg`, `--shadow-card`, min-height 72px. Name (18px bold, `--color-text-primary`) on left with "Last done: DD Mon YYYY" below; due date (12px amber, nowrap) + ChevronDown (18px, muted) stacked on right.
- **Appointment rows — State B (next_due_date null):** Same card style, min-height 56px, single line: name (18px bold, navy) left + "Not scheduled" (16px muted) right. No chevron.
- **Expanded state:** 4px amber left border, 20px all-side padding. Field labels (14px, 700, uppercase, letter-spacing 0.05em). NEXT DUE sub-label "(Auto-computed, overridable)" at 12px muted. Inputs: 16px, 1px border, `--radius-md`, full width. Textarea: 3 rows, resize vertical. MARK AS DONE TODAY: full-width, navy, white, `--radius-full`, 52px. SAVE/CANCEL: equal flex, 44px, `--radius-full`.
- 9 default appointments seeded. Sorted by next_due_date ascending, nulls last.
- Dimming: appointments with interval_months > 6 are dimmed unless next_due_date is within 4 months or last_completed_date is null.
- Save/mark-done logic unchanged — visual restyling only.

---

### History Tab — NOT IN NAV (removed April 30, 2026)

- Chronological entries, expandable, color-coded scores. Component preserved on disk.
- History content is now inlined at the bottom of the Dashboard tab in app/page.tsx. HistoryTab.tsx is the source but is not imported.

---

## NUTRITION — DATA MODEL & LOCKED DECISIONS

### Design principles

- Ingredient-by-weight only. Everything is grams. No servings, no ml, no other units.
- Macro totals never stored at ingredient level — always computed as `nutrients_per_100g × weight_grams / 100` at read time.
- daily_nutrition_summary is the source of truth for Coach and Dashboard. Upserted after every meal save/edit/delete.
- No fixed meal slots. Free-form, ordered by logged_at.
- Meal names auto-generated if blank: before 11:00 = "Morning meal", 11:00–15:00 = "Afternoon meal", 15:00–19:00 = "Evening meal", after 19:00 = "Night meal".
- Day boundary: 05:00 Europe/Berlin.
- USDA calls server-side only via /api/nutrition/search. Cached permanently in food_items on first use. Results returned as-is — no deduplication.
- Five macros everywhere: calories, protein, carbs, fat, fiber. That order. Never four.
- No micronutrient UI in phase 1. Data preserved in nutrients_per_100g JSONB for future use.
- Clean break from legacy daily_entries.nutrition JSONB. Historical data not migrated. New data in new tables only.

### Tables

**`food_items`** — ingredient library.
- id (uuid PK), user_id (text), fdc_id (text, nullable), name (text), nutrients_per_100g (jsonb), source (text), use_count (int), created_at
- source allowlist: 'usda' | 'openfoodfacts' | 'recipe' | 'recipe_deleted' | 'custom' | 'ai_estimate'
- Macro overrides applied via PATCH /api/nutrition/food-item — merges into nutrients_per_100g, preserving raw USDA array.

**`meal_logs`** — one row per meal occasion.
- id, user_id, logged_at (timestamptz), name (text), logged_via (text), peak_glucose_mmol (numeric, nullable), notes (text, nullable)
- Top-level macro fields (calories, protein_g, carbs_g, fat_g, fiber_g): **nullable, only populated when logged_via = 'photo_estimate'**. Do not populate for ingredient-based or barcode meals.
- logged_via values: 'ingredients' | 'barcode' | 'photo_estimate' | 'manual_macros'

**`meal_log_items`** — one row per ingredient per meal. Not created for photo-estimated meals.
- id, meal_log_id (FK → meal_logs), food_item_id (FK → food_items), weight_grams (numeric)

**`daily_nutrition_summary`** — denormalized daily totals. Coach and Dashboard read from here.
- id, user_id, date (date), calories, protein, carbs, fat, fiber (all numeric), meal_count (int), logged_via_summary (jsonb, e.g. {ingredients: 2, photo_estimate: 1}), updated_at
- logged_via_summary used by Coach to calibrate confidence in nutrition advice.

**`meal_templates`** and **`meal_template_items`** — preserved in DB and API routes. Not surfaced in UI. Do not re-add to UI.

**`recipes`** — batch recipe definitions.
- id, user_id, name, total_servings (int), total_cooked_grams (numeric, nullable), default_serving_grams (numeric, nullable), is_raw (boolean DEFAULT false), food_item_id (FK → food_items, null when draft), status ('draft'|'active'), created_at, updated_at

**`recipe_ingredients`** — raw batch ingredient weights.
- id, recipe_id (FK CASCADE), food_item_id (FK RESTRICT), weight_grams (numeric > 0)

**RLS:** All eight tables have RLS enabled. Nutrition API routes use service-role key (bypasses RLS server-side). The four user_id FK constraints to auth.users were dropped April 26 — NUTRITION_USER_ID is not an auth row.

### Required environment variables (Vercel — both Production and Preview)

- `USDA_API_KEY` — server-side only
- `SUPABASE_SERVICE_ROLE_KEY` — server-side only
- `NUTRITION_USER_ID` — UUID for all nutrition rows, read via `nutritionUserId()` in lib/nutrition.ts
- `ANTHROPIC_API_KEY` — used by /api/nutrition/estimate and /api/goals/greeting, server-side only
- `HEALTH_IMPORT_SECRET` — API key checked against `x-api-key` header on /api/health-import. Server-side only.
- `VAPID_PUBLIC_KEY` — server-side only. Used by web-push when sending notifications in Sessions 2–3.
- `VAPID_PRIVATE_KEY` — server-side only. Paired with VAPID_PUBLIC_KEY. Never expose client-side.
- `VAPID_EMAIL` — server-side only. Contact email passed to web-push setVapidDetails.
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY` — client-side. Same value as VAPID_PUBLIC_KEY. Read by SwRegister.tsx when calling pushManager.subscribe().
- `MCP_SECRET` — server-side only. Dual purpose (see MCP SERVER section below): (1) the HS256 signing key for every JWT the OAuth 2.1 layer issues (client_id, authorization code, access token — inbound auth on `/api/mcp` is now bearer-token-only, the old inbound `x-mcp-secret` header check is removed); (2) still sent as the outbound `x-mcp-secret` header when the MCP tools call `/api/nutrition/meal/quick-log` and `/api/nutrition/recipe/quick-import` (unrelated, unchanged).
- `LIBRE_LINK_EMAIL` / `LIBRE_LINK_PASSWORD` — server-side only (added Sept 9, 2026, CGM connector Session A, Task 4). LibreLinkUp account credentials, read directly from `process.env` at import time by the `libre-link-unofficial-api` library itself (not read manually in app code) — used by `/api/cgm-import` to authenticate as the LibreView connector. Never referenced in client components. Locally these live in a git-ignored `.env.libre.local` (not `.env.local`), loaded ad hoc via `node --env-file=` for investigation only — the real app reads them from Vercel's ambient environment like every other server secret.

### Recipe builder — locked decisions

A recipe IS a food item. Once active, appears in ingredient search and is logged by weight.

**Two modes:**
- **Cooked (is_raw = false, default):** requires total_cooked_grams before activation. Divisor = total_cooked_grams.
- **Raw/assembled (is_raw = true):** activates as soon as ingredients are present. Divisor = sum of raw ingredient weights. total_cooked_grams stays null.

**Macro computation:** sum(nutrients_per_100g × weight_grams / 100) for all ingredients → divide by divisor → multiply by 100 → store in food_items.nutrients_per_100g.

**Deleted recipes:** food_items source set to 'recipe_deleted'. Row never hard-deleted (preserves historical logs).

### daily_nutrition_summary upsert logic

After every meal_log save, edit, or delete:
1. Compute date from logged_at using 05:00 Berlin boundary
2. Sum nutrients_per_100g × weight_grams / 100 from meal_log_items joined to food_items for that user/date
3. Add top-level macro fields from meal_logs where logged_via = 'photo_estimate' for that date
4. Count distinct meal_log_ids for meal_count
5. Build logged_via_summary JSONB
6. Upsert into daily_nutrition_summary. Delete row if no meals remain.

### API routes

| Route | Method | Purpose |
|---|---|---|
| /api/nutrition/search | GET ?q= | Local food_items first (by use_count) + USDA. No deduplication. |
| /api/nutrition/food-item | POST | Idempotent upsert (first use of USDA/OFF item) |
| /api/nutrition/food-item | PATCH | Macro override — merges into nutrients_per_100g |
| /api/nutrition/meal | POST | Create meal_log + items. Bumps use_count. Upserts summary. |
| /api/nutrition/meal | PUT | Replace items wholesale. Re-upserts summary. |
| /api/nutrition/meal | PATCH | Update peak_glucose_mmol and/or logged_at (ISO string, time-only edit — date preserved client-side). No summary recompute. |
| /api/nutrition/meal | DELETE | Delete meal_log (cascades). Re-upserts summary. |
| /api/nutrition/day | GET ?date= | Meals + items + food_items joined + peak_glucose + summary row. 05:00 boundary. |
| /api/nutrition/recipe | GET/POST/PUT/DELETE | Recipe CRUD. Handles cooked and raw modes. Creates/updates food_items entry. |
| /api/nutrition/estimate | POST | Anthropic Vision. image base64 + description → JSON macros + confidence. No DB write. |
| /api/nutrition/name-meal | POST | Anthropic Haiku. ingredient list → short meal name (max 40 chars). No DB write. Called by ScreenConfirm when mealName is empty. |
| /api/nutrition/barcode | GET ?code= | Open Food Facts lookup. Null on not-found or sparse data. |
| /api/nutrition/templates | GET/POST/PUT/DELETE | Data preserved. UI hidden. Do not surface to user. |
| /api/nutrition/recipe/quick-import | POST | Create a macro-only recipe from per-serving macro values. Called by both the local stdio MCP server and the new remote MCP route (see MCP SERVER section below) with an `x-mcp-secret` header — this route itself does not yet verify the header (unchanged, out of scope for the remote-MCP session). Requires `ALTER TABLE recipes ADD COLUMN IF NOT EXISTS ingredients_text text` migration. |
| /api/nutrition/meal/quick-log | POST | Log a meal directly from macros (MCP/Claude Desktop). Accepts: name, calories, protein, carbs, fat, fiber. Uses `logged_via = 'photo_estimate'`, sets `logged_at` to server timestamp, date computed with 05:00 Berlin boundary. Calls recomputeDailySummary + recomputeScores. No meal_log_items rows created. Added May 2026. Called with an `x-mcp-secret` header by both MCP callers (unchanged, out of scope for the remote-MCP session — this route itself does not yet verify the header). |

**ingredients_text display (added May 18, 2026):** `ingredients_text` (nullable text column on `recipes`) is now surfaced in the Library UI. GET /api/nutrition/recipe select includes `ingredients_text`. In the recipe card (list view): a single-line truncated preview renders below the macros row when `ingredients_text` is present and the recipe is not a draft. A chevron button (▼) at the card's right edge toggles an expanded detail section showing the full `ingredients_text` under an "Ingredients" label (`pre-wrap`, `--color-text-secondary`). Chevron rotates 180° when expanded. Only rendered when `ingredients_text` is non-null. In the recipe detail view (`ScreenRecipeBuilder`, opened via Edit): `ingredients_text` is threaded into `RecipeBuilderState` via `startEditRecipe` and rendered as a read-only `pre-wrap` block under a "INGREDIENTS" `FieldLabel`, above the save error and footer macros. Only rendered when non-null. Not editable — managed via the quick-import MCP route.

### Logging flow (built — do not redesign)

Screen 1 (bottom sheet): Add ingredients / Browse Library / Create a recipe / Estimate from photo or description
Screen 2: Search (ghost text autocomplete + USDA). Pencil icon on local results for macro override. Scan icon for barcode.
Screen 3: Weight entry. Live macro preview. Serving shortcut chip if available.
Screen 4: Building meal. Running ingredient list. Running macro total bar. Save meal.
Screen 5: Save confirmation. Meal name (editable), time, macros, notes. Confirm writes to DB.

**Meal name behaviour (added June 2026):**
- Recipe from Library: recipe name is pre-filled into the meal name field on ScreenConfirm. User can edit or clear it. No API call.
- Ingredient-assembled meals with no name typed: ScreenConfirm calls `/api/nutrition/name-meal` on mount (Haiku, max 60 tokens). Shows "Naming your meal…" placeholder while in flight. Result is pre-filled and editable.
- Photo-estimated meals: name is set from the estimate result's `meal_name` field (returned by `/api/nutrition/estimate`).
- Meal name display on Today tab meal cards truncates with CSS ellipsis (`overflow: hidden; text-overflow: ellipsis; white-space: nowrap`) — NutritionSection.tsx line 223.
- MCP routes (`/api/nutrition/meal/quick-log` and `/api/nutrition/recipe/quick-import`) silently truncate incoming `name` to 40 characters before any DB write.
- **Bug fix (July 2026) — word-boundary truncation for generated meal names:** `/api/nutrition/name-meal` hard-cut the model's response mid-word at 40 characters via `.slice(0, 40)`. Fixed to truncate at the last word boundary before the limit (no partial word, no "..." appended). MCP quick-log/quick-import 40-char silent truncation is unrelated and untouched.
- **Follow-up fix (July 2026) — generate complete names, don't rely on truncation:** Word-boundary truncation could still produce a sentence fragment (e.g. "Grilled chicken with roasted") when the model's raw output ran long. The `/api/nutrition/name-meal` prompt now explicitly instructs the model to count characters and return a complete, natural-sounding phrase that already fits within 40 chars on the first attempt — summarizing to the most defining ingredients rather than writing a longer name and trailing off. Word-boundary truncation remains in the code only as a safety-net fallback if the model doesn't comply.
- **Bug fix (July 2026) — real food identity for ingredient-mode photo estimates:** `/api/nutrition/estimate`'s single shared Vision prompt already asks for `meal_name` in every case (no full-meal-vs-ingredient branching server-side), so ingredient-mode estimates were already returning a real identified name — but `ScreenPhotoEstimate`'s `handleAddToMeal` (and the results-step display) in `MealLogger.tsx` ignored `ingredientEstimate.meal_name` and fell back straight to the hardcoded placeholder `'Estimated item'` whenever no text description was typed. Fixed both spots to prefer `ingredientEstimate.meal_name` before falling back to the placeholder. Full-meal photo-estimate path (`logged_via = 'photo_estimate'`) and the May 18 `ai_estimate` deduplication-skip fix are unchanged.

Peak glucose: inline editable on meal card in day view. PATCH /api/nutrition/meal. Never on logging screens.
Meal time: inline editable on meal card — tap the time to enter an `<input type="time">`, blur saves via PATCH /api/nutrition/meal with `logged_at` (ISO string). Client reconstructs the correct UTC ISO by converting the Berlin HH:MM back to UTC using `buildNewLoggedAt()` in NutritionSection.tsx. No summary recompute.

---

## MCP SERVER

### Remote MCP route (added July 2026, branch feat/mcp-remote-server)

`app/api/[transport]/route.ts` — a Vercel-hosted, stateless remote MCP server, replacing the standalone local stdio server (`~/Documents/Northstar/bodycipher-mcp`, on disk, untouched) as the primary path for MCP-based logging.

- **Endpoint:** `https://health-coach-rho.vercel.app/api/mcp` (Streamable HTTP transport only — SSE disabled, no Redis).
- **Dependency:** `mcp-handler@1.1.0` (peer-pins `@modelcontextprotocol/sdk@1.26.0`) + `zod@^4.4.3`. Built via `createMcpHandler(...)` from `mcp-handler`, configured with `basePath: '/api'`, `disableSse: true`, no `redisUrl` — each POST spins up a fresh `McpServer` + transport per request (no session state, matching the stateless-only requirement).
- **Inbound auth:** bearer-token OAuth 2.1, via `withMcpAuth` from `mcp-handler` wrapping the handler returned by `createMcpHandler`. See "OAuth 2.1 layer" below. GET, POST, and DELETE are all routed through the same wrapped handler.
- **Tools:** `log_meal` and `save_recipe`, ported field-for-field from the local server's Zod schemas and handler logic. Each tool still calls the existing internal routes directly via `fetch()` — same URLs (`https://health-coach-rho.vercel.app/api/nutrition/meal/quick-log` and `.../recipe/quick-import`), same outbound `x-mcp-secret` header (using `process.env.MCP_SECRET`), same success/error message wording. Deliberately not refactored into a shared internal function — both this route and the eventual Pi client are meant to go through the same tested HTTP API surface. **This outbound `x-mcp-secret` header (tools → internal quick-log/quick-import routes) is unrelated to the inbound OAuth layer below and is unchanged.**
- **Local server status:** `~/Documents/Northstar/bodycipher-mcp` is **deprecated but preserved** on disk as a fallback until the remote route is verified working end-to-end (e.g. from an actual MCP client, not just curl). Do not delete or modify it.
- **Not touched:** `/api/nutrition/meal/quick-log` and `/api/nutrition/recipe/quick-import` are unchanged — they still do not verify the `x-mcp-secret` header themselves (both MCP callers send it as a matter of convention, but neither destination route checks it). Adding that check is a separate future task, not part of this change.

### OAuth 2.1 layer (added July 2026, branch feat/mcp-oauth)

Self-hosted, single-user, fully stateless OAuth 2.1 + PKCE in front of `/api/mcp`, added so Claude's phone/web "custom connector" UI (which only supports OAuth, not static headers) can authenticate. No third-party auth provider, no database, no new secrets — `MCP_SECRET` is reused as the HS256 JWT signing key for every token this layer issues. All logic lives in `lib/mcp-auth.ts` plus the routes below.

- **`x-mcp-secret` header auth on `/api/mcp` is REMOVED as of this change.** The old static-header check (`withAuth` wrapper in the route) is replaced entirely by `withMcpAuth(mcpHandler, verifyBearerToken, { required: true })`. Desktop's `mcp-remote` config (which sent `x-mcp-secret`) will need to be reconfigured for OAuth separately — not done as part of this session.
- **No persisted codes or tokens** — matches the Vercel serverless/stateless constraint. Every "code" and "token" is a signed JWT that carries everything needed to verify itself:
  - **`client_id`** (minted by `/oauth/register`) is itself a signed JWT embedding the registered `redirect_uris` array. No client table — verifying `client_id` at `/oauth/authorize` time just means verifying that JWT's signature and reading its embedded redirect URIs back out. No expiry (registration is permanent, single-user).
  - **Authorization code** (minted by `/oauth/authorize`) is a signed JWT embedding `client_id`, `redirect_uri`, and the PKCE `code_challenge`. 5-minute expiry.
  - **Access token** (minted by `/oauth/token`) is a signed JWT, ~1 year expiry (`ACCESS_TOKEN_TTL_SECONDS` in `lib/mcp-auth.ts`). No refresh-token grant — the long lifetime is the intended way this stays usable without persisted state.
  - **Known tradeoff:** because there's no server-side store of "used" codes, a code can be redeemed more than once within its 5-minute window provided the correct `code_verifier` is presented each time. Accepted deliberately given the no-storage constraint — exposure is narrow (5 min, bound to one client + redirect_uri + PKCE challenge).
- **No consent screen:** `/oauth/authorize` validates `client_id`/`redirect_uri`/PKCE and immediately redirects with a code — no login form, no "Allow" click. Deliberate simplification appropriate only because there is exactly one user and the app is already private; the one thing still validated is that `redirect_uri` matches a URI actually returned by `/oauth/register` for that `client_id`, so a forged `client_id`/`redirect_uri` pair can't be used to redirect a code to an attacker-controlled URL.
- **Endpoints:**
  | Route | Method | Purpose |
  |---|---|---|
  | `/.well-known/oauth-protected-resource` and `/.well-known/oauth-protected-resource/api/mcp` | GET | RFC 9728 protected resource metadata, served identically at both locations (see below). `resource` = `{origin}/api/mcp`, `authorization_servers` = `[{origin}]` (this app is its own AS). Built with `protectedResourceHandler` from `mcp-handler`. |
  | `/.well-known/oauth-authorization-server` | GET | RFC 8414 AS metadata. Hand-built (mcp-handler doesn't provide this side) — `authorization_endpoint`, `token_endpoint`, `registration_endpoint`, `code_challenge_methods_supported: ["S256"]`, `token_endpoint_auth_methods_supported: ["none"]`. |
  | `/oauth/register` | POST | RFC 7591 dynamic client registration. Accepts any `redirect_uris`, returns a `client_id` JWT (see above). `token_endpoint_auth_method: "none"` — public client, no `client_secret`. |
  | `/oauth/authorize` | GET | Validates `client_id`/`redirect_uri`/PKCE `code_challenge` (S256 only), immediately issues a code JWT, redirects to `redirect_uri` with `?code=...&state=...`. |
  | `/oauth/token` | POST | Verifies the code JWT, verifies `code_verifier` (SHA-256/S256) against the embedded `code_challenge`, issues the ~1-year access token JWT. Accepts `application/x-www-form-urlencoded` or JSON bodies. |
- **Verification on `/api/mcp`:** `verifyBearerToken` in `lib/mcp-auth.ts` checks the bearer token's HS256 signature (against `MCP_SECRET`) and expiry, returning an `AuthInfo` (`token`, `clientId`, `scopes`, `expiresAt`) for `withMcpAuth`. Missing/invalid token → `401` with a `WWW-Authenticate: Bearer ... resource_metadata="..."` header, per spec.
- **Dependency:** `jose@^6.2.3` added as a direct dependency (was already present transitively) — used for all JWT signing/verification. PKCE S256 hashing uses Web Crypto (`crypto.subtle.digest`), not Node's `crypto` module, so the code isn't tied to the Node runtime.
- **Tested manually end-to-end** (register → authorize → token → `initialize`/`tools/list` call against `/api/mcp`) against a local dev server before this was pushed. Also verified: missing/garbage bearer tokens get `401`; a forged `redirect_uri` not present in the client's registration is rejected with `400` before any redirect happens; a wrong `code_verifier` is rejected with `invalid_grant`.

**Bug fix (July 11, 2026) — `code_verifier does not match code_challenge` on token exchange:** `/oauth/authorize` stored the incoming `code_challenge` query param verbatim into the code JWT, while `/oauth/token` always recomputes the challenge from `code_verifier` using canonical base64url (via `pkceChallengeFromVerifier` in `lib/mcp-auth.ts`). A client that sends its `code_challenge` in standard base64 (with `+`/`/`/`=` padding) instead of base64url — a real, observed interop bug in some OAuth client implementations — would never match, even with the objectively correct verifier, since the two encodings are never byte-identical for the same underlying hash. RFC 7636 vector-tested and confirmed our own S256 hashing was already correct; the bug was purely in accepting an un-normalized challenge at storage time. **Fix:** `signAuthorizationCode` (`lib/mcp-auth.ts`) now runs the incoming `code_challenge` through `canonicalizeBase64Url()` (`+`→`-`, `/`→`_`, strips trailing `=`) before embedding it in the code JWT, so a challenge sent in either encoding matches our always-base64url recomputation. `code_verifier` is also `.trim()`-ed before hashing in `/oauth/token` as a cheap defensive measure against incidental whitespace. Confirmed: (1) the exact reported error reproduces pre-fix when `code_challenge` is sent as standard base64; (2) post-fix, that same request succeeds; (3) a genuinely wrong `code_verifier` is still correctly rejected — the fix only tolerates encoding format, not actual mismatches; (4) the real `mcp-remote` client (what Claude Desktop uses) was re-run end-to-end post-fix with no regression — its own PKCE generation was already base64url-correct, so this fix targets other/older clients exhibiting the encoding bug, not `mcp-remote` itself.

**Bug fix (July 11, 2026) — protected resource metadata now also served at the RFC 9728 path-suffixed location:** the previous session flagged `GET /.well-known/oauth-protected-resource/api/mcp` 404ing as a known follow-up. Per RFC 9728 §3.1, the well-known URI is constructed by inserting `/.well-known/oauth-protected-resource` between the host component and the resource identifier's own path — for our resource (`{origin}/api/mcp`, which has a path component, not just the bare origin), that construction yields exactly `{origin}/.well-known/oauth-protected-resource/api/mcp`, a location compliant clients may probe directly instead of the bare path. **Fix:** the metadata-building logic in `app/.well-known/oauth-protected-resource/route.ts` was extracted into `app/.well-known/oauth-protected-resource/handler.ts` (not a route file — a plain module Next.js's router ignores), and a second route file, `app/.well-known/oauth-protected-resource/api/mcp/route.ts`, re-exports the same `GET`/`OPTIONS` from it. Both URLs now return identical metadata; no route-matching conflict with `app/api/[transport]/route.ts` (different top-level path segment, `.well-known` vs `api`). Confirmed: both paths return `200` with byte-identical JSON, and the real `mcp-remote` client (which explicitly requests the suffixed path, previously logged as a 404) now gets `200` with no regression to the rest of the flow.

---

## TRAINING LOAD

### Computation (lib/trainingLoad.ts — pure module, no Supabase calls)

**Training Stress Unit (TSU) per session:**
`TSU = (duration_min × 0.5) + (zone3_plus_minutes × 2.0)`

For multiple sessions in a day: sum all TSUs. Rest days = 0.

**EWMA:**
- Acute (7-day): `acute_today = TSU_today + (1 - 1/7) × acute_yesterday`
- Chronic (28-day): `chronic_today = TSU_today + (1 - 1/28) × chronic_yesterday`
- Seed both at 0 if no prior data.

**Ratio:** acute / chronic. If chronic = 0, ratio = null. Show "Establishing baseline" until 28 days of data exist.

**Status bands:**
| Ratio | Status | Colour |
|---|---|---|
| < 0.6 | Undertraining | amber |
| 0.6–0.8 | Easy week | light green (#6DBF8A) |
| 0.8–1.3 | Building well | green |
| 1.3–1.5 | Pushing hard | amber |
| > 1.5 | Overreach risk | red |
| null | Establishing baseline | gray |

**Exported functions:**
- `computeDailyTSU(sessions: TrainingSession[]): number`
- `computeTrainingLoad(entries: DailyEntry[]): { acute, chronic, ratio, status, colour }`
- `computeTrainingLoadHistory(entries: DailyEntry[]): Array<{ date, ratio, status, colour }>`

---

## SCORING MODEL (locked — do not change without explicit instruction)

### Behavior Score

| Input | Weight |
|---|---|
| Nutrition targets hit (protein + fiber priority) | 30% |
| Supplements confirmed (morning + evening + hormones) | 20% |
| Training appropriate to HRV framework | 20% |
| Bedtime consistency (within 30 min of rolling 30-day bedtime average, fallback 21:45; −1.1 pts per minute beyond 30) | 15% |
| Active calorie target reached (600 kcal from training sessions + cycling) | 15% |

**Active calories component source:** the 600 kcal target is intentional-training energy — `active_calories` summed across `training_sessions` plus `cycling_calories`. This is distinct from Julie's ~900 kcal all-day active-calorie figure (`daily_entries.active_calories`, written by the Health Auto Export webhook) — that all-day figure is not read by this component. See `lib/scores.ts` behaviorScore() component 5.

**Critical rule:** Empty or missing fields = N/A, not zero. Weight redistributes to other components. Never penalise for unlogged optional data.

**Nutrition component source (updated April 28, 2026):** Behavior Score now reads nutrition from `daily_nutrition_summary` (not the legacy `daily_entries.nutrition` JSONB). A day counts as nutrition-logged if `meal_count > 0`. Score is recomputed server-side via `lib/scores-server.ts → recomputeScores(date)` which is called after every meal operation (POST/PUT/DELETE to `/api/nutrition/meal`) and after every `saveEntry()` save (fires `/api/scores` in the background). Key files: `lib/scores.ts` (`behaviorScore()` accepts optional `NutritionSummaryForScore`), `lib/scores-server.ts` (server-only recompute helper), `app/api/scores/route.ts` (POST endpoint).

**HRV training logic:**
- HRV >100 → hard (zone3+ ≥16 min or 2 sessions) = full | moderate = partial | easy/rest = low
- HRV 80–100 → moderate = full | easy/rest = partial (not penalised) | hard = partial penalty
- HRV 60–80 → easy = full | moderate/hard = penalty
- HRV <60 → rest = full | any training = penalty
- HRV missing → redistributes, no penalty
- Going easier than HRV recommends is **NEVER penalised.**

**Sick day exclusions (added July 26, 2026):** When `entry.context.is_sick` is true, the nutrition (1), bedtime-consistency (3), and active-calories (5) components are excluded entirely — same "component not pushed, weight redistributes" mechanism used everywhere else for missing data (not a new mechanism). Supplements (2) and training-vs-HRV (4) are **not** gated by `is_sick` — they score normally. A sick rest day scores fine on the training-vs-HRV component because rest is never penalised against the HRV framework (existing behaviour, unchanged). `behaviorScore()` reads `entry.context.is_sick` directly — no new parameter.

**Bullet display fix (added July 26, 2026):** `getBehaviorBullets()` in `app/page.tsx` only ever surfaced 3 of the 5 scoring components as bullets — supplements, bedtime, and nutrition. (There is no active-calories or training-vs-HRV bullet in the UI; those two components are not displayed here at all, sick day or not — this was already true before this change and is unrelated to it.) Of the 3 bullets that do exist, bedtime and nutrition correspond to components excluded on a sick day (see above) but were still showing stale on/off-target verdicts. Fixed: when `entry.context.is_sick` is true, the bedtime and nutrition bullets are replaced with a single `"Nutrition and bedtime — not scored (sick day)"` line (✓, not penalised) instead of computing their normal verdicts — same collapse-to-one-line pattern already used for the sick-day Outcome Score sleep bullet. The supplements bullet is unaffected and always renders normally, sick or not.

**Bedtime target (added July 11, 2026):** The bedtime-consistency comparison no longer uses a hardcoded 21:45. It uses a **30-day trailing circular average of the manual `bedtime` column** (`daily_entries.bedtime`, HH:MM), computed at runtime (compute-not-store — no DB column, no migration). Circular averaging: each HH:MM is converted to minutes-since-midnight, shifted -720 (centred on noon, where nobody's bedtime falls) mod 1440 before averaging, then shifted back — avoids the midnight-wraparound bug (e.g. 23:30 and 00:15 average to ~23:53, not ~11:52). Falls back to **'21:45'** only when zero non-null bedtimes exist in the 30-day window (no other minimum-sample threshold). Computed via `getBedtimeRolling30DayAvg(asOfDate?)` in `lib/db.ts`; `behaviorScore(entry, nutritionSummary?, bedtimeTarget = '21:45')` in `lib/scores.ts` takes the target as a third argument — the "within 30 min" scoring logic itself is unchanged. **Score recompute is forward-only:** `lib/scores-server.ts → recomputeScores(date)` computes the target as of that date; historical stored scores are not bulk-backfilled. The Today-tab Behavior Score bullet computes the same rolling average client-side from `dashEntries` (same fallback rule) via `bedtimeTargetFromEntries()` in `app/page.tsx`. The Coach interpolates the target into its "vs target" prompt commentary in `app/api/coach/route.ts` — both the wakeup-mode and endofday-mode briefing prompts, plus a runtime `.replace()` on the `Bedtime target: 21:45` line inside the `JULIE_PROFILE` context block passed to chat mode (mirrors the existing HRV-baseline replace; the `JULIE_PROFILE` constant's source text itself is untouched — this is a per-request substitution on a local copy). Does not touch `sleep_duration_min` scoring, the Dashboard Sleep trend chart, the fixed 7h30–8h30 sleep-duration target, or how `bedtime` is written to the DB. **Bullet display fix (July 11, 2026):** the Today-tab Behavior Score bullet built by `getBehaviorBullets()` in `app/page.tsx` now shows the rolling target on both the "on target" and "off target" bullet text (previously only the off-target branch showed it), and normalises the logged bedtime to `HH:MM` before display (`entry.sleep.bedtime` can come back from the DB with seconds, e.g. `22:04:00` — the parsed `h`/`m` are re-formatted zero-padded rather than interpolating the raw string). Display-only — no change to the scoring/comparison logic in `lib/scores.ts` or `lib/scores-server.ts`, or to the Coach's bedtime commentary.

### Outcome Score

| Input | Weight |
|---|---|
| HRV vs personal baseline (rolling 28-day median, fallback 88ms) | 30% |
| Sleep duration (effective, incl. naps) + Rested score | 30% |
| RHR vs 52 bpm baseline | 20% |
| CGM glucose score | 20% (redistributes to 0% if no CGM) |

**HRV baseline (added June 26, 2026):** The HRV-vs-baseline comparison no longer uses a hardcoded 88. It uses a **28-day trailing median of the manual `hrv` column** (`daily_entries.hrv`), computed at runtime (compute-not-store — no DB column, no migration). Falls back to **88** when fewer than 14 non-null readings exist in the window. Computed via `getHrvRolling28DayMedian(asOfDate?)` in `lib/db.ts`; `outcomeScore(entry, hrvBaseline = 88)` in `lib/scores.ts` takes the baseline as a second argument. **NOT computed from `apple_hrv_avg`.** Only the recovery comparison (the `>= baseline` band) is personalised — the absolute framework bands (>100 / 80–100 / 60–80 / <60) used for the train-hard/moderate/easy/rest recommendation are physiological thresholds and remain hardcoded everywhere. **Score recompute is forward-only:** `lib/scores-server.ts → recomputeScores(date)` computes the baseline as of that date; historical stored scores are not bulk-backfilled. The Today-tab Outcome bullet and the Dashboard HRV chart compute the same median client-side from `dashEntries` (same fallback rule) via `hrvBaselineFromEntries()` in `app/page.tsx`; the chart draws a flat dashed line at the current median value (per-point rolling curve deferred). The Coach interpolates the rounded median into its "vs baseline" prompt commentary.

**Effective sleep — naps counted toward Outcome Score + Coach only (added July 11, 2026):** `nap_minutes` (captured in the Sleep section, previously scored nowhere) now pads the sleep-duration figure used by two consumers: the Outcome Score's sleep-duration sub-component and the Coach's sleep/recovery commentary. Effective sleep = `(entry.sleep.duration_min ?? 0) + (entry.sleep.nap_minutes ?? 0)`, computed inline where each consumer already read `duration_min` — no new function, no DB migration. **N/A-not-zero rule preserved:** the null-check gating each component still tests raw `sleep_duration_min`, not the summed value — naps only pad an existing night's figure, they never manufacture a sleep entry (or unblank a redistributed N/A) when `sleep_duration_min` itself is null. `lib/scores.ts → outcomeScore()`: the sleep-duration score band now runs against `effectiveDur` instead of `dur`. `app/api/coach/route.ts → formatEntry()`: the `Sleep: {duration}` line shown to the Coach (for both today and every day of the 30-day history) now shows effective duration. **Explicitly untouched:** the Dashboard Sleep trend chart (still raw `sleep_duration_min`), the Sleep section UI/fields, the `nap_minutes` column itself, and Behavior Score (bedtime consistency is about timing, not duration — no Behavior Score component reads naps).

**Effective sleep — display layer (added July 26, 2026):** Two display-only follow-ups so the effective-sleep figure that already drives the Outcome Score is visible where Julie actually looks, instead of being invisible math.
- **Sleep accordion (`components/sections/SleepSection.tsx`):** a read-only "Total sleep" field now renders directly below the existing "Sleep duration" h/min inputs (untouched — same inputs, same `hmToDuration`/`durationToHM` logic). Shows `{h}h {mm}m, incl. {nap}m nap` (e.g. "8h 31m, incl. 45m nap"), computed client-side as `(duration_min ?? 0) + nap_minutes`. Only rendered when `nap_minutes` is truthy/non-zero — when nap is 0 or null, nothing new renders and the accordion is pixel-identical to before this change.
- **Today-tab Outcome Score bullet (`getOutcomeBullets()` in `app/page.tsx`):** the "Sleep Xh Ym — on/below/over target" bullet now computes `effectiveDur = duration_min + (nap_minutes ?? 0)` and uses it for both the displayed time and the on-target comparison (same 450–510min band as before, just against effective instead of raw duration) — matching what `outcomeScore()` in `lib/scores.ts` already scores against. When `nap_minutes > 0`, the bullet appends `(incl. Xm nap)`, e.g. `Sleep 8h 31m — over target (incl. 45m nap)`.
- **Untouched:** `outcomeScore()`/`lib/scores.ts` itself, the Dashboard Sleep trend chart, the Behavior Score bullet, and every other Sleep-section field.

**Sick day exclusion (added July 26, 2026):** When `entry.context.is_sick` is true, the sleep-duration sub-component of the combined duration+rested slot (30% weight) is excluded — same null-duration mechanism already used for an unlogged day: `outcomeScore()` substitutes `dur = is_sick ? null : entry.sleep.duration_min` before the existing `dur != null` gating runs. Rested still scores normally alone in that slot if present. If both duration is excluded (sick) and rested is null, the whole 30% component drops and weight redistributes to HRV/RHR as usual.

**7-day sleep debt forgiveness (added July 26, 2026):** Oversleeping past the 450–510min "on target" band is partly forgiven when Julie is running a trailing sleep deficit, so a genuine catch-up night doesn't score as "over target."
- **`getSleepDebtRolling7Day(asOfDate?)` (`lib/db.ts`):** looks at the 7 days strictly *before* `asOfDate` (default today, Europe/Berlin) — never includes the day being scored. For each of those days, skips it (counts neither deficit nor surplus) if `sleep_duration_min` is null or `is_sick` is true. For remaining days, sums `max(0, 450 - effectiveDur)` where `effectiveDur = duration_min + nap_minutes`; days ≥450min contribute 0 (no negative debt, no banking surplus). Returns 0 if fewer than 3 qualifying days exist in the window. Compute-not-store — no DB column, no migration.
- **`outcomeScore(entry, hrvBaseline, sleepDebtMinutes)` (`lib/scores.ts`):** new third parameter, default 0. Only applies when `!is_sick` and `effectiveDur > 510`, and only when `sleepDebtMinutes > 0` (zero debt — the common case — leaves band selection byte-identical to pre-forgiveness scoring; this mechanism only ever helps, never worsens a score). Computes `adjustedDur = min(570, max(forgivenessFloor, effectiveDur - sleepDebtMinutes))` and runs the existing score bands against `adjustedDur` instead of `effectiveDur` for band-selection purposes only. `forgivenessFloor` is `570` if the raw `effectiveDur` was already beyond 570, else `510` — this asymmetry is required: without it, a large enough debt could forgive a >570min day all the way down to the full/optimal (100) band, which the design explicitly forbids ("sleep beyond 570min effective should never score as full/optimal even with a large deficit to repay"). With the floor in place, overage within (510, 570] can be forgiven all the way to full, but overage beyond 570 can only ever be forgiven back to the 80-score tier, never further.
- **Wiring:** `lib/scores-server.ts → recomputeScores(date)` fetches `getSleepDebtRolling7Day(date)` the same way it already fetches `hrvBaseline`/`bedtimeTarget`, and passes it into `outcomeScore()`. Forward-only, like the other rolling computations — historical stored scores are not bulk-backfilled.
- **Today-tab bullet (`getOutcomeBullets()` in `app/page.tsx`):** client-side `sleepDebtFromEntries(dashEntries, currentDate)` mirrors `getSleepDebtRolling7Day` exactly (same window, same skip rules, same sum) and is passed into `getOutcomeBullets(entry, hrvBaseline, sleepDebt)`. When forgiveness measurably changes the scoring band (raw `effectiveDur > 510` but the debt-adjusted value would score full/on-target), the bullet reads `"Sleep Xh Ym — on target (offsetting this week's deficit)"` instead of `"— over target"`. When `entry.context.is_sick` is true, the bullet skips the verdict entirely and reads `"Sleep — not scored (sick day)"` (✓, not penalised) rather than showing below/on/over-target text.

**CGM scoring:**
- Fasting only: target ≤4.8 mmol/L = full, scales down toward 5.6
- Fasting + peaks: fasting 40% / peaks 60%. Peak target <7.5 mmol/L, penalty above 8.5
- Neither logged: CGM weight = 0%, redistributes to other components
- Missing meal peaks **never penalised**

---

## DATA MODEL (Supabase)

### `daily_entries`

**RLS disabled — but as of Sept 7, 2026 (session 2), every read/write goes through a server route on the service-role client.** `app/page.tsx` no longer imports `saveEntry`/`loadEntry`/`loadAllEntries`/`loadRecentEntries`/`isSleepLogged`/`deriveCycleDay`/`getGoalsData` from `lib/db.ts` directly — it calls `app/api/entries` (GET `?date=`/`?days=`/`?all=true`, POST), `app/api/entries/sleep-logged`, `app/api/entries/cycle-day`, and `app/api/goals` instead, all via small `fetch()` wrappers (`fetchEntry`/`fetchRecentEntries`/`fetchAllEntries`/`postEntry`/`fetchSleepLogged`/`fetchDeriveCycleDay`/`fetchGoalsData`) defined just above the `App` component in `app/page.tsx`. The seven `lib/db.ts` functions themselves are unchanged in behaviour — each gained an optional `client: SupabaseClient = supabase` parameter (same mechanism as session 1's rolling-average helpers) and the new routes call them with `supaAdmin()`. **RLS is still NOT enabled** — this is a big-surface-area change (sleep, training, nutrition-adjacent reads, cycle tracking, the Goals tab) and needs thorough testing on the branch preview first. See "RLS security fix — status" under the RLS section below for exactly what to test.

Sleep: sleep_duration_min, hrv, rhr (manual-only — HAE no longer writes to this), bedtime, **wake_time** (text, nullable — HH:MM string, written by /api/health-import from Health Auto Export sleep_analysis's `inBedEnd` field, overwrite-on-change; no manual entry point anywhere in the app; migration: `ALTER TABLE daily_entries ADD COLUMN IF NOT EXISTS wake_time text;`, run by Julie Sept 9, 2026), rested, nap_minutes, fasting_glucose_mmol
Training: cycled_today, cycling_minutes, cycling_calories (cycling transport only — training sessions in separate table), **active_calories** (integer, nullable — written by /api/health-import from Health Auto Export active_energy metric; migration: `ALTER TABLE daily_entries ADD COLUMN IF NOT EXISTS active_calories integer`), **basal_calories** (integer, nullable — written by /api/health-import from Health Auto Export basal_energy_burned metric; migration: `ALTER TABLE daily_entries ADD COLUMN IF NOT EXISTS basal_calories integer`)
HAE-only fields: **resting_hr_daytime** (integer, nullable — HAE resting_heart_rate, overwrite-if-higher; displayed in Context section), **walking_hr_avg** (integer, nullable — HAE walking_heart_rate_average, overwrite-if-higher; displayed in Context section), **walking_running_km** (numeric 8,2 nullable — HAE walking_running_distance converted to km, overwrite-if-higher; displayed in Context section), **apple_hrv_avg** (numeric, nullable — HAE heart_rate_variability in ms, plain overwrite; does NOT touch the manual `hrv` column). Migrations: `ALTER TABLE daily_entries ADD COLUMN IF NOT EXISTS resting_hr_daytime integer; ADD COLUMN IF NOT EXISTS walking_hr_avg integer; ADD COLUMN IF NOT EXISTS walking_running_km numeric(8,2); ADD COLUMN IF NOT EXISTS apple_hrv_avg numeric;`
**Health Auto Export → daily_entries mapping (as of June 14, 2026):**
- `resting_heart_rate` → `resting_hr_daytime` (integer, bpm). Uses "overwrite if higher". **`rhr` is now manual-only — HAE never writes to it.**
- `sleep_analysis` → `sleep_duration_min` (integer, minutes), `bedtime` (HH:MM string), and `wake_time` (HH:MM string, added Sept 9, 2026). HAE sends aggregated format — `totalSleep` is decimal hours (multiply × 60, round to integer); `inBedStart` is the sleep onset datetime ("YYYY-MM-DD HH:MM:SS +offset"), HH:MM portion extracted as bedtime; `inBedEnd` is extracted the same way for `wake_time`. `sleep_duration_min` and `bedtime` use COALESCE — only written if DB value is currently null (manual entry wins). `wake_time` uses **overwrite-on-change** instead (same pattern as `apple_hrv_avg` below, not COALESCE) — there's no manual entry point for wake_time anywhere in the app, so there's nothing for a webhook write to clobber; a re-sent day with an unchanged `wake_time` doesn't count toward `metricsImported`. Confirmed against a real payload (temporary debug logging session, Sept 9, 2026): `inBedEnd` and `sleepEnd` are present and identical in every real sync so far — Apple Watch's auto-detected sleep doesn't distinguish "stopped sleeping" from "got out of bed". Updated May 4, 2026 (bedtime/sleep_duration_min); wake_time added Sept 9, 2026.
- `active_energy` → `active_calories` (integer, kcal; converts kJ if needed). Uses "overwrite if higher" — written when DB value is null or incoming value is greater; a smaller or zero value never overwrites a real stored reading.
- `basal_energy_burned` → `basal_calories` (integer, kcal; converts kJ if needed). Same "overwrite if higher" rule as active_calories.
- `walking_heart_rate_average` → `walking_hr_avg` (integer, bpm). Uses "overwrite if higher".
- `walking_running_distance` → `walking_running_km` (numeric, 2 decimal places; mi→km converted if HAE sends units = 'mi'). Uses "overwrite if higher".
- `heart_rate_variability` → `apple_hrv_avg` (numeric, ms, raw decimal). **Overwrite-on-change** — written only when the incoming value differs from the stored value (equal-value skip guard). No COALESCE, no overwrite-if-higher. Latest different value wins. Once-daily, authoritative from Apple Health. Does NOT touch the manual `hrv` column.
`sleep_duration_min` and `bedtime` use COALESCE — only written if the DB value is currently null (manual entries always win). All other HAE-written fields use "overwrite if higher". `apple_hrv_avg` uses overwrite-on-change: a re-sent day with an unchanged value no longer forces a write, and a date whose only candidate field was an unchanged `apple_hrv_avg` counts as skipped (not counted in `metricsImported`). Updated June 26, 2026.

**Health Auto Export → biomarker_readings mapping (as of May 4, 2026; weight/body-fat/waist confirmed against a real payload 29 Aug 2026):**
- `vo2_max` → `biomarker_readings` with marker = `'vo2_max'`, unit = `'ml/kg/min'`. Inserted only if no existing row for that user_id + marker + recorded_on (23505 unique-violation = skip). No overwrite of existing values.
- `weight_body_mass` → `biomarker_readings` with marker = `'weight'`, unit = `'kg'` (wired Aug 18, 2026). lb→kg converted if HAE sends units = `'lb'`; rounded to 1 decimal. **Overwrite-on-change**, not insert-only-if-absent like `vo2_max` above: the route selects the existing `(user_id, marker, recorded_on)` row first, and only upserts (`onConflict: 'user_id,marker,recorded_on'`, the same unique constraint `vo2_max`'s 23505 skip already relies on) when the incoming value differs from what's stored — same rule as `apple_hrv_avg` on `daily_entries`, adapted from a plain column compare to this table's composite-key compare. A resend with an unchanged value doesn't count toward `weightImported` (tracked separately from `metricsImported`/`vo2MaxImported`, same pattern as the other biomarker). `weight_body_mass` is HAE's standard identifier for Apple's body-mass metric — **confirmed against a real payload 29 Aug 2026**, closing the long-standing "not verified before wiring" caveat that used to live here. BMI is explicitly out of scope — no BMI mapping, storage, or display.
- `body_fat_percentage` → `biomarker_readings` with marker = `'body_fat_pct'`, unit = `'%'` (added 29 Aug 2026). Arrives in `'%'` already — no unit conversion. Same overwrite-on-change select-then-conditional-upsert pattern as `weight_body_mass` above, rounded to 1 decimal, tracked as `bodyFatImported`. Deliberately no manual-entry protection (no COALESCE) — the webhook always wins, since body fat % is corrected in Apple Health, never in the app.
- `waist_circumference` → `biomarker_readings` with marker = `'waist_cm'`, unit = `'cm'` (added 29 Aug 2026). Arrives in `'cm'` already — no unit conversion. Same pattern as `body_fat_percentage` above, tracked as `waistImported`. Same webhook-always-wins rule — no manual-entry protection.

**CGM prep — glucose logging hook (added Sept 7, 2026, defensive-only prep session):** `/api/health-import` logs (metric name, units, and a 3-entry sample of raw data points — not the full payload) for any incoming metric whose name contains `"glucose"` (case-insensitive substring match, not an exact-name check) to the same debug-log path used for unrecognized metrics. Kept unchanged, broader than the real write path below, specifically so a second glucose-related data source would still surface in logs rather than going unnoticed. **The real write path is now live (Sept 7, 2026, later same session):** confirmed against a real payload — exact metric name `blood_glucose`, units `mmol/L`, value field `qty`, timestamp field `date`, source field `source` (confirmed `"GlucosePhone"`) — tightened onto an exact-name match (not the substring above) and wired to `cgm_readings`, one insert per reading, duplicate-skipped via a new `(user_id, recorded_at)` unique constraint. See DATA MODEL → `cgm_readings` above for the full mapping and migration SQL. **No read path exists yet** — the Glucose Stability card on the Today tab still shows its placeholder; wiring it to real data is a separate follow-up.
Nutrition columns: **LEGACY — do not read or write from new code**
Hydration: hydration_ml
Supplements: morning_stack_taken, morning_exceptions, evening_stack_taken, evening_exceptions, progesterone_taken, progesterone_mg, estradiol_taken (retired), estradiol_sprays (retired), **estradiol_am_taken, estradiol_am_sprays, estradiol_pm_taken, estradiol_pm_sprays, testosterone_taken, testosterone_pumps** (all six added Aug 18, 2026 — migration already run)
**Dead columns (do not wire up without an explicit decision):** `phosphatidylserine_taken`, `ashwagandha_taken`, `dim_taken` exist on this table and are mapped through `lib/types.ts` (`SupplementsData`) and `lib/db.ts` (both directions), but are read by no UI and no scoring code — actual supplement state lives entirely in `morning_stack_taken`/`evening_stack_taken` plus the `morning_exceptions`/`evening_exceptions` name arrays above. `dim_taken` in particular is misleading — DIM is a live item in the morning stack array (`MORNING_ITEMS` in `SupplementsSection.tsx`), tracked via `morning_exceptions` like every other morning item, not via this column.
**`estradiol_taken` / `estradiol_sprays` — retired Aug 18, 2026** (same dead-column pattern as above): Lenzetto moved from a single daily 3-spray dose to twice-daily dosing, so the single toggle was replaced by `estradiol_am_taken`/`estradiol_am_sprays` and `estradiol_pm_taken`/`estradiol_pm_sprays`. The two old columns remain on the table and stay mapped through `lib/types.ts` and `lib/db.ts` (both directions, `rowToEntry()` and `saveEntry()`) exactly like `phosphatidylserine_taken`/`ashwagandha_taken`/`dim_taken` — preserved for historical rows, no longer read or written by any new code (UI, scoring, or Coach). Do not drop the columns; do not re-wire them.
**`estradiol_pm_taken` / `estradiol_pm_sprays` — retired Aug 30, 2026** (same dead-column pattern as above, joining `estradiol_taken`/`estradiol_sprays` in this list): the twice-daily AM/PM split above is reverted — back to a single AM-only dose at 2 sprays. The two PM columns remain on the table and stay mapped through `lib/types.ts` and `lib/db.ts` (both directions) exactly like the other retired columns here — preserved for historical rows, no longer read or written by any new code (UI, scoring, or Coach — except `app/api/coach/route.ts`, which still reads `estradiol_pm_taken` in three places as of this entry; flagged to Julie, not yet fixed, see Supplements section above). Do not drop the columns; do not re-wire them.
Context: cycle_day, symptoms, travelling, is_sick, notes
- **`is_sick`** (boolean, `DEFAULT false` — migration already applied, July 26, 2026). Manual toggle in Context section. Drives Behavior/Outcome Score exclusions and Coach sick-day training suppression — see SCORING MODEL and Coach Tab sections.
Scores: behavior_score, outcome_score

### `training_sessions` (separate normalised table — NOT JSONB in daily_entries)

Confirmed columns (verified April 28, 2026):
- id (uuid), user_id (text), date (date), created_at (timestamptz)
- activity_type (text), duration_min (integer), zone3_plus_minutes (integer)
- active_calories (integer), notes (text)
- avg_heart_rate (integer) — **column exists in DB but is unused. Do not read or write. Do not remove from DB (would require migration). Do not re-add to UI or types.**
- **source** (text, nullable) — set to 'health_auto_export' by /api/health-import. Mapped in `loadSessionsForDates`, included in `saveEntry` reinsert — survives the delete-reinsert save cycle. Migration: `ALTER TABLE training_sessions ADD COLUMN IF NOT EXISTS source text;`
- **start_time** (timestamptz, nullable) — populated from workout `start` field by /api/health-import. Displayed on session cards as local HH:MM. Mapped in `loadSessionsForDates`, included in `saveEntry` reinsert — survives the delete-reinsert save cycle.
- **external_id** (text, nullable) — Apple Health workout UUID. Sole duplicate-detection key used by /api/health-import. Mapped in `loadSessionsForDates`, included in `saveEntry` reinsert — survives the delete-reinsert save cycle. Requires column in DB: `ALTER TABLE training_sessions ADD COLUMN IF NOT EXISTS external_id text;`
- **RLS disabled — moved onto the service-role client alongside daily_entries, Sept 7, 2026 (session 2).** `loadSessionsForDates` (bundled into `saveEntry`/`loadEntry`/`loadAllEntries`/`loadRecentEntries`, which all thread their `client` param through to it) is now called only from `app/api/entries/*` — there is no remaining browser-direct call. Coupled 1:1 to daily_entries the whole time, since sessions are always loaded/saved alongside the entry for a date — see the `daily_entries` entry above for the actual route/fetch-wrapper mechanism. **RLS is still NOT enabled** on this table either — pending the same branch-preview testing pass as daily_entries. See "RLS security fix — status" under the RLS section below.

### Activity icons (canonical mapping)

Used consistently in `TrainingSection.tsx` and `HistoryTab.tsx`:
Run / Outdoor Run / Indoor Run → 🏃 | Walk / Outdoor Walk / Indoor Walk → 🚶 | Cycling / Outdoor Cycling / Indoor Cycling → 🚴 | Swim / Pool Swim → 🏊 | Strength / eGym → 🏋️ | Rowing → 🚣 | Elliptical → 〇 | Yoga / Pilates → 🧘 | Hiking → 🥾 | HIIT → ⚡ | anything else → 🏅

Sessions joined at read time via `loadSessionsForDates()` in lib/db.ts.

**Icon cap (July 9, 2026 — mobile overflow fix):** In compact/collapsed summary displays only, session icons are capped at 4, followed by a muted "+N" pill (rounded, `--color-border-subtle` background, `--color-text-secondary` text, `--radius-full`) representing the remaining count. Applies to the Training accordion header row on the Today tab (`components/sections/TrainingSection.tsx`, `summary` rightSlot) and the History row icon list on the Dashboard (inlined in `app/page.tsx`, `HistoryRow`). Fixes icon overflow/overlap on mobile viewports on days with more than 4 sessions. Expanded views (the open Training accordion, and the expanded History row detail via `EntryDetail`) are uncapped and always show every session — nothing is hidden from the user, only the collapsed summary row is capped.

### `health_appointments`

- id (uuid PK), user_id (text DEFAULT 'julie'), appointment_type (text), interval_months (int, **nullable** — NOT NULL constraint dropped), last_completed_date (**text** — stores datetime-local strings directly, not date/timestamptz), next_due_date (**text** — same), notes (text), created_at, updated_at
- **New columns (migration applied before feature/vaccinations):** `category text` ('appointment' | 'vaccination'), `doses_in_series integer` (nullable), `doses_received integer` (nullable)
- **Date columns are text type** — avoids timezone truncation. Stores datetime-local strings directly.
- RLS disabled. **Fully moved as of Sept 7, 2026 (session 3).** All remaining browser-direct anon-key call sites are gone: `app/page.tsx` no longer imports `saveHealthAppointment`/`fetchHealthAppointments`/`seedDefaultAppointments` from `lib/db.ts` directly — it calls the new `app/api/health-appointments` route (GET → list, POST → `{ seed: true }` to seed defaults or `{ id, last_completed_date?, next_due_date?, notes? }` to update one appointment) via three small `fetch()` wrappers of the same names, defined just above the `App` component. The three `lib/db.ts` functions are unchanged in behaviour — each gained an optional `client: SupabaseClient = supabase` parameter (same mechanism as sessions 1/2) and are now called only from the new route with `supaAdmin()`. Combined with session 2's move of the health_appointments *read* inside `getGoalsData()` (via `app/api/goals`), this table has no remaining anon-key call sites anywhere in the app. **RLS is still NOT enabled** — pending Julie's branch-preview testing pass. See "RLS security fix — status" under the RLS section below for exactly what to test.
- 9 default appointment rows seeded on first load: dermatologist (6mo), dentist (6mo), gynaecologist (12mo), full_bloodwork (12mo), breast_scan (12mo), thyroid_scan (12mo), eye_optometrist (12mo), bone_density_scan (24mo), colonoscopy (120mo).
- 7 vaccination rows seeded (category = 'vaccination'): fsme, shingrix, flu, covid_booster, hepatitis_b, tetanus, typhoid. Seeded once, manually, outside app code — no code path inserts these; do not add one without an explicit ask.
- **Unique constraint on `(user_id, appointment_type)`** — added Aug 24, 2026, directly in Supabase (not via a Claude Code migration), after a dedup of duplicate appointment-category rows caused by the bug below.
- **Bug fix (Aug 24, 2026) — duplicate appointment rows from a seeding race:** `seedDefaultAppointments()` (`lib/db.ts`) was a plain `insert()`, gated by the mount-only `useEffect` in `app/page.tsx` checking `d.appointments.length === 0` — total row count, not per-`appointment_type` existence, and not atomic with the insert. Two concurrent first-loads (e.g. two tabs/windows open before the first seed landed) could both see an empty table and both insert the full 9-row default set, producing a real duplicate DB row per appointment type — the duplicate always rendered as "Not scheduled" since only `appointment_type`/`interval_months` are set on insert. Vaccination rows were unaffected (see above — never seeded by app code). **Fix:** `seedDefaultAppointments()` now `upsert()`s with `onConflict: 'user_id,appointment_type', ignoreDuplicates: true`, relying on the new unique constraint — a second/concurrent seeding call is now a harmless no-op instead of a duplicate insert. **Follow-up (same day):** a `seededAppointmentsRef` (`useRef(false)`) guard was added to the mount-only `useEffect` in `app/page.tsx` — the seed branch now also checks `!seededAppointmentsRef.current` and sets it before awaiting the seed call. Cost/log-noise cleanup only, not a correctness fix (the upsert already made a double-fire harmless): avoids the redundant `getGoalsData()` → `seedDefaultAppointments()` → `fetchHealthAppointments()` round-trip and duplicate console logging on a double-fire (e.g. React StrictMode in dev). Doesn't protect across genuinely separate mounts (two tabs, two devices) — the ref is per component instance — but the DB-level upsert already covers that case.
- **Vaccination status logic (rendered in Health Calendar tab):** Seasonal suppression for flu/covid_booster in months Apr–Sep (shows "Next: Oct [year]"). Then: Overdue / Upcoming ≤42 days / Future scheduled / Protected (single-dose + last_completed + projected future) / Not scheduled fallback.
- **Dose progress label:** "Dose X of Y complete" sub-label on the vaccination card is only rendered when `doses_in_series > 1`. Null or 1 → label absent entirely. (Fix applied May 27, 2026.)
- **Health Calendar rendering:** All vaccination rows are split from appointment rows client-side using `category === 'vaccination'`. Vaccinations appear in a separate VACCINATIONS section below the appointments list. The hero card surfaces whichever item across both appointments and vaccinations has the nearest `next_due_date`; vaccination hero items render their name via VACC_LABELS, a Syringe icon, and their badge label via vaccinationStatus().

### `biomarker_readings`

- user_id, marker (text), value (numeric), unit (text), recorded_on (date)
- markers in use: 'vo2_max', 'ldl', 'hdl', 'hba1c', 'weight' (unit 'kg' — webhook-written, overwrite-on-change, see HAE mapping note above), 'body_fat_pct' (unit '%' — webhook-written, overwrite-on-change, same as weight, as of 29 Aug 2026), 'waist_cm' (unit 'cm' — webhook-written, overwrite-on-change, same as weight, as of 29 Aug 2026), 'smm_kg' (unit 'kg' — manual-only, no webhook path; HealthKit has no skeletal muscle mass type; written via saveBodyScanReading() from the Body Composition card's Muscle tile, added 29 Aug 2026)
- **'visceral_fat_l' — dead marker (as of 29 Aug 2026).** Rows retained in the table, unread — dropped from `getGoalsData()`'s `.in('marker', [...])` filter, same dead-column convention as `phosphatidylserine_taken`/`dim_taken` on `daily_entries`. The unit is wrong for InBody-sourced readings (InBody reports visceral fat as a 1–20 *level*, not litres — the `'L'` unit only matches the one seca clinic scan reading, 1.3 L, that predates the InBody data). Anyone re-wiring this marker must settle the unit mismatch first — either normalize InBody's level onto a comparable scale or store the two sources under different markers.
- unique constraint on `(user_id, marker, recorded_on)` — already existed before the Body Composition card (confirmed via the pre-existing `vo2_max` insert-only-if-absent 23505 skip logic); no migration needed for the new markers above.

### `push_subscriptions`

Stores Web Push device subscriptions for server-initiated notifications.
- id (uuid PK), user_id (text NOT NULL DEFAULT 'julie'), endpoint (text UNIQUE NOT NULL), p256dh (text NOT NULL), auth (text NOT NULL), created_at (timestamptz DEFAULT now())
- One row per device. Upserted on `endpoint` — re-subscribing from the same device updates the row rather than creating a duplicate.
- Written by `POST /api/notifications/subscribe` (service-role client, no RLS).
- **DB migration required before testing (run in Supabase SQL editor):**
  ```sql
  CREATE TABLE push_subscriptions (
    id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id text NOT NULL DEFAULT 'julie',
    endpoint text UNIQUE NOT NULL,
    p256dh text NOT NULL,
    auth text NOT NULL,
    created_at timestamptz DEFAULT now()
  );
  ```

### `glp1_injections`

- id (uuid PK), user_id (text NOT NULL DEFAULT 'julie'), date (date NOT NULL), dose_mg (numeric NOT NULL DEFAULT 2.5), injection_number (integer NOT NULL), notes (text, nullable), created_at (timestamptz DEFAULT now())
- Migration (applied Aug 18, 2026):
  ```sql
  CREATE TABLE IF NOT EXISTS glp1_injections (
    id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id text NOT NULL DEFAULT 'julie',
    date date NOT NULL,
    dose_mg numeric NOT NULL DEFAULT 2.5,
    injection_number integer NOT NULL,
    notes text,
    created_at timestamptz DEFAULT now()
  );
  ```
- `injection_number` is assigned at log time as `(highest existing injection_number for user_id) + 1` — see `logGlp1Injection()` in `lib/db.ts`. It reflects logging order, not necessarily chronological `date` order (a backfilled past date logged via "Log a different date" still gets the next sequential number).
- No RLS mentioned in the migration. **RLS is now ENABLED as of session 1's merge** — Julie confirmed the `/api/glp1` route on the branch preview and enabled it herself in Supabase. This table's leg of the RLS fix is complete; it is the first of the four flagged tables fully closed out.
- 24-injection course, tracked client-side (`COURSE_LENGTH = 24` in `Glp1Section.tsx`) — not enforced at the DB level.
- Surfaced on the Today tab — see GLP-1 card under Today Tab above.
- **Route (added Sept 7, 2026, session 1):** `app/api/glp1/route.ts` — `GET` (list, ordered by date ascending) and `POST` (`{ date, dose_mg?, notes? }`, assigns `injection_number = max+1`), both via `supaAdmin()`. `app/page.tsx`'s `loadGlp1`/`handleLogGlp1` call this route via `fetch()` instead of talking to Supabase directly from the browser. `lib/db.ts`'s `loadGlp1Injections()`/`logGlp1Injection()` (the functions this route's logic was ported from) remain in place but unused, flagged with a comment — do not re-wire them now that RLS is on; the anon client can no longer read this table.

### `cgm_readings` (added Sept 7, 2026 — CGM integration prep; real write path added Sept 7, 2026, later same day; read path added Sept 7, 2026, later same day; table confirmed live + source filter added Sept 8, 2026)

**Correction (Sept 8, 2026 session):** the two bullets below previously said the table doesn't exist in Supabase yet and that the read path was pushed to a branch but never merged. Both were stale — confirmed by Julie this session: **the table exists and is live**, the unique constraint on `(user_id, recorded_at)` has been applied (after a duplicate row was found and manually deleted), the read path (`app/api/cgm/route.ts`) is merged, and real `GlucosePhone` data is flowing into it. No migration was run this session and none was needed — this is a documentation correction only, the table/column shapes and write logic are unchanged.

**Correction (read-path session):** confirmed to be one row **per day** — that day's running glucose average from Apple Health/HAE — not an intraday stream, contradicting the "one row per timestamped reading" note this section originally carried (written before real data existed). The table/column shapes and write logic below are unchanged; only the granularity assumption is corrected. Sensor is Libre 3 Plus, syncing via LibreLink → GlucosePhone → Apple Health ("Blood Glucose") → Health Auto Export → `/api/health-import`.

- id (uuid PK), user_id (text, default 'julie'), recorded_at (timestamptz), value_mmol (numeric), source (text), created_at (timestamptz default now())
- **Table exists and is live in Supabase** (confirmed Sept 8, 2026 — see correction above). Migration below is what Julie ran herself in the Supabase SQL editor — not run by Claude Code, per standing process. Includes the **unique constraint on `(user_id, recorded_at)`**, now applied, so a resent export containing readings already written is safely skipped rather than duplicated.
  ```sql
  CREATE TABLE IF NOT EXISTS cgm_readings (
    id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id text NOT NULL DEFAULT 'julie',
    recorded_at timestamptz NOT NULL,
    value_mmol numeric NOT NULL,
    source text,
    created_at timestamptz DEFAULT now(),
    CONSTRAINT cgm_readings_user_id_recorded_at_key UNIQUE (user_id, recorded_at)
  );
  ```
  If the table was already created from an earlier version of this migration (without the constraint), run this instead: `ALTER TABLE cgm_readings ADD CONSTRAINT cgm_readings_user_id_recorded_at_key UNIQUE (user_id, recorded_at);`
- **Real write path is now live (Sept 7, 2026).** The confirmed Health Auto Export mapping: metric name `blood_glucose` (exact match — confirmed against a real payload this session, closing the "not yet confirmed" caveat below), units `mmol/L` (no conversion), per-point value field `qty` → `value_mmol`, timestamp field `date` → `recorded_at` (a Postgres-parseable `"YYYY-MM-DD HH:MM:SS +offset"` string, passed through as-is — same convention already used for `training_sessions.start_time`), source field `source` → `source` (confirmed `"GlucosePhone"`). Every entry in the metric's `data` array is processed and inserted individually (not aggregated by day, unlike most other metrics in this route) — a real export can contain many readings once the sensor's been running. Insert via `supaAdmin()`, duplicate skip via catching a `23505` unique-violation on `(user_id, recorded_at)` — same insert-then-catch pattern as `vo2_max`'s biomarker_readings dedup above — never overwrites an existing reading. `user_id` is hardcoded `'julie'`, matching every other write in this route. Tracked as `cgmImported`/`cgmSkipped` counters (same style as `metricsImported`/`vo2MaxImported`/etc.), included in the route's summary log line and JSON response (`imported.cgm`/`imported.cgmSkipped`).
- Already goes through a server route on the service-role client (`app/api/health-import` → `supaAdmin()`, same pattern as `/api/entries`, `/api/glp1`, `/api/health-appointments`) — no browser-direct anon-key path, so this table never joins the RLS backlog that daily_entries/training_sessions/health_appointments just closed out.
- **Read path added (Sept 7, 2026, later same day); merged, live, and now source-aware (Sept 8, 2026).** `app/api/cgm/route.ts` — `GET` returns the most recent row (`recorded_at`, `value_mmol`, `source`) for `user_id = 'julie'`, or `null` if no rows exist. Service-role via `supaAdmin()`, inline query (no `lib/db.ts` function — same minimal pattern as `/api/glp1`'s `GET`), `dynamic = 'force-dynamic'` + `revalidate = 0`. **`?source=` query param (added Sept 8, 2026):** when present, the query adds `.eq('source', source)` before the existing `order/limit/maybeSingle` chain, filtering to that source before picking the most recent row; when absent, behaviour is unchanged — most recent row, any source. Added ahead of a second write feed: a future session will add real per-reading values from a **LibreView connector**, writing into this same table under a different `source` value than `GlucosePhone`. Without the filter, "most recent row, any source" would return an HAE daily average or a real connector reading unpredictably, with no way for the caller to know which it got — the `?source=` param exists so a caller can pin to one feed. Currently called as `/api/cgm?source=GlucosePhone` by the Glucose Stability card's Day Average row (Today Tab → Long-term Goals, `app/page.tsx` — see the dedicated Today Tab entry below for the full card rebuild). New `CgmReading` type in `lib/types.ts` (unchanged this session). Verified: `tsc --noEmit`, `eslint`, `next build` all clean; real data flow not exercised locally — no local `SUPABASE_SERVICE_ROLE_KEY`, same documented limitation as every other service-role route — real verification happens on the Vercel branch preview. Not merged — pushed to a new branch; no PR opened.
- **LibreView connector write path — added Sept 9, 2026 (CGM connector Session A, Task 4).** `app/api/cgm-import/route.ts` — a scheduled (not webhook-triggered) `GET` route, CRON_SECRET bearer-auth same as `app/api/notifications/check-morning`/`check-evening`. Authenticates against LibreLinkUp via the `libre-link-unofficial-api` npm library (installed Task 3) using `LIBRE_LINK_EMAIL`/`LIBRE_LINK_PASSWORD` env vars (read directly by the library itself, not by app code — see env var list above), calls `client.history()` to pull the full intraday reading window (~11h40m at ~5min cadence, confirmed via live investigation, Task 3), and upserts every reading into `cgm_readings` with `source = 'LibreView'` — distinct from the HAE feed's `'GlucosePhone'` daily-average rows already in this table. `recorded_at` is sourced from each reading's **`FactoryTimestamp`** (confirmed sensor-true-UTC), explicitly NOT `Timestamp` (confirmed ~2h ahead, consistent with a Berlin/CEST device-local adjustment) — parsed manually (`parseFactoryTimestamp()`, `Date.UTC(...)`) rather than via `new Date(raw)`, so correctness doesn't silently depend on the Vercel function runtime's ambient timezone being UTC. A reading missing a parseable `FactoryTimestamp` is skipped and counted, never silently falls back to `Timestamp` — hasn't been observed in practice (every reading in Task 3's live test had both fields) but the route is built to flag it if it ever happens rather than mask it. Dedup via `.upsert(rows, { onConflict: 'user_id,recorded_at', ignoreDuplicates: true }).select('recorded_at')` — a single bulk statement, not a per-row insert+catch loop like `/api/health-import`'s `blood_glucose` path — the returned rows are only the ones actually inserted (`ON CONFLICT DO NOTHING` semantics), so `inserted`/`duplicatesSkipped` counts are exact, not estimated. Returns and logs `{fetched, inserted, duplicatesSkipped, missingFactoryTimestamp}` per run — a sanity-check signal, since this pipeline pulls heavily overlapping windows by design (see cron schedule below) and most readings are expected to arrive as duplicates on 2 of every 3 runs. **Fragility flagged in-code:** the library's hardcoded default LibreLinkUp API version (`4.7.0`) is already below Abbott's currently-enforced minimum (`4.16.0`, confirmed via a live `403 / {status: 920, data: {minimumVersion}}` in Task 3) — the route pins `lluVersion: '4.16.0'` explicitly in the `LibreLinkClient` constructor, with a comment noting Abbott can raise this again at any time and that a future silent failure should start there. **Cron schedule (`vercel.json`):** three separate daily entries, `0 2 * * *` / `0 10 * * *` / `0 18 * * *` (UTC) — 8h apart, each individually once-per-day (Hobby-plan compliant), giving ~3h40m of overlap between consecutive ~11h40m pulls to absorb Hobby's 1-hour cron timing flex plus any real sensor connectivity gaps. Verified: `tsc --noEmit`, `eslint`, `next build` all clean (route correctly registered as dynamic `ƒ`). Not exercised against live Supabase or a live cron trigger this session — no local `SUPABASE_SERVICE_ROLE_KEY` (same documented limitation as every other service-role route) and Vercel Cron itself only fires in the deployed environment; the LibreLinkUp auth/`history()` call path itself *was* exercised live during Task 3's investigation (same library, same credentials, same `lluVersion` fix), just not through this route's actual Supabase write. Real end-to-end verification (a real cron firing this route in Preview and dedup counts as expected) happens on the Vercel branch preview. Not merged — pushed to `claude/cgm-libre-connector-session-a`; no PR opened.

### `user_profiles`

**Corrected Sept 9, 2026 (CGM connector Session A, Task 1) — the "Preferences, macro targets" description below was stale/incomplete.** Confirmed by querying the live table (anon key, `select=*`, one existing row): columns are `user_id` (**text**, not uuid — value `'julie'`, matching the same convention as `daily_entries`/`training_sessions`/`health_appointments`/`glp1_injections`), `display_name` (text), `hrv_baseline` (numeric), `rhr_baseline` (numeric), `created_at` (timestamptz). No separate `id` uuid column observed — `user_id` itself is the key, one row per user. RLS enabled/disabled state not confirmed (anon key could read the row either way — same ambiguity the legacy tables had before their own RLS sessions). **As of this session, no app code reads or writes this table** — it predates the current `daily_entries`-centric model and is not wired into `lib/db.ts`, `app/api/**`, or any component.
- **`wearing_cgm`** (boolean, `NOT NULL DEFAULT true` — migration run by Julie, Sept 9, 2026, Task 2) — added ahead of the LibreView connector work below, as a future manual override for days the CGM sensor isn't being worn. **Not yet read or written by any app code** — the migration exists but nothing wires it up yet; a future session will need to actually gate connector/UI behaviour on it.

### `cycle_tracker`

Cycle day tracking.

### Nutrition tables

See Nutrition section above. Eight tables: food_items, meal_logs, meal_log_items, daily_nutrition_summary, meal_templates, meal_template_items, recipes, recipe_ingredients.

### RLS

- Nutrition tables: user_id = auth.uid() (bypassed server-side via service role)
- Legacy tables (daily_entries, training_sessions, health_appointments, glp1_injections): user_id = 'julie' (text). Originally all four had RLS disabled, flagged by Supabase as a critical "RLS Disabled in Public" warning. Do not enable RLS on any still-disabled table below without first confirming every remaining anon-key call site for it is gone — see the multi-session fix status below and the changelog at the top of this file.
- **RLS security fix — status as of Sept 7, 2026 (session 3 of a multi-session plan):**
  - ✅ **glp1_injections — fully done.** Session 1: new `app/api/glp1/route.ts` (service-role) replaced the browser-direct anon calls. Julie confirmed it on the branch preview and **enabled RLS herself in Supabase** — this table is closed out.
  - ✅ Done (session 1): three server-only spots that were using the anon key despite running server-side now use the service-role client (`lib/scores-server.ts` recomputeScores, `app/api/scores/route.ts` GET, `app/api/coach/route.ts` getCoachContext + its direct rolling-average calls). No route changes, no behaviour change — pure client swap.
  - ✅ Done (session 2): **daily_entries and training_sessions** — every remaining browser-direct anon-key call site is gone. New routes: `app/api/entries/route.ts` (GET `?date=`/`?days=`/`?all=true`, POST), `app/api/entries/sleep-logged/route.ts` (GET `?date=`), `app/api/entries/cycle-day/route.ts` (GET), all service-role. `app/page.tsx` now calls these via `fetch()` wrappers (`fetchEntry`, `fetchRecentEntries`, `fetchAllEntries`, `postEntry`, `fetchSleepLogged`, `fetchDeriveCycleDay`) instead of importing `loadEntry`/`saveEntry`/`loadAllEntries`/`loadRecentEntries`/`isSleepLogged`/`deriveCycleDay` from `lib/db.ts` directly. Those `lib/db.ts` functions are unchanged in behaviour (each gained an optional `client` param, same mechanism as session 1) and are now called only from the new routes. **RLS is NOT yet enabled on either table** — this is the largest-surface-area piece of the whole fix (sleep, training, nutrition-adjacent reads, cycle tracking) and needs a real testing pass before it's safe. **What Julie needs to test on the branch preview before enabling RLS on daily_entries/training_sessions:** load the Today tab on several different dates (including a date with no row yet); save each of the six logging accordions (Sleep, Training, Nutrition, Hydration, Supplements, Context) and confirm the save lands and reloads correctly; add/edit/delete training sessions and confirm they survive a reload; check the "Complete yesterday's sleep" banner appears/disappears correctly; check cycle-day auto-derivation on a fresh day and the manual reset button; check the Dashboard's 30-day charts and History list still populate; check the Today-tab score cards and long-term Goals cards (VO2 Max, Cardiovascular, Glucose Stability) still load, since `getGoalsData()` moved as part of this too. Only once all of that is confirmed working should RLS go on for these two tables.
  - ✅ Done (session 2, as a side effect): `getGoalsData()` — which reads daily_entries, biomarker_readings, **and** health_appointments together in one function — moved onto the service-role client as part of the daily_entries work above, via new `app/api/goals/route.ts`.
  - ✅ Done (session 3): **health_appointments' remaining three dedicated functions** — `saveHealthAppointment`, `fetchHealthAppointments`, `seedDefaultAppointments` — are off the browser-direct anon-key pattern. New route: `app/api/health-appointments/route.ts` (GET → list; POST → `{ seed: true }` to seed defaults, or `{ id, last_completed_date?, next_due_date?, notes? }` to update one appointment), service-role. `app/page.tsx` calls this via three `fetch()` wrappers of the same names (`fetchHealthAppointments`, `seedDefaultAppointments`, `saveHealthAppointment`) instead of importing the `lib/db.ts` functions directly — same call shape at all four original call sites (the seed-on-empty effect, `handleMarkDone`, `handleSaveAppt`). Those three `lib/db.ts` functions are unchanged in behaviour (each gained an optional `client: SupabaseClient = supabase` param, same mechanism as sessions 1/2) and are now called only from the new route. Combined with session 2's `getGoalsData()` move, health_appointments has **no remaining browser-direct anon-key call sites anywhere in the app.**
  - **RLS is still NOT enabled on health_appointments** — pending a branch-preview testing pass. **What Julie needs to test before enabling RLS on health_appointments:** load the Health Log tab and confirm the appointment list and vaccination list both populate; on a fresh/empty appointments table, confirm the 9 default appointments seed correctly (no duplicates on a double-fire); mark an appointment done and confirm `last_completed_date`/`next_due_date` update and persist through a reload; edit an appointment's notes/dates via the edit flow and confirm the same; confirm the Today-tab Goals section (which reads health_appointments via `getGoalsData()`/`app/api/goals`, already on service-role since session 2) still shows the nearest-due item correctly.
  - RLS must not be enabled on daily_entries, training_sessions, or health_appointments until each table's respective testing above is confirmed by Julie on the branch preview.
  - **Bugfix pass (session 2, before merge):** live branch-preview testing surfaced a same-date Behavior Score inconsistency under rapid date-picker navigation — root-caused to a request-race in `loadDay()`/the score-loading effect (both `app/page.tsx`, fixed) and, separately, a pre-existing client-side score-recompute mismatch in the Dashboard's `HistoryRow` (unrelated to RLS or caching, predates both sessions, flagged but not fixed). Full writeup in the changelog entry at the top of this file. `export const dynamic = 'force-dynamic'` + `revalidate = 0` added to `/api/entries`, `/api/entries/sleep-logged`, `/api/entries/cycle-day`, `/api/goals`, `/api/scores`, and `/api/glp1`; `{ cache: 'no-store' }` added to every relevant client-side `fetch()` in `app/page.tsx`. `/api/health-appointments` (session 3) was built with the same `dynamic = 'force-dynamic'` + `revalidate = 0` convention from the start and its three `fetch()` wrappers use `cache: 'no-store'`, so this class of bug shouldn't recur there — not independently re-verified against a live rapid-navigation test this session (see verification note below).

---

## BACKLOG

### Known bug — VO2 Max spectrum bar target tick (open, out of scope)

The target tick on the VO2 Max spectrum bar (`app/page.tsx`, expanded VO2 Max card) renders near the current-value tick instead of at its correct position for the bar's scale (target = 40, scale 0–50+). Flagged during the Body Composition v3 session (Aug 29, 2026) as a known issue explicitly out of scope for that work — not touched, not investigated further. Fix it in its own pass.

### Active bugs (Coach)

All 7 known Coach bugs fixed in branch claude/coach-fix-1 (April 28, 2026):
1. ✅ Coach now reads from daily_nutrition_summary (service-role client) — legacy daily_entries.nutrition JSONB no longer read
2. ✅ Training override fixed — getCoachContext now calls loadSessionsForDates() so sessions populate DailyEntry.training.sessions
3. ✅ Calorie calc fixed — resolved as part of #1 (wrong data source was the cause)
4. ✅ earlyevening mode now returns recovery and training fields (not null)
5. ✅ earlyevening prompt now includes explicit LANGUAGE RULE: "this evening" not "this afternoon"
6. ✅ hrv_score was already removed from ContextSection.tsx — confirmed clean, no changes needed
7. ✅ Refresh button added to Coach header (RefreshIcon, re-calls generateBriefing)

Two follow-up bugs resolved (same branch, April 28, 2026):
A. ✅ Mode computed client-side in CoachTab.tsx using new Date().getHours() — fixes UTC vs local time mismatch that showed "posttraining" at 13:05 Munich time. Mode sent in request body; server uses client mode if provided.
B. ✅ afternoon mode now returns non-null recovery field (one sentence on HRV vs baseline and sleep quality). training remains null in afternoon mode by design.

### Meal save — past-date fix (April 30, 2026)

- **Branch:** `claude/meal-date-fix`
- **Bug:** `handleConfirm` in `MealLogger.tsx` always sent `logged_at: new Date().toISOString()` (today's wall-clock time). When the user was viewing a past date, the meal was saved under today's date but `fetchDay()` refreshed the past-date view — the meal silently disappeared.
- **Root cause of delay:** A previous fix attempt was committed to a feature branch (`claude/focused-bassi-ca8605`) that was never merged to `main`. The deployed app continued to run the unfixed code.
- **Fix:** `MealLogger` now accepts a required `currentDate: string` prop. `logged_at` is computed as `${currentDate}T${new Date().toISOString().slice(11)}` — the user-viewed date with the current wall-clock UTC time. `NutritionSection` passes `currentDate` through to `MealLogger`.

### Apple Touch Icon (branch: feat/splash-logo-scale, April 30, 2026)

- **File:** `public/apple-touch-icon.png` — 180×180 PNG, #0a0a0a background, BodyCipher mark centred with 24px padding.
- **Generated by:** `scripts/generate-icon.mjs` using the `sharp` library. Run `node scripts/generate-icon.mjs` from project root to regenerate. sharp must be installed (`npm install --os=darwin --cpu=arm64 sharp`).
- **Wired via:** `metadata.icons.apple` in `app/layout.tsx` — Next.js App Router emits `<link rel="apple-touch-icon" href="/apple-touch-icon.png" />` automatically.
- **Purpose:** When Julie saves the app to her iPhone home screen, iOS uses this icon instead of a screenshot thumbnail.

### SplashScreen (branch: feature/splash-screen, April 30, 2026)

- **File:** `components/SplashScreen.tsx`
- **Wired into:** `app/page.tsx` — rendered as first child of outermost div, above all content and tab bar. `showSplash` state controls mount/unmount.
- **Timing:** 3000ms display → 400ms CSS fade-out → `onDismiss` called → component unmounts. Total visible duration ~3400ms. The 3000ms delay is intentional and easy to adjust by changing the `setTimeout` value in the `useEffect` inside `SplashScreen.tsx`.
- **Animations:** CSS keyframes only (no JS animation library). Body form reveals top-to-bottom via `clip-path: inset()`. ECG line draws via `stroke-dashoffset`. Amber dots fade/pulse in. Wordmark and tagline rise from opacity 0.

### Layout / safe-area fixes

- **Tab bar (April 29, 2026):** outer div in `app/page.tsx` uses `paddingBottom: calc(72px + env(safe-area-inset-bottom))`. Tab bar itself has `height: 72` + `paddingBottom: env(safe-area-inset-bottom)`.
- **MealLogger bottom sheets (April 29, 2026, revised April 30, 2026):** The overlay backdrop `div` in `MealLogger.tsx` now has `paddingBottom: calc(72px + env(safe-area-inset-bottom))` so the sheet sits above the tab bar entirely. Full-sheet height reduced from `92vh` to `calc(92vh - 72px - env(safe-area-inset-bottom))` so it does not overflow above the viewport. Internal padding on ScreenMenu, Footer, ScreenSearch, and ScreenLibrary adds spacing within the sheet. Root cause: `isolation: isolate` on `<main>` creates a sandboxed stacking context — anything inside main (including the sheet at z-index 100) paints below the tab bar nav (z-index 50) in the outer context. The fix is layout, not z-index.

### Push notifications — Session 1 complete (May 4, 2026, branch: claude/nervous-jackson-862730)

PWA foundation laid. Files created/changed:
- `public/manifest.json` — PWA manifest (standalone display, dark bg, green theme)
- `public/sw.js` — Service Worker: push event → showNotification; notificationclick → confirm POSTs /api/notifications/supplement-confirm, snooze closes notification
- `components/SwRegister.tsx` — client component: registers SW on load; if already granted, calls subscribeAndSend. Exports `enableNotifications()` named function — must be called from a user gesture (iOS requirement). `app/page.tsx` calls it from the Enable Notifications button.
- `app/layout.tsx` — manifest wired into metadata; SwRegister rendered in body
- `app/api/notifications/subscribe/route.ts` — POST handler, upserts into push_subscriptions
- `package.json` — web-push ^3.6.7 added

**Before testing:** run the push_subscriptions CREATE TABLE migration in Supabase, and add the five VAPID env vars to Vercel (see push_subscriptions table entry above and Required environment variables section).

### Push notifications — Session 2 complete (May 4, 2026, branch: claude/musing-knuth-cdd141)

Cron jobs and push-sending logic built. Files created/changed:
- `vercel.json` — two Vercel cron entries: `55 7 * * *` (07:55 UTC = 09:55 Berlin summer) → `/api/notifications/check-morning`; `0 19 * * *` (19:00 UTC = 21:00 Berlin summer) → `/api/notifications/check-evening`
- `app/api/notifications/check-morning/route.ts` — GET handler. Authenticated via `Authorization: Bearer {CRON_SECRET}` (Vercel auto-generates and auto-sends CRON_SECRET for cron requests). Gets today in Europe/Berlin timezone, queries `daily_entries.morning_stack_taken` for user_id='julie'. If already true → returns `{sent: false, reason: 'already logged'}`. Otherwise fetches all `push_subscriptions` rows and sends each a push notification via web-push with title "BodyCipher", body "Good morning — have you taken your morning supplements?", actions: confirm / snooze. Returns `{sent, failed}` counts.
- `app/api/notifications/check-evening/route.ts` — Identical pattern, checks `evening_stack_taken`, body "Evening check — have you taken your evening supplements?".

**Notification payload structure (both routes):**
```json
{
  "title": "BodyCipher",
  "body": "...",
  "actions": [
    { "action": "confirm", "title": "Yes, taken" },
    { "action": "snooze", "title": "Remind me later" }
  ]
}
```

**Authentication:** Vercel automatically injects `CRON_SECRET` into the environment and sends it as `Authorization: Bearer <secret>` on every cron request. No manual setup required — Vercel manages the secret.

### Push notifications — Session 3 complete (May 4, 2026, branch: claude/musing-knuth-cdd141)

Supplement confirm route built. All three sessions complete — push notification feature is fully live.

**Files created/changed:**
- `public/sw.js` — `confirm` action handler updated to send `{ tag: event.notification.tag }` as JSON body in the POST to `/api/notifications/supplement-confirm`. Previously sent no body.
- `app/api/notifications/check-morning/route.ts` — added `tag: 'morning-supplements'` to notification payload so sw.js can identify the source.
- `app/api/notifications/check-evening/route.ts` — added `tag: 'evening-supplements'` to notification payload.
- `app/api/notifications/supplement-confirm/route.ts` — POST handler: reads `tag` from request body, maps `'morning-supplements'` → `morning_stack_taken` and `'evening-supplements'` → `evening_stack_taken`, upserts `daily_entries` row for today in Europe/Berlin time with the correct field set to `true` (onConflict: `user_id,date` so it merges rather than replaces). Returns 400 for unknown tags, 500 on DB failure, 200 + `{ ok, field, date }` on success.

**Snooze behaviour:** entirely client-side — sw.js closes the notification and takes no further action. No server call on snooze.

**Full notifications API surface (all three sessions):**

| Route | Method | Purpose |
|---|---|---|
| /api/notifications/subscribe | POST | Upsert device push subscription into push_subscriptions |
| /api/notifications/check-morning | GET (cron) | 07:55 UTC — send morning push if morning_stack_taken not true |
| /api/notifications/check-evening | GET (cron) | 19:00 UTC — send evening push if evening_stack_taken not true |
| /api/notifications/supplement-confirm | POST (SW) | Mark morning or evening stack taken in daily_entries |

### Features — next

1. **Glucose Stability expanded card** — targeted edit to GoalsTab.tsx only. No migrations. No new routes. Design specced above under Goals Tab. 🔍 CHECK: Is this the agreed next task?
2. **Coach overhaul** — full redesign. See Coach section above for known bugs. Switch to daily_nutrition_summary. Incorporate Training Load context. 30-day pattern reasoning. Fix all bugs above.
3. **BODYCIPHER.md update via Claude Code** — Training Load spec, health_appointments schema, training_sessions table, Goals tab built state. (This document supersedes the need — but Claude Code must be told to read and maintain it.)

### Features — later

1. Dashboard scores above data inputs
2. Behavior/Outcome score review — confirm empty = N/A not zero
3. CGM rolling average in Coach context
4. Food quality coaching — one nudge/day. Design TBD.
5. Responsive design / mobile layout
6. Apple Health XML import — deferred indefinitely
7. Regression analysis — revisit July 2026
8. Next-day carryover/compensation for calorie-overshoot days — explicitly deferred when the Fixed-Baseline Nutrition Targets model (August 27, 2026) was built. Not designed, not built.

### Greeting — resolved (April 28, 2026; content replaced July 12, 2026)

Replaced Anthropic API greeting with static rotating list. Client-side only, instant, zero cost. 50 greetings across 5 time bands. API route deleted.

**July 12, 2026:** The April 28 greeting set was fully replaced with a new 50-greeting set (10 per band, same 5 bands: wakeup/midmorning/afternoon/earlyevening/endofday) — content only, no logic change. Same `getGreeting()` random-per-mount selection in both `app/page.tsx` (Today tab hero) and `GoalsTab.tsx` (Goals tab hero, not in nav but preserved on disk) — each file keeps its own independent copy of the `GREETINGS` object, as before.

### Removed / will not do

- Breakfast templates
- Cyclic supplements (Ashwagandha — Phosphatidylserine re-added to evening stack July 2026, see Supplements section)
- Andreas secondary user
- avg_heart_rate in training sessions
- Template UI in My Library (tables and API routes preserved in DB)
- USDA name deduplication
- Calorie warnings or penalties
- "Cycled today (transport)" UI toggle (removed April 29, 2026 — Cycling is now a regular quick-add session type)

---

## HOW JULIE USES THIS APP

Julie receives workouts auto-imported from Apple Health via the webhook, and then manually augments them — for example, adding Zone 3+ minutes. This means training session rows contain a mix of machine-written fields and human-written fields. Any code that touches training sessions must preserve both. It cannot assume that what the UI displays is everything the row contains.

Any save logic that deletes and rewrites session rows must carry forward every field the webhook can write — currently external_id, source, active_calories, and start_time — not just the fields the UI controls. If a new webhook field is added in future, this list must be updated and the save logic must be checked.

The same overwrite risk exists for daily_entries fields. Any field that the webhook can write — currently sleep_duration_min and rhr — must not be blindly overwritten by saveEntry() if the in-memory value is null. The fix is to omit null webhook-owned fields from the upsert payload rather than writing null over a real value. In lib/db.ts saveEntry(), sleep_duration_min and rhr use conditional spread: they are only included in the flat upsert object if the in-memory value is non-null.

---

## PROCESS REMINDERS

- **Branch only.** Never push to main directly.
- **Review before merge.** Claude Code outputs change summary → Julie pastes here → go/no-go → merge.
- **DB first.** Julie runs all SQL migrations manually in Supabase before code deploys.
- **No full rewrites.** Targeted edits only. If a full rewrite seems necessary, stop and explain why first.
- **Update this file.** BODYCIPHER.md committed at end of every session. This is the only source of truth — the separate NUTRITION_DATA_MODEL.md and NUTRITION_UX_FLOW.md files in /docs are now superseded by this document. Do not maintain them separately.
