'use client'

export default function DashboardTab() {
  return (
    <div className="flex flex-col items-center justify-center py-20 gap-3">
      <span className="section-label" style={{ color: 'var(--color-text-dim)' }}>Dashboard</span>
      <p className="text-hint text-center">Full Dashboard — Phase 5</p>
    </div>
  )
}
