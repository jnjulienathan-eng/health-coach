'use client'

export default function CoachTab() {
  return (
    <div className="flex flex-col items-center justify-center py-20 gap-3">
      <span className="section-label" style={{ color: 'var(--color-text-dim)' }}>Coach</span>
      <p className="text-hint text-center">Full Coach tab — Phase 4</p>
    </div>
  )
}
